# 🏛️ 박물관 — 핵심 파일·의사결정·의존성

**Last Updated** 2026-09-14

## 핵심 파일

| 파일 | 무엇 | 주의 |
|---|---|---|
| `sims/museum-sim.html` | 외관 증축 시안 + 드로우콜 카운터 | **조형의 정본이 될 파일** — 게임 이식 시 여기 구조를 옮긴다 |
| `js/game.js:1000` `DEX` | 도감 11카테고리 | `cook` 은 `RECIPES.map` 으로 동적 생성 — 하드코딩 목록이 아니다 |
| `js/game.js` `DEX_TOTAL` | 61종 | 주석엔 "현재 33종" 이라 적혀 있으나 낡았다(실측 44 + 요리 17) |
| `js/game.js` `dexDiscover(cat, id)` | 첫 발견 등록 | **발견 시각(ms)을 저장한다** — 명판에 쓸 자산 |
| `js/game.js` `__space` | 공간 입퇴장 패턴 | 박물관도 `enterMuseum/exitMuseum` 으로 여기 합류 |
| `js/game.js:4001` `buildCafeHall` | 실내 병합 패턴의 본보기 | "색을 묶고 재질별로 합친다" — 박물관이 따를 규칙 |
| `js/game.js:2315` `mergeGeos` | 자체 병합 구현 | BufferGeometryUtils 를 쓰지 않는다. 시안도 같은 구현을 복사해 둠 |

## 의사결정 기록

| 결정 | 고른 것 | 왜 |
|---|---|---|
| 박물관 vs 콘서트장 | 박물관 먼저 | 박물관은 **있는 수집을 보이게 하는 것**(투자 대비 효과). 콘서트장은 리듬 판정이 플랫폼마다 흔들린다 |
| 톤 | 크림 벽 `0xf3e2c8` | 레퍼런스의 사실적 석조를 그대로 넣으면 박물관만 다른 게임처럼 보인다 |
| 규모 | 1층부터 증축 | 처음부터 3층이면 텅 빈 방만 잔뜩 보인다 |
| 해금 | 코인이 아니라 수집률 | 돈으로 건너뛰면 수집이 의미를 잃는다 |
| 위치 | (-13, 17.5) | 충돌체 스캔 결과 — 🍄채집 숲 7.4 / ⛏️채굴 14.5 / 마을 21.8(후보 중 최단) |

## 측정 — 병합의 효과 (시안 실측)

| 단계 | 병합 ON | 병합 OFF |
|---|---|---|
| 1층 | **9** | 151 |
| 2층 | **9** | 314 |
| 3층 | **9** | 452 |
| +특별전 | **9** | 470 |

바닥 1콜 포함. 재질 8종이라 **어느 단계든 드로우콜이 9로 일정하다** — 늘어나는 건 삼각형(1,896 → 8,982)뿐이다.
목표 25 이하를 크게 밑돈다.

## 함정

- ⚠️ `mergeGeos` 는 **첫 지오메트리의 속성 목록을 기준**으로 합친다. 직접 만든 BufferGeometry(페디먼트 등)에 `uv` 가 없으면 터진다 — 시안은 `put()` 에서 `ensureUV` 로 채운다. 게임 이식 때도 같은 처리가 필요하다
- ⚠️ 아치를 짧은 막대로 근사할 때 **막대의 길이축을 접선에 맞춰야 한다**(`rotateZ(am)`). `π/2 - am` 으로 두면 꼭대기에서 막대가 수직으로 서서 아치가 톱니처럼 벌어진다
- ⚠️ 벽에 구멍을 뚫지 않는다(CSG 는 무겁다) — 좌우 벽 + 위 인방으로 **조립**하고 아치 트림을 얹는다
- 전시물은 기존 작물·광석·물고기 메시를 재사용한다. 신규 모델링은 범위 밖
