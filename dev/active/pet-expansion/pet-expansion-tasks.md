# 🐾 펫 4종 확장 — 체크리스트

**Last Updated:** 2026-09-23

## 1. 조형 이식 — ✅ 완료
- [x] `PET_PALETTE` 에 ✨정령·🐦새·🫘흙꼬마 색 합침 (블룸 검산 20색 전부 0.85 아래 통과)
- [x] `matsFor` 에 `glow`·`film` 추가
- [x] `buildSpirit`·`buildBird`·`buildGolem` 을 `js/pet/art.js` 로 이식
- [x] `BUILD` 4종 · `buildPet(THREE, kind, stage)` 시그니처 그대로
- [x] `sims/pet-sim.html` 은 네 종 **전부 모듈에서** 받는다(복제본 0)
- [x] 🐛 `mergeStatics` 에 **반투명 가드 누락** 발견·수정 —
      film(껍질·꼬리)까지 합쳐져 ✨정령이 불투명 덩어리가 되고 있었다
- [x] 드로우콜 실측 — 🍃4 / ✨6·10·16 / 🐦5 / 🫘2·3·3

## 2. 소유 모델 — ✅ 완료 (사용자 결정: **종마다 사서 바꾸기**)
- [x] `gameState.pets` (산 종) + `gameState.pet` (동행 중) — 둘이 **한 객체**를 가리킨다
- [x] `usePet(kind)` 로만 바꿔 끼운다 (JSON 왕복에서 참조가 끊기는 함정)
- [x] 옛 세이브 마이그레이션 — `pet` 한 마리를 `pets` 로 옮긴다
- [x] 낯선 kind 는 버린다 (`petKindOf`)
- [x] `__pet.roundTrip()` — 세이브 왕복 후 참조가 다시 물리는지 검사하는 dev 훅
- [x] 실측: 구매 2종 → 교체 → works 60 → 왕복 `linked: true`, works 보존

## 3. 상점 화면 — ✅ 완료
- [x] 4종을 줄로 깔고, 줄을 누르면 프리뷰가 그 종으로 바뀐다
- [x] 버튼: `3,000🪙` / `데려가기` / `함께 있음`(비활성)
- [x] 산 종은 `N단계 · 다음까지 M번`, 안 산 종은 한 줄 설명
- [x] 이름 두 줄 쌓기 — 한 줄이면 폰(390px)에서 진행 숫자가 먼저 잘렸다
- [x] PC·폰 실측 캡처

## 4. 👣 발자국 → ✨ 이펙트 — ✅ 완료 (사용자 지시: **분류 이름만** 바꾸는 것)
- [x] 탭 라벨 `👣 발자국` → `✨ 이펙트`
- [x] 가게 첫 안내 문구
- [x] i18n(en) 갱신

## 5. i18n — ✅ 완료
- [x] 종 이름 4개 · 설명 4개 · 버튼 2개
- [x] `{0}단계 · 다 자랐어요` / `{0}단계 · 다음까지 {1}번` 은 **통문장 패턴**으로 박음
      (' · ' 글루가 반만 번역하는 함정 — beta-feedback-r3)

## 6. 코드 리뷰 반영 — ✅ 완료 (2026-09-23)
- [x] 🔴 **망토 뒤트임이 실제로는 안 뚫려 있었다** — `clothShell` 의 면 루프가 각도 점프 구간을
      한 장으로 메워, 등을 가로지르는 폭 0.655 짜리 판(삼각형 56개)이 있었다.
      캡처로는 안 잡힌다(꼬리가 그 판을 뚫은 걸 "트임 통과"로 오독했다).
      → `split` 옵션으로 열을 끊고, **아랫자락·윗단 테두리도 트임에서 끊었다**(이걸 빼면 4개가 남는다).
      검증: x=0 관통 삼각형 56 → 0
- [x] 🔴 뚫고 보니 트임 0.50 은 등이 통째로 열려 커튼 두 장이 됐다 →
      **꼬리 종류가 폭을 정한다**(`k.tail`). bushy·long 0.26 / 나머지 0.06
- [x] 🔴 일 시키는 중에 종을 바꾸면 작업량이 새 펫에게 적립됐다 → 바꾸기 전 `finishPetJob()`
- [x] 🟡 "샀지만 아무도 안 데리고 나감" 이 왕복에서 사라졌다 → 새 포맷 세이브에선 자동 소환 안 함
- [x] 🟡 시안의 `clothShell` 복제본 제거 — 시안에만 트임 버그가 남아 실물과 갈렸다
- [x] 🟡 `shot.mjs` 가 디스크 캐시를 물어 **고친 코드를 안 찍고 있었다** → `ignoreCache`
- [x] `PET_KINDS` / `petKindOf` 테스트 3개 추가 (총 970 pass)

## 남은 일
- [ ] 배포 — 웹 · 토스(`bundle_upload(memo)`) · itch 3곳
- [ ] 공지(notices_admin.html) — 토스 출시 후
