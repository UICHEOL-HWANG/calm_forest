# 🪓 도구 등급 — 단계별 체크리스트

## 1. 시안 (디자인 확정)
- [x] 🪓💧🎣 3종 0/1/2단계 시안 — `sims/tool-tier-sim.html` (커밋 `499be46`)
- [x] 2단계 시안 A/B/C 중 **C 보석 상감** 확정
- [x] 🎣 1단계를 릴 없이 강화 (코르크 그립·2색 이음·길이)
- [x] 나머지 6종 확장 — ⛏️괭이 🌰씨앗 낫 🪏삽 🔨망치 🦋포충망 (커밋 `9139f16`)
- [ ] 9종 전체를 실전 거리·밤 조명에서 확인받기

## 2. 외형을 게임에 연결
- [x] 등급 규칙을 모듈로 분리 (`js/tool-tiers.js`) — sim 이 TIER_PALETTE 를 import 한다
- [x] `toolMesh(id, tier)` 로 확장 · 주민 도구는 tier 0 고정(기본 인자 + 테스트로 잠금)
- [x] `gameState.upgrades` → tier 1 판정 (기존 유저 즉시 체감)
- [x] 업그레이드 구매 직후 `heldToolMesh` 갱신 (`refreshHeldTool`) — 상점 실측 확인
- [x] 순수 함수 테스트 (`tests/tool-tiers.test.mjs`) 13개 — 블룸 휘도 규칙까지 잠금

## 3. 새 업그레이드 5종
- [x] `UPGRADES` · 상점 `buy_*` 에 5종 추가 + `gameState.upgrades` 기본값
- [x] 효과 구현 — 수치는 `js/tool-tiers.js` 에 모았다(밸런스를 한눈에)
- [x] 잘 드는 낫 — 인접 밭 판정(`viaSickle` 플래그로 재귀 1회 제한)
- [x] i18n 영어 11줄
- [x] 트래킹 — `harvest_crop` 에 `sickle` 축 추가(낫으로 딸려 온 칸 구분). 구매는 기존 `shop_buy`/`craft_item` 이 덮는다

## 4. 2단계 — 친밀도 히든 퀘스트
- [ ] 친밀도 문턱 설계 (지금은 3단계마다 답례가 전부)
- [ ] 히든 퀘스트 공급 규칙 — `js/quests.js` 에 순수 함수로 (체인·반복과 같은 자리)
- [ ] 도면 아이템 + 작업대 제작(코인 150~300 + 재료)
- [ ] 세이브 스키마 (`gameState.toolTier` 또는 `blueprints`) + 복원
- [ ] 트래킹 `hidden_quest_open/clear` · `tool_tier2_craft`

## 5. 검증·배포
- [ ] 유닛 테스트 전부 통과
- [ ] 브라우저 실측 — 0/1/2 손에 든 모습, 주민 도구가 0단계인지, 모바일 세로
- [ ] 코드 리뷰 (`code-reviewer`)
- [ ] 웹 배포 → 토스 번들 → itch zip

## 열린 질문
- 친밀도 문턱을 몇으로? (선물 1개 = +1, 📿목걸이 = +2, 반복 의뢰 보상도 친밀도)
- 히든 퀘스트를 주민 전원이 내는가, 전문 분야와 맞는 주민만 내는가
- 도면은 도구를 지정하는가(농부=낫), 아무 도구에나 쓰는 범용인가
