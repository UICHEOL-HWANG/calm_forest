# 📖 도감 희귀종 해금 게이트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 희귀종 4종(🌈무지개 물고기·💎보석·🌈무지개반디·🌿숲 약초)을 상황이 맞는 날에만 나오게 하고, 그 안에서는 확률을 크게 올려 "조건을 알면 더 빨라지게" 만든다.

**Architecture:** 게이트 표를 순수 모듈 `js/dex-gates.js` 하나에 두고, 획득 판정(`js/game.js`)과 큐레이터 의뢰 선택(`js/museum.js`)이 **같은 표**를 본다. 누적 확률로 굴리는 세 카테고리(물고기·반딧불이·채집)는 `rollKind` 하나로 모으고, 광맥은 스폰 시 종류가 정해지는 구조라 `weightedOre()`에서 `gateOpen()`만 부른다.

**Tech Stack:** 바닐라 ES 모듈 · `node --test` (의존성 추가 없음)

**Spec:** `docs/superpowers/specs/2026-09-18-dex-gates-design.md`

## Global Constraints

- **브랜치**: `feat/habitat` — 🦋방문객 구현이 이미 얹혀 있다. 게이트를 같은 브랜치에 더해 **함께 배포**한다
- **테스트**: `npm test` — `node --test tests/*.test.mjs`. 모든 태스크 끝에 **전체**가 통과해야 한다(현재 749개)
- **커밋 메시지**: `<type>: <description>`. **attribution 줄을 붙이지 않는다**(전역 비활성)
- **게이트 표 (스펙 확정값, 그대로 옮긴다)**
  ```
  fish.rare     weather ['rain']          p 0.22
  ore.gem       weather ['fog']           p 0.28
  bug.rainbow   weather ['rain','fog']    night true   p 0.18
  forage.herb   (날씨 무관)                night true   p 0.30
  ```
- **기존 유저 무영향**: 종을 추가하지 않는다 → `DEX_TOTAL` 불변, 소식함 공지 불필요. 이미 등록한 종은 `dexDiscover`가 추가만 하므로 그대로다
- **안내 문구를 새로 만들지 않는다**: 종 이름을 밝히지 않는다(스펙 전제 4). 기존 `WEATHER_MSG`가 힌트다
- **기존 날씨 보정을 뒤집지 않는다**: 채집의 "비 오는 날 버섯↑"(`js/game.js:4239`)은 그대로 둔다
- **큐레이터 의뢰는 날씨만 본다** — `night`은 보지 않는다(하루치 시드로 고정되는데 밤낮은 하루 안에 바뀐다). 획득 판정은 `night`을 본다. **둘의 기준이 다른 것은 의도다**

---

## File Structure

| 파일 | 책임 |
|---|---|
| `js/dex-gates.js` (신규) | 게이트 표 · `gateOf` · `gateOpen` · `rollKind`. 순수 함수, DOM·THREE·gameState 의존 0 |
| `tests/dex-gates.test.mjs` (신규) | 표 잠금 · 정규화 검증 · "게이트가 기존 획득을 막지 않는다" 회귀 |
| `js/game.js` (수정) | 세 굴림 지점을 `rollKind`로 · `weightedOre`에 게이트 · `situation()` · 트래킹 |
| `js/museum.js` (수정) | `pickMissingDex`에 날씨 필터 |
| `tests/museum.test.mjs` (수정) | 닫힌 날엔 게이트 종을 안 집는지 |

---

### Task 1: `js/dex-gates.js` 순수 모듈

**Files:**
- Create: `js/dex-gates.js`
- Test: `tests/dex-gates.test.mjs`

**Interfaces:**
- Consumes: 없음 (최초 태스크)
- Produces:
  - `DEX_GATES: Record<cat, Record<id, {weather?: string[], night?: boolean, p: number}>>`
  - `gateOf(cat, id) -> gate|null`
  - `gateOpen(gate, situation) -> boolean` — `gate`가 `null`이면 `true`(게이트 없음 = 항상 열림)
  - `weatherOpen(gate, weather) -> boolean` — 날씨만 본다(큐레이터 의뢰용)
  - `rollKind(kinds, cat, situation, rnd) -> kind`
  - `situation` 형태: `{ weather: 'clear'|'rain'|'snow'|'fog', night: boolean }`
  - `kinds` 형태: `[{ id?, rarity?, p }]` — `p`는 **누적** 확률. 마지막 원소의 `p`는 1.00
  - ⚠️ `kinds` 원소의 도감 id는 `fish`만 `rarity`, 나머지는 `id`다. `rollKind`는 `k.id ?? k.rarity`로 읽는다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/dex-gates.test.mjs` 생성:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { DEX_GATES, gateOf, gateOpen, weatherOpen, rollKind } from '../js/dex-gates.js';

const sit = (weather, night = false) => ({ weather, night });

test('게이트 표 — 스펙 확정값 4종이 정확히 일치한다', () => {
  assert.deepEqual(Object.keys(DEX_GATES).sort(), ['bug', 'fish', 'forage', 'ore']);
  assert.deepEqual(DEX_GATES.fish.rare,     { weather: ['rain'], p: 0.22 });
  assert.deepEqual(DEX_GATES.ore.gem,       { weather: ['fog'], p: 0.28 });
  assert.deepEqual(DEX_GATES.bug.rainbow,   { weather: ['rain', 'fog'], night: true, p: 0.18 });
  assert.deepEqual(DEX_GATES.forage.herb,   { night: true, p: 0.30 });
});

test('게이트가 걸린 종은 희귀종뿐이다 — 흔한 종은 튜토리얼 역할이라 건드리지 않는다', () => {
  const gated = Object.values(DEX_GATES).flatMap(m => Object.keys(m));
  for (const bad of ['common', 'uncommon', 'mushroom', 'berry', 'acorn', 'stone', 'coal', 'yellow', 'blue']) {
    assert.ok(!gated.includes(bad), `${bad} 는 흔한 종이다 — 게이트를 걸면 안 된다`);
  }
});

test('gateOf: 없으면 null', () => {
  assert.deepEqual(gateOf('fish', 'rare'), DEX_GATES.fish.rare);
  assert.equal(gateOf('fish', 'common'), null);
  assert.equal(gateOf('crop', 'carrot'), null);
});

test('gateOpen: 게이트가 없으면 항상 열린다 — 게이트 없는 종이 막히면 대형 사고다', () => {
  assert.equal(gateOpen(null, sit('clear')), true);
  assert.equal(gateOpen(undefined, sit('clear')), true);
});

test('gateOpen: 날씨 게이트', () => {
  const g = gateOf('fish', 'rare');                  // ['rain']
  assert.equal(gateOpen(g, sit('rain')), true);
  assert.equal(gateOpen(g, sit('clear')), false);
  assert.equal(gateOpen(g, sit('fog')), false);
});

test('gateOpen: 날씨가 배열이면 하나만 맞아도 열린다', () => {
  const g = gateOf('bug', 'rainbow');                // ['rain','fog'] + night
  assert.equal(gateOpen(g, sit('rain', true)), true);
  assert.equal(gateOpen(g, sit('fog', true)), true);
  assert.equal(gateOpen(g, sit('snow', true)), false);
});

test('gateOpen: night 게이트는 낮에 닫힌다', () => {
  assert.equal(gateOpen(gateOf('bug', 'rainbow'), sit('rain', false)), false, '비 오는 낮엔 안 된다');
  assert.equal(gateOpen(gateOf('forage', 'herb'), sit('clear', true)), true, '약초는 날씨를 안 본다');
  assert.equal(gateOpen(gateOf('forage', 'herb'), sit('clear', false)), false);
});

test('weatherOpen: 날씨만 본다 — 큐레이터 의뢰용(하루치 시드라 밤낮을 못 본다)', () => {
  assert.equal(weatherOpen(gateOf('bug', 'rainbow'), 'rain'), true, '밤 조건을 무시한다');
  assert.equal(weatherOpen(gateOf('bug', 'rainbow'), 'clear'), false);
  assert.equal(weatherOpen(gateOf('forage', 'herb'), 'clear'), true, '약초는 날씨 조건이 없다');
  assert.equal(weatherOpen(null, 'clear'), true);
});

// ── rollKind ──────────────────────────────────────────────
const FISH = [{ rarity: 'rare', p: 0.07 }, { rarity: 'uncommon', p: 0.28 }, { rarity: 'common', p: 1.00 }];

test('rollKind: 게이트가 닫힌 날엔 그 종이 절대 안 나온다', () => {
  for (let i = 0; i < 200; i++) {
    const k = rollKind(FISH, 'fish', sit('clear'), () => i / 200);
    assert.notEqual(k.rarity, 'rare', '맑은 날에 무지개 물고기가 나왔다');
  }
});

test('rollKind: 게이트가 열린 날엔 표의 p 만큼 나온다', () => {
  let rare = 0; const N = 2000;
  for (let i = 0; i < N; i++) {
    if (rollKind(FISH, 'fish', sit('rain'), () => i / N).rarity === 'rare') rare++;
  }
  const ratio = rare / N;
  assert.ok(Math.abs(ratio - 0.22) < 0.01, `비 오는 날 rare 비중이 ${ratio.toFixed(3)} — 0.22 여야 한다`);
});

test('rollKind: 닫힌 날엔 남은 종으로 정규화된다 — 합이 1 이라 항상 뭔가 나온다', () => {
  const seen = {};
  const N = 2000;
  for (let i = 0; i < N; i++) {
    const k = rollKind(FISH, 'fish', sit('clear'), () => i / N);
    assert.ok(k, '아무것도 안 나왔다 — 정규화가 틀렸다');
    seen[k.rarity] = (seen[k.rarity] || 0) + 1;
  }
  assert.equal(seen.rare, undefined);
  // 원래 개별 가중치 uncommon .21 · common .72 → 정규화하면 .2258 / .7742
  assert.ok(Math.abs(seen.uncommon / N - 0.2258) < 0.01, `uncommon ${(seen.uncommon / N).toFixed(3)}`);
  assert.ok(Math.abs(seen.common / N - 0.7742) < 0.01, `common ${(seen.common / N).toFixed(3)}`);
});

test('rollKind: 열린 날엔 나머지가 (1 - p) 안에서 원래 비율을 지킨다', () => {
  const seen = {}; const N = 3000;
  for (let i = 0; i < N; i++) {
    const k = rollKind(FISH, 'fish', sit('rain'), () => i / N);
    seen[k.rarity] = (seen[k.rarity] || 0) + 1;
  }
  // rare .22, 나머지 .78 을 uncommon .21 : common .72 비율로 → .1766 / .6034
  assert.ok(Math.abs(seen.uncommon / N - 0.1766) < 0.012, `uncommon ${(seen.uncommon / N).toFixed(3)}`);
  assert.ok(Math.abs(seen.common / N - 0.6034) < 0.012, `common ${(seen.common / N).toFixed(3)}`);
});

test('rollKind: 게이트가 하나도 없는 카테고리는 원래 분포 그대로', () => {
  const KINDS = [{ id: 'a', p: 0.3 }, { id: 'b', p: 1.00 }];
  let a = 0; const N = 1000;
  for (let i = 0; i < N; i++) if (rollKind(KINDS, 'crop', sit('clear'), () => i / N).id === 'a') a++;
  assert.ok(Math.abs(a / N - 0.3) < 0.01, `a ${(a / N).toFixed(3)} — 원래 0.3 이어야 한다`);
});

test('rollKind: id 든 rarity 든 읽는다 — fish 만 rarity 를 쓴다', () => {
  const BUGS = [{ id: 'rainbow', p: 0.06 }, { id: 'green', p: 0.28 }, { id: 'yellow', p: 1.00 }];
  for (let i = 0; i < 200; i++) {
    assert.notEqual(rollKind(BUGS, 'bug', sit('clear', true), () => i / 200).id, 'rainbow',
      '맑은 밤엔 무지개반디가 안 나와야 한다');
  }
});

test('rollKind: 목록이 한 종뿐이고 그게 닫혀 있어도 터지지 않는다', () => {
  const ONE = [{ id: 'rare', p: 1.00 }];
  const k = rollKind(ONE, 'fish', sit('clear'), () => 0.5);
  assert.ok(k, '폴백이 없으면 undefined 를 반환해 호출부가 터진다');
});

// ⚠️ 회귀 잠금 — 이 기능의 가장 큰 위험
test('게이트는 도감 종을 늘리거나 줄이지 않는다 — DEX_TOTAL 불변', () => {
  const known = {
    fish:   ['common', 'uncommon', 'rare'],
    ore:    ['stone', 'coal', 'gem'],
    bug:    ['rainbow', 'green', 'blue', 'yellow'],
    forage: ['herb', 'acorn', 'berry', 'mushroom'],
  };
  for (const [cat, m] of Object.entries(DEX_GATES)) {
    for (const id of Object.keys(m)) {
      assert.ok(known[cat]?.includes(id), `${cat}.${id} 는 존재하지 않는 종이다`);
    }
  }
});

test('게이트 확률은 원래 확률보다 커야 한다 — 기다린 대가가 있어야 벌칙이 아니다', () => {
  const before = { 'fish.rare': 0.07, 'ore.gem': 0.10, 'bug.rainbow': 0.06, 'forage.herb': 0.12 };
  for (const [key, was] of Object.entries(before)) {
    const [cat, id] = key.split('.');
    assert.ok(DEX_GATES[cat][id].p > was,
      `${key}: 게이트 안 확률 ${DEX_GATES[cat][id].p} 가 원래 ${was} 보다 크지 않다 — 그냥 어려워진 것뿐이다`);
  }
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

```bash
npm test 2>&1 | head -20
```

Expected: FAIL — `Cannot find module '../js/dex-gates.js'`

- [ ] **Step 3: 모듈을 구현한다**

`js/dex-gates.js` 생성:

```js
// =============================================================
//  calm forest · 📖 도감 희귀종 해금 게이트 (순수 함수 — DOM/Three 의존 없음)
//  ------------------------------------------------------------
//  스펙: docs/superpowers/specs/2026-09-18-dex-gates-design.md
//  ▶ 희귀종은 "많이 하면 언젠가 나오는 것" 이었다. 상황이 맞는 날에만 나오게 하고
//    그 안에서는 확률을 크게 올린다 — **조건을 알면 더 빨라진다.**
//  ▶ 게이트마다 근거가 코드에 있다(기존 날씨 보정). 문구가 아니라 **코드**를 근거로 삼는다:
//    초안은 "비 온 뒤 약초가 돋는다" 로 잡았는데, 실제 코드는 비 오는 날 **버섯**을 밀어준다.
//  ▶ ⚠️ 게이트가 없는 종(gateOf → null)은 **항상 열림**이다. 이걸 뒤집으면 흔한 종까지 막힌다.
//  ▶ 테스트: npm test (tests/dex-gates.test.mjs)
// =============================================================

/**
 * 희귀종 게이트.
 *   weather — 열리는 날씨 **배열**. 없으면 날씨를 안 본다
 *   night   — true 면 밤에만. 없으면 시간대를 안 본다
 *   p       — 게이트가 열렸을 때의 확률(원래 확률보다 커야 한다 — 테스트가 잠근다)
 */
export const DEX_GATES = {
  // 🌈 무지개 물고기 — RAIN_DAY 에서 이미 희귀↑ 였다(catchFish)
  fish:   { rare:    { weather: ['rain'], p: 0.22 } },
  // 💎 보석 — WEATHER==='fog' 에서 이미 gemP 0.1 → 0.2 였다(weightedOre)
  ore:    { gem:     { weather: ['fog'], p: 0.28 } },
  // 🌈 무지개반디 — rain||fog 에서 이미 희귀↑ 였다(rollBugKind). 반딧불이는 원래 밤 전용
  bug:    { rainbow: { weather: ['rain', 'fog'], night: true, p: 0.18 } },
  // 🌿 숲 약초 — 이슬 맺힌 밤에 캔다. 🌙달빛 물고기(night:true) 선례.
  //   ⚠️ 날씨를 안 본다. 비 오는 날은 기존 보정이 **버섯**을 밀어주므로 근거가 반대다.
  forage: { herb:    { night: true, p: 0.30 } },
};

export function gateOf(cat, id) {
  return DEX_GATES[cat]?.[id] || null;
}

/** 날씨만 본다 — 큐레이터 의뢰는 하루치 시드로 고정돼 밤낮을 볼 수 없다(스펙 참고) */
export function weatherOpen(gate, weather) {
  if (!gate) return true;
  return !gate.weather || gate.weather.includes(weather);
}

/** @param {{weather:string, night:boolean}} situation */
export function gateOpen(gate, situation) {
  if (!gate) return true;                                    // ⚠️ 게이트 없는 종은 항상 열림
  if (!weatherOpen(gate, situation.weather)) return false;
  if (gate.night && !situation.night) return false;
  return true;
}

const keyOf = (k) => k.id ?? k.rarity;   // fish 만 rarity, 나머지는 id

/**
 * 누적 확률 목록에서 한 종을 굴린다.
 *   ① 누적 → 개별 가중치로 풀고
 *   ② 게이트가 닫힌 종은 빼고
 *   ③ 열린 종은 표의 p 로 갈아끼우고
 *   ④ 남은 가중치를 (1 - 갈아끼운 합) 안에서 정규화한다
 * @param {{id?:string, rarity?:string, p:number}[]} kinds p 는 **누적**(마지막이 1.00)
 * @param {() => number} rnd 테스트에서 고정 가능
 */
export function rollKind(kinds, cat, situation, rnd = Math.random) {
  // ① 누적 → 개별
  let prev = 0;
  const items = kinds.map(k => { const w = k.p - prev; prev = k.p; return { k, w }; });

  // ②③ 게이트 적용
  let fixed = 0, freeSum = 0;
  for (const it of items) {
    const gate = gateOf(cat, keyOf(it.k));
    if (!gate) { freeSum += it.w; continue; }
    if (!gateOpen(gate, situation)) { it.w = 0; continue; }   // 닫힘 → 제외
    it.w = gate.p; fixed += gate.p;                            // 열림 → 표의 p
  }

  // ④ 나머지를 남은 몫에 맞춰 정규화
  const room = Math.max(0, 1 - fixed);
  for (const it of items) {
    if (gateOf(cat, keyOf(it.k))) continue;                    // 갈아끼운 것은 그대로
    it.w = freeSum > 0 ? (it.w / freeSum) * room : 0;
  }

  // 굴림
  const total = items.reduce((s, it) => s + it.w, 0);
  const r = rnd() * (total || 1);
  let acc = 0;
  for (const it of items) { acc += it.w; if (r < acc) return it.k; }
  // 폴백 — 전부 닫혔거나 부동소수 오차. 게이트 없는 것 중 마지막, 없으면 목록 마지막
  return items.filter(it => !gateOf(cat, keyOf(it.k))).pop()?.k ?? kinds[kinds.length - 1];
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

```bash
npm test 2>&1 | tail -20
```

Expected: 전체 PASS.

- [ ] **Step 5: 회귀 테스트가 실제로 잡는지 증명한다**

```bash
cp js/dex-gates.js /tmp/dg.bak
perl -0pi -e 's/  if \(gate\.night && !situation\.night\) return false;/  \/\/ 일부러 제거/' js/dex-gates.js
node --test tests/dex-gates.test.mjs 2>&1 | grep -E "^not ok|# (pass|fail)" | head -5
cp /tmp/dg.bak js/dex-gates.js
node --test tests/dex-gates.test.mjs 2>&1 | grep -E "# (pass|fail)"
```

Expected: 망가뜨렸을 때 `night 게이트는 낮에 닫힌다`가 실패하고, 원복하면 전부 통과.

- [ ] **Step 6: 커밋**

```bash
git add js/dex-gates.js tests/dex-gates.test.mjs
git commit -m "feat: 📖 도감 희귀종 게이트 모듈 — 표·gateOpen·rollKind"
```

---

### Task 2: 획득 판정 세 곳을 `rollKind`로 (물고기·반딧불이·채집)

**Files:**
- Modify: `js/game.js` — import 추가 · `situation()` 신규 · `catchFish`(11808행) · `rollBugKind`(4066행) · `rollForageKind`(4237행)

**Interfaces:**
- Consumes: Task 1의 `rollKind`, `gateOf`, `gateOpen`
- Produces: `situation() -> {weather, night}` — 게이트 판정의 단일 입력

- [ ] **Step 1: import 와 `situation()` 을 넣는다**

`js/game.js` 상단 import 영역(`from './habitat.js'` 근처):

```js
import { DEX_GATES, gateOf, gateOpen, weatherOpen, rollKind } from './dex-gates.js';   // 📖 희귀종 해금 게이트
```

`habitatCtx()`(659행) 옆에:

```js
// 📖 게이트 판정 입력 — 획득 판정은 날씨와 밤낮을 둘 다 본다.
//   ⚠️ 큐레이터 의뢰는 weatherOpen(날씨만) 을 쓴다 — 하루치 시드라 밤낮을 볼 수 없다(스펙).
function situation() { return { weather: WEATHER, night: isNight() }; }
```

- [ ] **Step 2: 🐟 물고기 굴림을 바꾼다**

`catchFish()`(11808행 부근). 기존 세 줄을 갈아끼운다.

```js
  // 기존
  let roll = Math.random();
  if (buffOn('luck') || RAIN_DAY || baitActive) roll = Math.min(roll, Math.random());
  const kind = FISH_KINDS.find(k => roll <= k.p) || FISH_KINDS[FISH_KINDS.length - 1];
```

```js
  // 바꾼 뒤 — 🌈무지개 물고기는 비 오는 날에만(게이트 안에서는 22%).
  //   ⚠️ 버프·미끼의 "두 번 굴려 작은 값" 보정은 유지한다. 게이트는 후보를 제한하고,
  //      보정은 남은 후보 안에서 앞쪽(희귀)을 밀어준다 — 둘이 겹쳐도 게이트가 먼저다.
  const rnd = (buffOn('luck') || RAIN_DAY || baitActive)
    ? () => Math.min(Math.random(), Math.random())
    : Math.random;
  const kind = rollKind(FISH_KINDS, 'fish', situation(), rnd);
```

- [ ] **Step 3: 🌟 반딧불이 굴림을 바꾼다**

`rollBugKind()`(4066행). 본문을 갈아끼우고 낡은 주석을 고친다.

```js
// 종류 추첨 — 🌈무지개반디는 🌧️비·🌫️안개 낀 날 **밤**에만(게이트 안에서는 18%).
//   ⚠️ 옛 주석은 "비 온 날엔 초록반디가, 안개 낀 날엔 무지개반디가" 라고 했지만
//      실제 코드는 rain||fog 둘 다 희귀↑ 였다. 게이트가 그 동작을 명시적으로 만든 것이다.
function rollBugKind() {
  const rnd = (WEATHER === 'rain' || WEATHER === 'fog')
    ? () => Math.min(Math.random(), Math.random())
    : Math.random;
  return rollKind(BUG_KINDS, 'bug', situation(), rnd);
}
```

- [ ] **Step 4: 🍄 채집 굴림을 바꾼다**

`rollForageKind()`(4237행). **비 오는 날 버섯↑ 보정은 그대로 둔다.**

```js
// 종류 추첨 — 🌿숲 약초는 **밤**에만(게이트 안에서는 30%). 날씨는 안 본다.
//   ⚠️ 비 오는 날 "큰 값 → 목록 뒤쪽(버섯)" 보정은 **유지**한다.
//      약초 게이트를 날씨로 잡지 않은 이유가 바로 이것이다(스펙 참고).
function rollForageKind() {
  const rnd = WEATHER === 'rain'
    ? () => Math.max(Math.random(), Math.random())
    : Math.random;
  return rollKind(FORAGE_KINDS, 'forage', situation(), rnd);
}
```

- [ ] **Step 5: 테스트를 돌린다**

```bash
npm test 2>&1 | grep -E "^not ok|# (pass|fail)"
```

Expected: 전체 PASS. `js/game.js`를 소스 텍스트로 읽는 테스트들(`ga-params`·`features` 등)이 깨지지 않았는지 본다.

- [ ] **Step 6: 브라우저에서 확인한다**

```bash
cat .claude/launch.json
```

preview를 띄우고(`?dbg&weather=clear`) 콘솔에서:

```js
const g = await import('/js/dex-gates.js');
[['fish','rare'],['ore','gem'],['bug','rainbow'],['forage','herb']]
  .map(([c,i]) => [c+'.'+i, g.gateOpen(g.gateOf(c,i), { weather: 'clear', night: false })]);
```

기대: 네 개 모두 `false`. `?dbg&weather=rain`으로 다시 띄워 `fish.rare`가 `true`가 되는지 확인한다.

- [ ] **Step 7: 커밋**

```bash
git add js/game.js
git commit -m "feat: 📖 물고기·반딧불이·채집 굴림을 게이트 적용 rollKind 로"
```

---

### Task 3: ⛏️ 광물 게이트 (`weightedOre`)

**Files:**
- Modify: `js/game.js:9969` `weightedOre()`

**Interfaces:**
- Consumes: Task 1의 `gateOf`·`gateOpen`·`DEX_GATES`, Task 2의 `situation()`
- Produces: 없음 (기존 시그니처 유지 — `weightedOre() -> ore`)

**왜 `rollKind`를 쓰지 않는가:** 광맥은 스폰 시 종류가 정해지고 재생성(14초) 때 바뀌지 않는다(`js/game.js:9980` `userData = { ore, ... }`, `:10122`은 `hp`만 되돌린다). `weightedOre()`는 **이미 `WEATHER`를 보는 자리**라 거기서 `gateOpen()`만 부르면 된다.

- [ ] **Step 1: 함수를 바꾼다**

```js
// 기존 (9969행)
function weightedOre() { const gemP = WEATHER === 'fog' ? 0.2 : 0.1; const r = Math.random(); return r < 0.55 ? ORES[0] : r < 1 - gemP ? ORES[1] : ORES[2]; }
```

```js
// 바꾼 뒤 — 💎보석은 🌫️안개 낀 날에만(그 안에서 28%).
//   ⚠️ 광맥은 **스폰 시** 종류가 정해지고 재생성 때 바뀌지 않는다(spawnOreRock 의 userData.ore).
//      즉 동굴을 지을 때의 날씨가 그 세션 광맥 구성을 정한다 — 안개 낀 날 동굴에 가야 한다.
//   ⚠️ rollKind 를 쓰지 않는 이유: 누적 확률 구조가 아니고, 여기가 이미 WEATHER 를 보는 자리다.
function weightedOre() {
  const gemP = gateOpen(gateOf('ore', 'gem'), situation()) ? DEX_GATES.ore.gem.p : 0;
  const r = Math.random();
  if (r < gemP) return ORES[2];                    // 💎 보석
  const t = (r - gemP) / (1 - gemP);               // 나머지를 0~1 로 다시 펴서 원래 55:35 비율 유지
  return t < 0.55 / 0.9 ? ORES[0] : ORES[1];
}
```

- [ ] **Step 2: 분포를 실제로 잰다**

```bash
node --input-type=module -e "
import { DEX_GATES, gateOf, gateOpen } from './js/dex-gates.js';
const ORES = [{id:'stone'},{id:'coal'},{id:'gem'}];
function weightedOre(sit) {
  const gemP = gateOpen(gateOf('ore','gem'), sit) ? DEX_GATES.ore.gem.p : 0;
  const r = Math.random();
  if (r < gemP) return ORES[2];
  const t = (r - gemP) / (1 - gemP);
  return t < 0.55/0.9 ? ORES[0] : ORES[1];
}
for (const w of ['clear','fog']) {
  const c = {stone:0,coal:0,gem:0};
  for (let i=0;i<200000;i++) c[weightedOre({weather:w,night:false}).id]++;
  console.log(w, Object.fromEntries(Object.entries(c).map(([k,v])=>[k,(v/2000).toFixed(1)+'%'])));
}"
```

Expected:
```
clear { stone: '61.1%', coal: '38.9%', gem: '0.0%' }
fog   { stone: '44.0%', coal: '28.0%', gem: '28.0%' }
```

숫자가 다르면 식을 고친다 — **측정이 기준이다.**

- [ ] **Step 3: 테스트를 돌린다**

```bash
npm test 2>&1 | grep -E "^not ok|# (pass|fail)"
```

Expected: 전체 PASS.

- [ ] **Step 4: 브라우저에서 확인한다**

`?dbg&weather=fog`로 띄우고 콘솔에서:

```js
__mine.enter();
await new Promise(r => setTimeout(r, 1200));
const ids = __mine.ores().map(o => o[2]);
({ 광맥: ids, 보석수: ids.filter(x => x === 'gem').length, 전체: ids.length })
```

기대: 안개 낀 날엔 보석이 섞인다(15개 중 기대 4.2개). `?dbg&weather=clear`로 다시 띄우면 **보석 0개**.

- [ ] **Step 5: 커밋**

```bash
git add js/game.js
git commit -m "feat: 📖 보석은 안개 낀 날에만 — weightedOre 게이트"
```

---

### Task 4: 큐레이터 의뢰가 닫힌 종을 집지 않게

**Files:**
- Modify: `js/museum.js:73` 부근 `pickMissingDex`
- Modify: `js/game.js:913` — `pickMissingDex` 호출에 날씨 전달
- Test: `tests/museum.test.mjs`

**Interfaces:**
- Consumes: Task 1의 `weatherOpen`·`gateOf`
- Produces: `pickMissingDex(dex, DEX, seed, ctx)` — `ctx.weather`를 추가로 받는다(없으면 게이트를 안 본다)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/museum.test.mjs` 끝에 추가:

```js
// ⚠️ 맑은 날에 🌈무지개 물고기를 집으면 그날 의뢰가 불가능해진다(🦋방문객 DEX_NEVER 와 같은 함정).
//    DEX_NEVER 로 전부 막는 건 손해다 — 제일 재미있는 종을 큐레이터가 영영 못 집는다.
//    대신 **오늘 날씨에 열린 종만** 후보에 넣는다.
test('큐레이터는 오늘 날씨에 닫힌 게이트 종을 집지 않는다', () => {
  const GATED = { fish: [{ id: 'rare' }, { id: 'uncommon' }, { id: 'common' }] };
  const owned = { fish: { uncommon: 1, common: 1 } };   // rare 만 안 가졌다
  for (let seed = 0; seed < 30; seed++) {
    assert.equal(pickMissingDex(owned, GATED, seed, { weather: 'clear' }), null,
      '맑은 날에 🌈무지개 물고기를 집었다 — 그날 못 깨는 의뢰가 된다');
    const rain = pickMissingDex(owned, GATED, seed, { weather: 'rain' });
    assert.deepEqual(rain && { cat: rain.cat, id: rain.id }, { cat: 'fish', id: 'rare' },
      '비 오는 날엔 집어야 한다');
  }
});

test('큐레이터 의뢰는 밤 조건을 보지 않는다 — 하루치 시드라 밤낮이 하루 안에 바뀐다', () => {
  const GATED = { forage: [{ id: 'herb' }, { id: 'mushroom' }] };
  const owned = { forage: { mushroom: 1 } };
  const pick = pickMissingDex(owned, GATED, 7, { weather: 'clear' });
  assert.equal(pick && pick.id, 'herb',
    '밤 종을 낮에 집지 못하게 하면 의뢰가 사라진다 — 플레이어가 밤까지 기다리면 된다');
});

test('weather 를 안 넘기면 게이트를 보지 않는다(하위 호환)', () => {
  const GATED = { fish: [{ id: 'rare' }, { id: 'common' }] };
  const owned = { fish: { common: 1 } };
  assert.ok(pickMissingDex(owned, GATED, 3, {}), 'ctx.weather 없이도 동작해야 한다');
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

```bash
node --test tests/museum.test.mjs 2>&1 | grep -E "^not ok|error:|# (pass|fail)" | head -6
```

Expected: `큐레이터는 오늘 날씨에 닫힌 게이트 종을 집지 않는다`가 실패.

- [ ] **Step 3: `js/museum.js` 를 고친다**

파일 상단에 import를 더한다:

```js
import { gateOf, weatherOpen } from './dex-gates.js';   // 📖 희귀종 게이트 — 획득 판정과 같은 표를 본다
```

후보 수집 루프에 필터를 더한다. 기존:

```js
    for (const e of DEX[cat]) if (!dex[cat]?.[e.id]) pool.push({ ...e, cat });
```

바꾼 뒤:

```js
    for (const e of DEX[cat]) {
      if (dex[cat]?.[e.id]) continue;
      // 📖 오늘 날씨에 닫힌 희귀종은 집지 않는다 — 그날 못 깨는 의뢰가 된다.
      //   ⚠️ night 은 보지 않는다. 의뢰는 하루치 시드로 고정되는데 밤낮은 하루 안에 바뀌므로,
      //      밤 종을 낮에 걸러내면 의뢰가 사라진다. 플레이어가 밤까지 기다리면 된다.
      if (ctx.weather && !weatherOpen(gateOf(cat, e.id), ctx.weather)) continue;
      pool.push({ ...e, cat });
    }
```

주석 블록(`⚠️ 집으면 안 되는 카테고리`)에 한 줄 더한다:

```js
//   · 게이트가 걸린 희귀종 — 오늘 날씨에 닫혀 있으면 후보에서 뺀다(js/dex-gates.js).
//     카테고리 단위가 아니라 **종 단위**라 DEX_NEVER 로는 못 막는다.
```

- [ ] **Step 4: `js/game.js` 호출부에 날씨를 넘긴다**

```bash
grep -n "pickMissingDex(gameState.dex" -B 6 js/game.js
```

`ctx`가 만들어지는 자리에 한 줄 더한다:

```js
      ctx.weather = WEATHER;   // 📖 오늘 닫힌 희귀종은 의뢰로 나오지 않게(js/dex-gates.js)
```

- [ ] **Step 5: 테스트를 돌린다**

```bash
npm test 2>&1 | grep -E "^not ok|# (pass|fail)"
```

Expected: 전체 PASS.

- [ ] **Step 6: 커밋**

```bash
git add js/museum.js js/game.js tests/museum.test.mjs
git commit -m "feat: 📖 큐레이터가 오늘 닫힌 희귀종을 집지 않게"
```

---

### Task 5: 트래킹 · 전체 검증

**Files:**
- Modify: `js/game.js` — `dex_gate_blocked` 발사
- Test: `tests/ga-params.test.mjs` (기존)

**Interfaces:**
- Consumes: Task 1~4 전부

- [ ] **Step 1: `trackGateBlocked` 를 만든다**

⚠️ 굴림마다 쏘면 이벤트가 폭주한다. **카테고리별 하루 한 번**만 쏜다.

`js/game.js`의 `situation()` 옆에:

```js
// 📖 [GA4] 게이트가 닫혀 못 얻은 순간 — 게이트가 너무 조이는지 보는 축.
//   ⚠️ 굴림마다 쏘면 이벤트가 폭주한다. 카테고리별로 **하루 한 번**만 쏜다.
const _gateBlockedToday = {};
function trackGateBlocked(cat, id) {
  if (gameState.dex[cat]?.[id]) return;                 // 이미 가진 사람은 관심 없다
  const key = cat + ':' + todayStr();
  if (_gateBlockedToday[key]) return;
  _gateBlockedToday[key] = 1;
  const s = situation();
  trackEvent('dex_gate_blocked', { category: cat, entry: id, weather: s.weather, night: s.night ? 1 : 0 });
}
```

- [ ] **Step 2: 네 획득 지점에 한 줄씩 넣는다**

각 굴림 **직후**, 게이트가 닫혀 있으면 부른다.

```js
// catchFish() — rollKind 호출 뒤
if (!gateOpen(gateOf('fish', 'rare'), situation())) trackGateBlocked('fish', 'rare');
```

같은 꼴로:
- `rollBugKind()` 를 부르는 쪽(반딧불이를 실제로 잡은 지점, `js/game.js:4194` `dexDiscover('bug', ...)` 근처) → `trackGateBlocked('bug', 'rainbow')`
- `rollForageKind()` 를 부르는 쪽(`:4344` `dexDiscover('forage', ...)` 근처) → `trackGateBlocked('forage', 'herb')`
- 채굴 성공 지점(`:10111` `dexDiscover('ore', ...)` 근처) → `trackGateBlocked('ore', 'gem')`

⚠️ **굴림 함수 안이 아니라 "플레이어가 그 활동을 실제로 한 지점"** 에 넣는다. 반딧불이는 스폰마다 `rollBugKind`가 돌지만 플레이어가 잡은 건 아니다.

- [ ] **Step 3: GA4 파라미터 규칙을 확인한다**

```bash
node --test tests/ga-params.test.mjs 2>&1 | grep -E "^not ok|error:|# (pass|fail)"
```

이 테스트가 이벤트 파라미터 규칙을 잠근다. 실패하면 이름을 규칙에 맞춘다.

- [ ] **Step 4: 트래킹이 실제로 발사되는지 확인한다**

⚠️ **`trackEvent` 를 코드에 적어 놓는 것과 실제로 발사되는 것은 다르다.**
그리고 ⚠️⚠️ **dev 세션에서는 `onTrack` 훅조차 안 불린다** — `js/analytics.js` 의 조기 반환이
훅보다 **위**에 있다:

```js
export function trackEvent(name, params = {}) {
  if (IS_DEV_SESSION) return;                 // ← 여기서 끝. 훅도 못 간다
  const payload = { ...params, ts, platform };
  trackHooks.forEach(cb => cb(name, payload));
}
```

그래서 검증은 **임시 패치 → 확인 → 원복** 으로 한다. 실서비스 GA4 전송은 계속 막힌 채다.

```bash
cp js/analytics.js /tmp/analytics.bak
python3 - <<'PY'
import pathlib
p = pathlib.Path('js/analytics.js'); s = p.read_text()
old = "  if (IS_DEV_SESSION) return;"
new = "  // ⏳ 임시 검증용 — 아래로 내림"
i = s.index(old)
s = s[:i] + new + s[i+len(old):]
j = s.index("trackHooks.forEach(cb => { try { cb(name, payload); } catch (e) {} });")
j = s.index("\n", j) + 1
s = s[:j] + "  if (IS_DEV_SESSION) return;   // ⏳ 임시 — 훅만 통과, GA4 전송은 여전히 차단\n" + s[j:]
p.write_text(s)
PY
```

preview 를 `?dbg&weather=clear` 로 띄우고 콘솔에서:

```js
const a = await import('/js/analytics.js');
window.__seen = []; a.onTrack((n, p) => window.__seen.push({ name: n, params: p }));
// … 맑은 날 낚시를 한 번 한 뒤 …
window.__seen.filter(e => e.name === 'dex_gate_blocked');
```

기대: `{ category:'fish', entry:'rare', weather:'clear', night:0 }` 가 **한 번**. 여러 번 낚아도 늘지 않는다.

⚠️ **폭주도 반드시 재본다.** 🦋방문객에서 실제로 겪었다 — `visitor_nearmiss` 가 매 프레임 도는
함수에 있어 조건 안팎을 10번 오가니 같은 이벤트가 10개 나갔다. 중복 방지를 넣고 다시 재서
**10회 왕복 = 1개**를 확인했다. `dex_gate_blocked` 도 같은 방식으로 왕복 테스트를 한다.

끝나면 **반드시 원복**한다:

```bash
cp /tmp/analytics.bak js/analytics.js && git diff --stat js/analytics.js   # 출력이 비어야 한다
```

- [ ] **Step 5: 전체 테스트 · 네 게이트 재현**

```bash
npm test
```

| 종 | 재현 URL | 기대 |
|---|---|---|
| 🌈 무지개 물고기 | `?dbg&weather=rain` | 나온다. `?weather=clear`에선 **절대 안 나온다** |
| 💎 보석 | `?dbg&weather=fog` | 광맥에 섞인다. `clear`에선 0개 |
| 🌈 무지개반디 | `?dbg&weather=rain&time=0.75` | 나온다. `?weather=clear&time=0.75`에선 안 나온다 |
| 🌿 숲 약초 | `?dbg&time=0.75` | 밤 채집 숲에서 나온다. `?time=0.30`(낮)에선 안 나온다 |

확률은 콘솔에서 직접 잰다:

```js
const g = await import('/js/dex-gates.js');
const FISH = [{rarity:'rare',p:0.07},{rarity:'uncommon',p:0.28},{rarity:'common',p:1.00}];
const count = (w) => { const c={}; for(let i=0;i<20000;i++){ const k=g.rollKind(FISH,'fish',{weather:w,night:false}); c[k.rarity]=(c[k.rarity]||0)+1; } return c; };
({ 맑음: count('clear'), 비: count('rain') })
```

기대: 맑음에 `rare` 없음, 비에 `rare` ≈ 4400/20000.

- [ ] **Step 6: 기존 유저 보호 재확인**

```bash
node --test tests/museum.test.mjs 2>&1 | grep -E "기존|배지|층이 닫히지|# (pass|fail)"
```

🦋방문객 때 넣은 보호 테스트(배지 회수 없음 · 층 후퇴 없음 · 세이브 병합)가 전부 통과해야 한다. 게이트는 종을 추가하지 않으므로 `DEX_TOTAL`이 그대로다.

- [ ] **Step 7: 커밋**

```bash
git add js/game.js
git commit -m "feat: 📖 dex_gate_blocked 트래킹 — 게이트가 조이는지 보는 축"
```

---

## Self-Review

**스펙 커버리지**

| 스펙 항목 | 태스크 |
|---|---|
| 게이트 표 4종 (확정값) | Task 1 (테스트로 잠금) |
| 확률을 같이 올린다 | Task 1 (원래 확률보다 커야 한다는 테스트) |
| `rollKind` 누적→개별→정규화 | Task 1 · Task 2 |
| `ore`는 `rollKind`를 안 쓴다 | Task 3 (근거 주석 포함) |
| 큐레이터 의뢰 필터 | Task 4 |
| 큐레이터는 `night`을 안 본다 | Task 4 (테스트로 잠금) |
| 기존 날씨 보정 유지 | Task 2 Step 4 (채집 버섯↑) |
| 기존 유저 무영향 | Task 5 Step 6 |
| 트래킹 `dex_gate_blocked` | Task 5 (**실제 발사까지 확인** — Step 4) |
| 안내 문구 신규 없음 | 전 태스크 — UI 변경 0 |
| 소식함 공지 불필요 | — (종을 안 늘림) |

**타입 일관성**

- `situation()` — Task 2에서 정의, Task 3·5에서 같은 이름으로 호출 ✅
- `gateOpen(gate, situation)` vs `weatherOpen(gate, weather)` — 두 번째 인자가 객체 vs 문자열이다. Task 1에서 명시, Task 3은 `gateOpen`, Task 4는 `weatherOpen` ✅
- `rollKind(kinds, cat, situation, rnd)` — Task 1 정의, Task 2 세 곳 동일 ✅
- `keyOf(k) = k.id ?? k.rarity` — `FISH_KINDS`만 `rarity`라 Task 1에서 흡수 ✅
- `trackGateBlocked(cat, id)` — Task 5 Step 1 정의, Step 2에서 네 번 호출 ✅

**구현 중 판단이 필요한 지점**

- **Task 3 Step 2**: 분포 측정이 기준이다. 숫자가 다르면 식을 고친다
- **Task 4 Step 4**: `ctx`가 만들어지는 자리는 `grep`으로 찾는다 — 행 번호를 못 박지 않았다
- **Task 5 Step 2**: 트래킹은 굴림 함수가 아니라 **플레이어가 그 활동을 실제로 한 지점**에 넣는다
- **Task 5 Step 4**: `onTrack` 훅 이름은 `js/analytics.js`에서 확인한다
