# 🍎 과수원 언덕 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 마을 정동쪽에 해금형 과수원 공간을 만들고, 심으면 매일 열매가 열리는 과일나무 5종으로 "내일 다시 올 이유"를 만든다.

**Architecture:** 이 저장소의 확립된 패턴 — **순수 규칙 모듈 + `node:test` → `js/game.js` 는 그리기만.** 과일 표·성장·물·수확·일일 정산 판정을 전부 `js/orchard.js` 에 순수 함수로 두고, `game.js` 는 그 결과를 3D 로 그리고 액션에 연결하기만 한다. 해금은 기존 베타 맵 게이트 UI 를 그대로 쓰되 `js/tuning.js` 의 잠금 규칙을 "날짜 게이트 또는 진행도 게이트"로 일반화한다.

**Tech Stack:** Vanilla ES modules · Three.js · `node:test` (`npm test`) · Supabase(Postgres) · Cloudflare Worker · GA4

**Spec:** `docs/superpowers/specs/2026-09-17-orchard-design.md`

**Worktree:** `.claude/worktrees/orchard` · branch `feat/orchard` (main 기준). 모든 명령은 이 디렉터리에서 실행한다.

## Global Constraints

- **`js/game.js` 에 규칙을 넣지 않는다.** 이미 11,475줄이다. 새 판정 로직은 전부 `js/orchard.js` 로 간다. `game.js` 에는 배선·렌더만.
- **과일 id 는 이 다섯 문자열만 쓴다:** `apple` `pear` `peach` `persimmon` `chestnut`. GA4 파라미터·`econ_logs.item`·도감 키·세이브 필드가 전부 같은 값이다. 표시용 한글은 절대 보내지 않는다.
- **자리 번호로 종류를 대신하지 않는다.** `slot:3` 같은 값은 금지(`courier:3` 복원 불가 사고).
- **GA4 예약 파라미터 금지:** `source` `medium` `campaign` `campaign_id` `term` `content`. 물 출처는 반드시 `method` 라는 이름을 쓴다.
- **나무는 죽지 않는다.** 물을 안 주면 그날 열매가 안 열릴 뿐이다.
- **나무 자리 10개 상한**, 시냇가 자리 4개.
- **커밋 메시지**는 `<type>: <emoji> <한국어 한 줄>` 형식(저장소 관례). 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- **테스트 실행은 언제나** `npm test` (= `node --test tests/*.test.mjs`).

## File Structure

| 파일 | 책임 | 상태 |
|---|---|---|
| `js/orchard.js` | 과일 표 · 성장/물/수확/정산 판정. DOM·Three 의존 0 | 신설 |
| `tests/orchard.test.mjs` | 위 규칙의 수치를 못 박음 | 신설 |
| `js/tuning.js` | `PROGRESS_GATE` · `isProgressLocked` 추가, `isMapLocked` 일반화 | 수정 |
| `tests/map-gate.test.mjs` | 진행도 게이트 테스트 추가 | 수정 |
| `js/game.js` | `atOrchard` 공간 · 입구 · 렌더 · 액션 · 세이브 배선 | 수정 |
| `sql/orchard_events.sql` | `orchard_events` 테이블 DDL | 신설 |
| `functions/api/orchard-events.js` | 이벤트 적재 엔드포인트 | 신설 |
| `worker/index.js` | 위 엔드포인트 라우트 등록 (**빠뜨리면 404**) | 수정 |

**Phase 1 (Task 1–4)** 은 순수 규칙 + 테스트라 단독으로 머지·검증 가능하다. **Phase 2 (Task 5–9)** 가 게임에 붙이는 배선, **Phase 3 (Task 10–11)** 이 트래킹 인프라다.

---

## Task 1: 과일 표와 성장 규칙

**Files:**
- Create: `js/orchard.js`
- Test: `tests/orchard.test.mjs`

**Interfaces:**
- Consumes: 없음 (이 파일이 시작점)
- Produces: `FRUITS` (배열), `TREE_SLOTS = 10`, `STREAM_SLOTS = 4`, `STREAM_R = 5`, `YIELD_PER_DAY = 2`, `CAP_DAYS = 3`, `fruitOf(id) → 과일객체|null`, `fruitKeyOf(id) → string`, `sapKeyOf(id) → string`, `growDaysOf(id) → number`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/orchard.test.mjs` 를 새로 만든다:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { FRUITS, TREE_SLOTS, STREAM_SLOTS, STREAM_R, YIELD_PER_DAY, CAP_DAYS,
         fruitOf, fruitKeyOf, sapKeyOf, growDaysOf } from '../js/orchard.js';

test('FRUITS: 스펙 §5 표 — 5종의 id·묘목값·자람일·판매가가 정확히 일치한다', () => {
  assert.deepEqual(FRUITS.map(f => f.id), ['apple', 'pear', 'peach', 'persimmon', 'chestnut']);
  assert.deepEqual(FRUITS.map(f => f.sapCoin), [90, 130, 180, 240, 300]);
  assert.deepEqual(FRUITS.map(f => f.growDays), [3, 3, 4, 4, 5]);
  assert.deepEqual(FRUITS.map(f => f.price), [5, 6, 8, 10, 12]);
  for (const f of FRUITS) {
    assert.ok(f.fruitColor > 0 && f.leafColor > 0, `${f.id}: 색이 있어야 인스턴스로 그린다`);
    assert.ok(typeof f.name === 'string' && f.name.length > 0, `${f.id}: 한국어 이름`);
  }
});

test('상한 상수: 나무 자리 10 · 시냇가 4 · 반경 5 · 하루 2개 · 3일치까지', () => {
  assert.equal(TREE_SLOTS, 10);
  assert.equal(STREAM_SLOTS, 4);
  assert.ok(STREAM_SLOTS < TREE_SLOTS, '시냇가는 일부여야 "어디 심을까" 고민이 생긴다');
  assert.equal(STREAM_R, 5);
  assert.equal(YIELD_PER_DAY, 2);
  assert.equal(CAP_DAYS, 3);
});

test('fruitOf: id 로 찾고, 모르는 값은 null', () => {
  assert.equal(fruitOf('apple').price, 5);
  assert.equal(fruitOf('bogus'), null);
  assert.equal(fruitOf(null), null);
});

test('fruitKeyOf / sapKeyOf: 인벤 키는 id 에서만 파생된다 — 한글·자리번호 금지', () => {
  assert.equal(fruitKeyOf('apple'), 'apple');
  assert.equal(sapKeyOf('apple'), 'sap_apple');
  assert.equal(sapKeyOf('chestnut'), 'sap_chestnut');
});

test('growDaysOf: 모르는 id 는 가장 짧은 3일로 안전하게 떨어진다', () => {
  assert.equal(growDaysOf('chestnut'), 5);
  assert.equal(growDaysOf('bogus'), 3);
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `npm test`
Expected: FAIL — `Cannot find module '../js/orchard.js'`

- [ ] **Step 3: 최소 구현을 쓴다**

`js/orchard.js` 를 새로 만든다:

```js
// =============================================================
//  calm forest · 🍎 과수원 규칙 (순수 함수 — DOM/Three 의존 없음)
//  ------------------------------------------------------------
//  스펙: docs/superpowers/specs/2026-09-17-orchard-design.md
//  ▶ 나무는 죽지 않는다. 물을 안 주면 그날 열매가 안 열릴 뿐이다.
//  ▶ 과일 id 5종은 GA4·econ_logs·도감·세이브에서 전부 같은 문자열을 쓴다.
//    자리 번호(slot:3)로 종류를 대신하지 않는다 — courier:3 복원 불가 사고.
//  ▶ 테스트: npm test (tests/orchard.test.mjs)
// =============================================================

export const TREE_SLOTS = 10;     // 나무 자리 상한 — 코인 인플레 방어(스펙 §5)
export const STREAM_SLOTS = 4;    // 그중 시냇가(물 면제) 자리
export const STREAM_R = 5;        // 시냇물 중심선에서의 면제 반경 — 💧우물과 같은 문법
export const YIELD_PER_DAY = 2;   // 하루에 달리는 열매 수
export const CAP_DAYS = 3;        // 안 따면 3일치까지 쌓이고 멈춘다

/** 과일 표 — 스펙 §5. fruitColor/leafColor 는 인스턴스 색(새 메시 없이 색만 다르게) */
export const FRUITS = [
  { id: 'apple',     name: '사과',   ico: '🍎', sapCoin: 90,  growDays: 3, price: 5,  fruitColor: 0xd64a42, leafColor: 0x5f9e52 },
  { id: 'pear',      name: '배',     ico: '🍐', sapCoin: 130, growDays: 3, price: 6,  fruitColor: 0xd9cf7a, leafColor: 0x6aa85a },
  { id: 'peach',     name: '복숭아', ico: '🍑', sapCoin: 180, growDays: 4, price: 8,  fruitColor: 0xef9aad, leafColor: 0x7ab069 },
  { id: 'persimmon', name: '감',     ico: '🍊', sapCoin: 240, growDays: 4, price: 10, fruitColor: 0xe08a30, leafColor: 0x55924a },
  { id: 'chestnut',  name: '밤',     ico: '🌰', sapCoin: 300, growDays: 5, price: 12, fruitColor: 0x7a5433, leafColor: 0x4a833f },
];

export function fruitOf(id) { return FRUITS.find(f => f.id === id) || null; }
export function fruitKeyOf(id) { return id; }          // 과일 인벤 키 = id 그대로
export function sapKeyOf(id) { return 'sap_' + id; }   // 묘목 인벤 키
export function growDaysOf(id) { return fruitOf(id)?.growDays ?? 3; }
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `npm test`
Expected: PASS — 새 테스트 5개 통과, 기존 테스트 전부 그대로 통과

- [ ] **Step 5: 커밋**

```bash
git add js/orchard.js tests/orchard.test.mjs
git commit -m "$(cat <<'EOF'
feat: 🍎 과수원 과일 5종 표와 키 규칙을 만든다

사과·배·복숭아·감·밤의 묘목값·자람일·판매가·색을 한 곳에 두고,
인벤 키를 id 에서만 파생시켜 GA4·econ_logs·도감이 같은 문자열을 쓰게 한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 물 판정과 수확량 규칙

**Files:**
- Modify: `js/orchard.js`
- Test: `tests/orchard.test.mjs`

**Interfaces:**
- Consumes: Task 1 의 `STREAM_R`, `YIELD_PER_DAY`, `CAP_DAYS`
- Produces:
  - `nearStream(tree, stream) → boolean` — `tree` 는 `{x, z}`, `stream` 은 `[{x, z}, ...]` 중심선 점 목록
  - `isWatered(tree, stream) → boolean` — 시냇가면 항상 true, 아니면 `tree.watered`
  - `harvestable(tree) → number` — 지금 딸 수 있는 열매 수(0 이상)
  - `capped(tree) → boolean` — 3일치 상한에 걸렸는지

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/orchard.test.mjs` 의 import 줄에 네 함수를 더하고, 파일 끝에 이어 붙인다:

```js
// 위쪽 import 에 nearStream, isWatered, harvestable, capped 를 추가한다

const STREAM = [{ x: 0, z: 0 }, { x: 0, z: 10 }, { x: 0, z: 20 }];   // z축으로 흐르는 시냇물

test('nearStream: 중심선의 가장 가까운 점 기준 반경 5 — 시작점 기준이 아니다', () => {
  assert.equal(nearStream({ x: 3, z: 10 }, STREAM), true,  '중간 지점 옆 3');
  assert.equal(nearStream({ x: 6, z: 10 }, STREAM), false, '중간 지점 옆 6');
  assert.equal(nearStream({ x: 3, z: 20 }, STREAM), true,  '끝점 옆도 면제 — 시작점만 보면 여기가 false 로 샌다');
  assert.equal(nearStream({ x: 0, z: 40 }, STREAM), false, '시냇물에서 한참 아래');
  assert.equal(nearStream({ x: 0, z: 0 }, []), false, '시냇물이 없으면 면제 없음');
});

test('isWatered: 시냇가는 물을 안 줘도 켜져 있다 · 밖은 watered 플래그를 따른다', () => {
  assert.equal(isWatered({ x: 1, z: 10, watered: false }, STREAM), true, '시냇가는 플래그 무시');
  assert.equal(isWatered({ x: 9, z: 10, watered: false }, STREAM), false);
  assert.equal(isWatered({ x: 9, z: 10, watered: true }, STREAM), true);
});

test('harvestable: 쌓인 열매를 그대로 돌려주고, 자라는 중인 나무는 0', () => {
  assert.equal(harvestable({ stage: 'mature', fruit: 4 }), 4);
  assert.equal(harvestable({ stage: 'mature', fruit: 0 }), 0);
  assert.equal(harvestable({ stage: 'growing', fruit: 2 }), 0, '아직 안 자란 나무에서 따면 안 된다');
  assert.equal(harvestable({ stage: 'sapling', fruit: 0 }), 0);
});

test('capped: 3일치(6개) 도달이면 더 안 쌓인다', () => {
  assert.equal(capped({ fruit: 6 }), true);
  assert.equal(capped({ fruit: 5 }), false);
  assert.equal(capped({ fruit: 7 }), true, '어쩌다 넘어도 상한으로 본다');
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `npm test`
Expected: FAIL — `nearStream is not a function` (또는 import 실패)

- [ ] **Step 3: 최소 구현을 쓴다**

`js/orchard.js` 끝에 이어 붙인다:

```js
/** 시냇물 중심선 점 목록 중 **가장 가까운 점**까지의 거리가 STREAM_R 이내인가.
 *  시작점만 보면 시냇물 아래쪽 나무가 통째로 새므로 전 구간을 본다. */
export function nearStream(tree, stream = []) {
  return stream.some(p => Math.hypot(p.x - tree.x, p.z - tree.z) <= STREAM_R);
}

/** 정산 시점에 "물이 있는 상태"인가. 시냇가면 플래그와 무관하게 항상 true. */
export function isWatered(tree, stream = []) {
  return nearStream(tree, stream) || !!tree.watered;
}

/** 지금 딸 수 있는 열매 수. 다 자란 나무만 딸 수 있다. */
export function harvestable(tree) {
  return tree.stage === 'mature' ? Math.max(0, tree.fruit || 0) : 0;
}

/** 상한(3일치)에 걸려 더 안 쌓이는가. */
export function capped(tree) {
  return (tree.fruit || 0) >= YIELD_PER_DAY * CAP_DAYS;
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `npm test`
Expected: PASS — 전부 통과

- [ ] **Step 5: 커밋**

```bash
git add js/orchard.js tests/orchard.test.mjs
git commit -m "$(cat <<'EOF'
feat: 🍎 시냇가 물 면제와 수확 상한 규칙을 만든다

시냇물 중심선 전 구간에서 반경 5 안이면 물주기를 면제한다.
안 따면 3일치(6개)까지만 쌓여 매일 올 이유가 유지된다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 일일 정산 규칙

**Files:**
- Modify: `js/orchard.js`
- Test: `tests/orchard.test.mjs`

**Interfaces:**
- Consumes: Task 1 의 `growDaysOf`, `YIELD_PER_DAY`, `CAP_DAYS`, Task 2 의 `isWatered`, `capped`
- Produces: `settleTrees(trees, stream, days = 1) → { trees, matured, fruited, capped }`
  - 새 배열을 돌려준다(원본 불변 — 저장소 코딩 규칙)
  - `matured`/`fruited`/`capped` 는 호출부가 그대로 `trackEvent` 에 넘길 수 있는 모양이다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/orchard.test.mjs` 의 import 에 `settleTrees` 를 더하고 끝에 이어 붙인다:

```js
const mk = (o = {}) => ({ x: 20, z: 20, kind: 'apple', stage: 'growing', age: 0, watered: false, fruit: 0, ...o });

test('settleTrees: 원본을 건드리지 않고 새 배열을 돌려준다', () => {
  const trees = [mk()];
  const out = settleTrees(trees, STREAM, 1);
  assert.notEqual(out.trees, trees);
  assert.notEqual(out.trees[0], trees[0]);
  assert.equal(trees[0].age, 0, '원본 나무의 나이가 안 변해야 한다');
});

test('settleTrees: 자람일을 채우면 mature 로 바뀌고 matured 에 kind 가 실린다', () => {
  const out = settleTrees([mk({ age: 2 })], STREAM, 1);   // 사과 3일
  assert.equal(out.trees[0].stage, 'mature');
  assert.deepEqual(out.matured, [{ kind: 'apple', grew_days: 3 }]);
});

test('settleTrees: 다 자라기 전엔 열매가 안 달린다', () => {
  const out = settleTrees([mk({ age: 0 })], STREAM, 1);
  assert.equal(out.trees[0].stage, 'growing');
  assert.equal(out.trees[0].fruit, 0);
  assert.deepEqual(out.fruited, []);
});

test('settleTrees: 물이 있으면 하루 2개가 달리고 watered 는 꺼진다', () => {
  const out = settleTrees([mk({ x: 40, z: 40, stage: 'mature', watered: true })], STREAM, 1);
  assert.equal(out.trees[0].fruit, 2);
  assert.equal(out.trees[0].watered, false, '정산 직후 플래그는 꺼진다 — 매일 다시 줘야 한다');
  assert.deepEqual(out.fruited, [{ kind: 'apple', n: 2, watered: 1 }]);
});

test('settleTrees: 물이 없으면 열매가 안 달리지만 나무는 살아 있다', () => {
  const out = settleTrees([mk({ x: 40, z: 40, stage: 'mature', watered: false })], STREAM, 1);
  assert.equal(out.trees[0].fruit, 0);
  assert.equal(out.trees[0].stage, 'mature', '죽지 않는다');
  assert.deepEqual(out.fruited, [{ kind: 'apple', n: 0, watered: 0 }]);
});

test('settleTrees: 시냇가 나무는 물을 안 줘도 열린다', () => {
  const out = settleTrees([mk({ x: 1, z: 10, stage: 'mature', watered: false })], STREAM, 1);
  assert.equal(out.trees[0].fruit, 2);
});

test('settleTrees: 상한 6개를 넘지 않고, 걸린 나무는 capped 에 실린다', () => {
  const out = settleTrees([mk({ x: 1, z: 10, stage: 'mature', fruit: 5 })], STREAM, 1);
  assert.equal(out.trees[0].fruit, 6);
  assert.deepEqual(out.capped, [{ kind: 'apple' }]);
});

test('settleTrees: 여러 날이 지났으면 그만큼 돌지만 상한은 지켜진다', () => {
  const out = settleTrees([mk({ x: 1, z: 10, stage: 'mature' })], STREAM, 5);
  assert.equal(out.trees[0].fruit, 6, '5일이 지나도 3일치까지만');
});

test('settleTrees: days 가 0 이하면 아무것도 안 한다', () => {
  const out = settleTrees([mk({ stage: 'mature' })], STREAM, 0);
  assert.equal(out.trees[0].fruit, 0);
  assert.deepEqual(out.fruited, []);
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `npm test`
Expected: FAIL — `settleTrees is not a function`

- [ ] **Step 3: 최소 구현을 쓴다**

`js/orchard.js` 끝에 이어 붙인다:

```js
/**
 * 하루(또는 여러 날) 정산. 🐝벌통·🐛해충과 같은 날짜 게이트에서 한 번 호출한다.
 *   trees  : [{ x, z, kind, stage:'sapling'|'growing'|'mature', age, watered, fruit }]
 *   stream : 시냇물 중심선 점 목록
 *   days   : 경과 일수(접속이 끊겼던 날 포함)
 * 원본을 바꾸지 않고 새 배열을 돌려준다(저장소 코딩 규칙 — 불변).
 */
export function settleTrees(trees = [], stream = [], days = 1) {
  const matured = [], fruited = [], cappedList = [];
  if (days <= 0) return { trees: trees.map(t => ({ ...t })), matured, fruited, capped: cappedList };

  const out = trees.map(t => {
    let tree = { ...t };
    for (let d = 0; d < days; d++) {
      if (tree.stage === 'sapling' || tree.stage === 'growing') {
        const age = (tree.age || 0) + 1;
        const need = growDaysOf(tree.kind);
        tree = age >= need ? { ...tree, age, stage: 'mature' } : { ...tree, age, stage: 'growing' };
        if (tree.stage === 'mature') matured.push({ kind: tree.kind, grew_days: need });
        continue;                     // 다 자란 날엔 열매가 안 달린다 — 다음 날부터
      }
      // mature
      if (capped(tree)) { cappedList.push({ kind: tree.kind }); continue; }
      const wet = isWatered(tree, stream);
      const before = tree.fruit || 0;
      const fruit = Math.min(YIELD_PER_DAY * CAP_DAYS, before + (wet ? YIELD_PER_DAY : 0));
      fruited.push({ kind: tree.kind, n: fruit - before, watered: wet ? 1 : 0 });
      tree = { ...tree, fruit, watered: false };
      if (capped(tree)) cappedList.push({ kind: tree.kind });
    }
    return tree;
  });
  return { trees: out, matured, fruited, capped: cappedList };
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `npm test`
Expected: PASS

> 실패하면 십중팔구 "다 자란 그날 열매가 달려버리는" 경우다. `continue` 로 그날은 건너뛴다.

- [ ] **Step 5: 커밋**

```bash
git add js/orchard.js tests/orchard.test.mjs
git commit -m "$(cat <<'EOF'
feat: 🍎 과수원 일일 정산 규칙을 만든다

성장·물 판정·열매 부착·상한을 한 함수에 모으고 원본을 바꾸지 않는다.
오프라인으로 여러 날이 지나도 같은 함수가 그만큼 돌아 결과가 갈리지 않는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 진행도 해금 게이트

**Files:**
- Modify: `js/tuning.js:86-91` (`isMapLocked`)
- Test: `tests/map-gate.test.mjs`

**Interfaces:**
- Consumes: 기존 `mapOpenDay`, `betaDay`
- Produces: `PROGRESS_GATE` (표), `isProgressLocked(map, progress) → boolean`, 그리고 **`isMapLocked` 가 인자 객체에 `progress` 를 하나 더 받는다**

**왜 필요한가:** 현재 `isMapLocked` 첫 줄이 `if (!/^beta_/.test(variant)) return false` 라 일반 유저에겐 아무것도 잠기지 않는다. 과수원은 베타와 무관하게 잠겨야 한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/map-gate.test.mjs` 의 import 에 `PROGRESS_GATE, isProgressLocked` 를 더하고 끝에 이어 붙인다:

```js
test('PROGRESS_GATE: 과수원은 고급 작물 1회 수확으로 열린다', () => {
  assert.deepEqual(PROGRESS_GATE.orchard, { kind: 'advHarvest', n: 1 });
});

test('isProgressLocked: 카운터가 기준에 못 미치면 잠긴다', () => {
  assert.equal(isProgressLocked('orchard', { advHarvest: 0 }), true);
  assert.equal(isProgressLocked('orchard', { advHarvest: 1 }), false);
  assert.equal(isProgressLocked('orchard', { advHarvest: 9 }), false);
  assert.equal(isProgressLocked('orchard', {}), true, '카운터가 없으면 0 으로 본다');
  assert.equal(isProgressLocked('orchard', undefined), true, '옛 세이브 방어');
});

test('isProgressLocked: 표에 없는 맵은 잠그지 않는다', () => {
  assert.equal(isProgressLocked('sea', { advHarvest: 0 }), false);
});

test('isMapLocked: 일반 유저에게도 진행도 게이트는 적용된다', () => {
  const base = { variant: 'prod', mapOrder: null, createdAtIso: null, nowMs: T('2026-09-17T02:00:00Z') };
  assert.equal(isMapLocked({ ...base, progress: { advHarvest: 0 } }, 'orchard'), true);
  assert.equal(isMapLocked({ ...base, progress: { advHarvest: 1 } }, 'orchard'), false);
  assert.equal(isMapLocked({ ...base, progress: { advHarvest: 0 } }, 'sea'), false, '날짜 게이트 맵은 그대로');
});

test('isMapLocked: 베타 날짜 게이트는 한 글자도 안 바뀐다', () => {
  const beta = { variant: 'beta_a', mapOrder: 'sea_first', createdAtIso: '2026-09-10T01:00:00Z' };
  assert.equal(isMapLocked({ ...beta, nowMs: T('2026-09-10T02:00:00Z') }, 'sea'), true,  'D1 엔 잠김');
  assert.equal(isMapLocked({ ...beta, nowMs: T('2026-09-12T02:00:00Z') }, 'sea'), false, 'D3 에 열림');
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `npm test`
Expected: FAIL — `PROGRESS_GATE` import 실패

- [ ] **Step 3: 최소 구현을 쓴다**

`js/tuning.js` 의 `isMapLocked` 를 아래로 교체하고 위에 표를 추가한다:

```js
/** 진행도 해금 표 — 날짜가 아니라 "무엇을 해냈나"로 여는 공간.
 *  세이브 기반이라 유저마다 열리는 시점이 자연히 다르다. */
export const PROGRESS_GATE = {
  orchard: { kind: 'advHarvest', n: 1 },   // 🌾고급 작물(밀·옥수수·포도) 1회 수확
};

/** 진행도 게이트로 잠겼나. 표에 없는 맵은 잠그지 않는다. */
export function isProgressLocked(map, progress) {
  const g = PROGRESS_GATE[map];
  if (!g) return false;
  return ((progress || {})[g.kind] || 0) < g.n;
}

/** 잠겼나 — 베타 날짜 게이트 **또는** 진행도 게이트. 둘 중 하나라도 잠그면 잠긴다. */
export function isMapLocked({ variant, mapOrder, createdAtIso, progress, nowMs = Date.now() }, map) {
  if (isProgressLocked(map, progress)) return true;
  if (!/^beta_/.test(variant || '')) return false;
  const openDay = mapOpenDay(mapOrder, map);
  if (openDay == null) return false;
  return betaDay(createdAtIso, nowMs) < openDay;
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `npm test`
Expected: PASS — `tests/map-gate.test.mjs` 의 기존 테스트도 전부 그대로 통과해야 한다(베타 동작 불변)

- [ ] **Step 5: 커밋**

```bash
git add js/tuning.js tests/map-gate.test.mjs
git commit -m "$(cat <<'EOF'
refactor: 🔒 잠금 규칙을 날짜 게이트와 진행도 게이트로 일반화한다

베타 전용이던 isMapLocked 에 진행도 게이트를 더해, 과수원처럼
"무엇을 해냈나"로 여는 공간을 같은 UI 로 잠글 수 있게 한다.
베타 날짜 게이트 동작은 그대로다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

> **Phase 1 종료 지점.** Task 1–4 까지가 순수 규칙 + 테스트다. 여기까지만으로도 `npm test` 가 전부 통과하고 단독 머지가 가능하다. 아래 Phase 2 부터 `js/game.js` 를 만진다.

---

## Task 5: 상점 묘목과 판매가

**Files:**
- Modify: `js/game.js:550` (`SELL_PRICE`), `js/game.js:561` 아래 (`SHOP_BUY`), `js/game.js:1037` (인벤 초기값), `js/game.js:12943` (인벤 라벨), `index.html:4776`·`index.html:4782` (도감)
- Test: `tests/orchard.test.mjs`

**Interfaces:**
- Consumes: Task 1 의 `FRUITS`, `sapKeyOf`
- Produces: 인벤 키 `sap_apple`…`sap_chestnut`(묘목), `apple`…`chestnut`(과일). 이후 태스크가 이 키를 그대로 쓴다.

- [ ] **Step 0: `js/game.js` 에 `js/orchard.js` import 를 추가한다**

**이 단계를 빠뜨리면 Task 5 부터 `FRUITS is not defined` 로 죽는다.** `js/game.js:47`(farm-crops import) 바로 아래에 더한다:

```js
import { FRUITS, TREE_SLOTS, YIELD_PER_DAY, fruitOf, fruitKeyOf, sapKeyOf, nearStream, harvestable, settleTrees,
         ORCHARD_STREAM_LOCAL, ORCHARD_SLOTS_LOCAL } from './orchard.js';   // 🍎 과수원 규칙(과일 표·물·수확·정산)
```

`ORCHARD_STREAM_LOCAL`·`ORCHARD_SLOTS_LOCAL` 은 Task 7 에서 `js/orchard.js` 에 추가된다. **Task 7 전까지는 그 두 이름을 import 목록에서 빼 두고, Task 7 에서 다시 넣는다** — 없는 이름을 import 하면 모듈 로드가 통째로 실패한다.

- [ ] **Step 1: 판매가를 등록한다**

`js/game.js:550` 의 `SELL_PRICE` 를 아래로 바꾼다. 값은 Task 1 의 `FRUITS[].price` 와 **반드시 같아야 한다.**

```js
const SELL_PRICE = { crop: 5, fish: 8, wood: 2, stone: 3, coal: 6, gem: 40, egg: 6, bug: 14, forage: 7, wheat: 15, corn: 20, grape: 30, honey: 12,
  apple: 5, pear: 6, peach: 8, persimmon: 10, chestnut: 12 };   // 🍎 과수원 과일 — js/orchard.js FRUITS[].price 와 같은 값
```

- [ ] **Step 2: 상점 묘목을 등록한다**

`js/game.js:561` 의 포도 씨앗 줄 바로 아래에 이어 붙인다:

```js
  // 🍎 과수원 묘목 — 코인 전용(최대 코인 싱크). 한 번 심으면 영구 자산이라 씨앗보다 훨씬 비싸다
  { id: 'sap_apple',     name: '사과나무 묘목',   ico: '🍎', coin: 90,  give: { sap_apple: 1 },     desc: '3일이면 자라요 · 매일 🍎2개' },
  { id: 'sap_pear',      name: '배나무 묘목',     ico: '🍐', coin: 130, give: { sap_pear: 1 },      desc: '3일이면 자라요 · 매일 🍐2개' },
  { id: 'sap_peach',     name: '복숭아나무 묘목', ico: '🍑', coin: 180, give: { sap_peach: 1 },     desc: '4일이면 자라요 · 매일 🍑2개' },
  { id: 'sap_persimmon', name: '감나무 묘목',     ico: '🍊', coin: 240, give: { sap_persimmon: 1 }, desc: '4일이면 자라요 · 매일 🍊2개' },
  { id: 'sap_chestnut',  name: '밤나무 묘목',     ico: '🌰', coin: 300, give: { sap_chestnut: 1 },  desc: '5일이면 자라요 · 매일 🌰2개' },
```

- [ ] **Step 3: 인벤 초기값과 라벨을 더한다**

`js/game.js:1037` 의 인벤 초기값 객체에:

```js
    apple: 0, pear: 0, peach: 0, persimmon: 0, chestnut: 0,
    sap_apple: 0, sap_pear: 0, sap_peach: 0, sap_persimmon: 0, sap_chestnut: 0,   // 🍎 과수원(js/orchard.js)
```

`js/game.js:12943` 의 라벨 표에:

```js
  apple: '🍎사과', pear: '🍐배', peach: '🍑복숭아', persimmon: '🍊감', chestnut: '🌰밤',
  sap_apple: '🍎사과나무 묘목', sap_pear: '🍐배나무 묘목', sap_peach: '🍑복숭아나무 묘목',
  sap_persimmon: '🍊감나무 묘목', sap_chestnut: '🌰밤나무 묘목',
```

- [ ] **Step 4: 도감에 과수원 칸을 만든다**

`index.html:4776` 근처 아이템 사전에:

```html
      apple: { ico: '🍎', name: '사과' }, pear: { ico: '🍐', name: '배' }, peach: { ico: '🍑', name: '복숭아' },
      persimmon: { ico: '🍊', name: '감' }, chestnut: { ico: '🌰', name: '밤' },
```

`index.html:4782` 의 `🌾 고급 작물` 줄 바로 아래에:

```html
      { name: '🍎 과수원', keys: ['apple', 'pear', 'peach', 'persimmon', 'chestnut'] },
```

- [ ] **Step 5: 두 곳에 흩어진 판매가가 어긋나면 터지게 한다**

`FRUITS[].price` 와 `game.js` 의 `SELL_PRICE` 는 다른 파일에 흩어진 같은 값이라 조용히 갈라진다. `game.js` 는 Three.js 를 끌어와 `node:test` 에서 import 할 수 없으므로, **소스를 텍스트로 읽어 대조한다.**

`tests/orchard.test.mjs` 에 더한다:

```js
import { readFileSync } from 'node:fs';

test('SELL_PRICE 의 과일 값이 FRUITS[].price 와 일치한다 — 한쪽만 고치면 여기서 터진다', () => {
  const src = readFileSync(new URL('../js/game.js', import.meta.url), 'utf8');
  const line = src.split('\n').find(l => l.includes('const SELL_PRICE'));
  assert.ok(line, 'SELL_PRICE 선언을 못 찾았다 — 변수명이 바뀌었으면 이 테스트를 같이 고친다');
  for (const f of FRUITS) {
    const m = new RegExp(`\\b${f.id}\\s*:\\s*(\\d+)`).exec(line);
    assert.ok(m, `SELL_PRICE 에 ${f.id} 가 없다`);
    assert.equal(Number(m[1]), f.price, `${f.id}: SELL_PRICE 와 FRUITS[].price 가 어긋난다`);
  }
});
```

> 이 테스트는 Task 5 Step 1 을 마친 뒤에야 통과한다. 먼저 돌려 **FAIL 을 확인하고** 나서 Step 1 을 적용해도 좋다.

Run: `npm test`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add js/game.js index.html tests/orchard.test.mjs
git commit -m "$(cat <<'EOF'
feat: 🍎 상점에 과일나무 묘목 5종과 과일 판매가를 넣는다

묘목은 90~300🪙 로 게임 내 최대 단일 코인 싱크가 된다.
과일 판매가는 기본 작물보다 조금 높은 선으로 두고, 큰 값은 요리에서 나오게 한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 과수원 공간과 잠긴 입구

**Files:**
- Modify: `js/game.js:499`(좌표), `js/game.js:302`·`js/game.js:2449`(플래그), `js/game.js:1688`(장소 이름), `js/game.js:1942`(`mapLocked`), `js/game.js:1065`(세이브 초기값), `js/game.js:9808` 부근(입구 프롬프트), `js/game.js:10040`(지도 지명)

> `setSpaceVisible()`(`js/game.js:524`)의 `orchardGroup` 줄은 **Task 7** 에서 넣는다 — 이 태스크에는 아직 `orchardGroup` 이 없다.

**Interfaces:**
- Consumes: Task 4 의 `isMapLocked`(이제 `progress` 를 받는다)
- Produces: `ORCHARD_GATE`, `ORCHARD`, `ORCHARD_HALF`, `atOrchard`, `enterOrchard()`, `exitOrchard()`

- [ ] **Step 1: 좌표 상수를 더한다**

`js/game.js:499`(`const SEA = ...`) 바로 아래:

```js
const ORCHARD_GATE = new THREE.Vector3(22, 0, 2);   // 🍎 마을 정동쪽 — 여덟 방향 중 유일하게 빈 자리(스펙 §1)
const ORCHARD = new THREE.Vector3(0, 0, 160);       // 과수원 인스턴스 — 텃밭(84)과 광산(250) 사이
const ORCHARD_HALF = 20;                            // 언덕 반경
```

- [ ] **Step 2: 공간 플래그를 등록한다**

`js/game.js:302` 근처에 `let atOrchard = false;` 를 더하고, 아래 두 곳에 함께 등록한다.

```js
// js/game.js:2451 — _spaceFlags 에 atOrchard: false 를 더하고 여기서 대입
_spaceFlags.atOrchard = atOrchard;

// js/game.js:1688 — 현재 장소 이름
if (atOrchard) return 'orchard';
```

> ⚠️ `outdoorZone()`(`js/game.js:9097`) 은 **건드리지 않는다.** 과수원은 실외라 날씨·야외 장식이 그대로 돌아야 한다. 텃밭(`atFarm`)도 같은 이유로 거기 안 들어가 있다.

- [ ] **Step 3: `mapLocked` 가 진행도를 넘기게 고친다**

`js/game.js:1942`:

```js
function mapLocked(map) {
  return isMapLocked({ variant: authState.variant, mapOrder: authState.mapOrder,
                       createdAtIso: authState.createdAt, progress: gameState.progress,
                       nowMs: betaNowMs() }, map);
}
```

`js/game.js:1065` 근처 세이브 초기값에 **두 줄 다** 넣는다. `orchard` 를 여기서 만들어야 Task 7·8 이 `gameState.orchard` 를 안전하게 읽는다:

```js
  orchard: { trees: [], sapSel: 'apple', settleDate: null },   // 🍎 과수원(js/orchard.js)
  progress: { advHarvest: 0 },   // 🔒 진행도 해금 카운터 — 고급 작물 수확 횟수(js/tuning.js PROGRESS_GATE)
```

(Task 9 는 이 초기값을 다시 쓰지 않고 **복원·직렬화만** 다룬다.)

- [ ] **Step 4: 입·퇴장 함수를 쓴다**

`enterMist`/`exitMist`(`js/game.js:5837`)와 같은 꼴로 만든다:

```js
function enterOrchard() {
  if (blockIfLocked('orchard')) return;           // 🔒 고급 작물 1회 수확 전이면 여기서 막힌다
  atOrchard = true;
  player.position.set(ORCHARD.x, 0, ORCHARD.z + ORCHARD_HALF - 1.6); player.rotation.y = Math.PI;
  nearDoor = null; ui.setDoorPrompt?.(null); ui.setZoneHint?.(null); lastZoneHint = null;
  snapCamera(); setSpaceVisible();
  // ⚠️ 일일 정산 호출은 여기 넣지 않는다 — settleOrchard 는 Task 8 에서 만들어진다.
  //    Task 8 Step 4 가 이 자리에 `settleOrchard();` 한 줄을 넣는다.
  firstHint('orchardIntro', '🍎', '과수원 언덕',
    '🌰씨앗 도구로 묘목을 심어요\n시냇가 나무는 물을 안 줘도 돼요\n다 자라면 매일 와서 따요');
  Sound.blip(); setBGMTheme?.('main');
  trackEvent('orchard_enter', { trees: (gameState.orchard?.trees || []).length });   // [GA4] 유입
}
function exitOrchard() {
  atOrchard = false;
  player.position.set(ORCHARD_GATE.x - 1.6, 0, ORCHARD_GATE.z + 1.6);
  nearDoor = null; ui.setDoorPrompt?.(null); ui.setZoneHint?.(null); lastZoneHint = null;
  snapCamera(); setSpaceVisible();
  Sound.blip();
  trackEvent('orchard_exit');   // [GA4]
}
```

- [ ] **Step 5: 마을 입구 프롬프트와 지도 지명을 더한다**

`js/game.js:9808`(바다 입구) 옆에 같은 꼴로:

```js
  } else if (!indoor && dist2D(player.position, ORCHARD_GATE) < 2.2) {
    const locked = mapLocked('orchard');
    prompt = locked ? '🔒 🌾고급 작물을 한 번 거두면 열려요' : '🍎 과수원 언덕에 올라가기';
    if (!locked) firstHintBanner('orchardGate', '🍎', '과수원 언덕', '묘목을 심어 매일 열매를 따는 언덕');
```

`js/game.js:10040` 의 `VILLAGE_PLACES` 에:

```js
  { ico: '🍎', name: '과수원 언덕', x: ORCHARD_GATE.x, z: ORCHARD_GATE.z, pri: 1, map: 'orchard' },
```

- [ ] **Step 6: 입구 자리가 비어 있는지 실측한다**

Run: 게임을 켜고 개발 콘솔에서 `isBlocked(22, 2)`
Expected: `false`

`true` 면 나무·바위가 서 있다는 뜻이다. `(23,2)` → `(22,4)` → `(24,1)` 순으로 `false` 인 자리를 찾아 `ORCHARD_GATE` 를 고치고, **스펙 §1 의 "구현 시 실측 필요" 항목에 실제 좌표를 적어 넣는다.**

- [ ] **Step 7: 커밋**

```bash
git add js/game.js docs/superpowers/specs/2026-09-17-orchard-design.md
git commit -m "$(cat <<'EOF'
feat: 🍎 과수원 공간과 잠긴 입구를 마을 정동쪽에 낸다

기존 베타 맵 게이트 UI(프롬프트·토스트·지도 흐림)를 그대로 쓰고
잠금 조건만 진행도 게이트로 받는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 언덕 지형과 과일나무 조형

**Files:**
- Modify: `js/orchard.js`(자리 좌표 표), `js/game.js:2608` 옆(`spawnFruitTree`), `js/game.js:524`(가시성)
- Test: `tests/orchard.test.mjs`

**Interfaces:**
- Consumes: Task 1 의 `FRUITS`·`TREE_SLOTS`·`STREAM_SLOTS`, Task 2 의 `nearStream`
- Produces: `ORCHARD_STREAM_LOCAL`, `ORCHARD_SLOTS_LOCAL`(둘 다 `js/orchard.js` export), `orchardGroup`, `spawnFruitTree(x, z, kind, stage, fruit)`, `rebuildOrchard()`, `orchardStreamWorld()`

- [ ] **Step 1: 자리 표를 `js/orchard.js` 에 둔다**

렌더가 아니라 **규칙**이다(어느 자리가 면제인지가 게임플레이를 정한다). 그래서 테스트 가능한 쪽에 둔다.

```js
// 🍎 시냇물 중심선 — 과수원을 세로로 가로지른다(ORCHARD 기준 국소 좌표)
export const ORCHARD_STREAM_LOCAL = [[-6, -14], [-5, -7], [-4, 0], [-5, 7], [-6, 14]];
// 나무 자리 10개 — 앞 4개는 시냇가(면제), 뒤 6개는 멀다
export const ORCHARD_SLOTS_LOCAL = [
  [-2, -11], [-1, -4], [-1, 4], [-2, 11],                       // 시냇가 4
  [7, -12], [11, -5], [12, 3], [8, 11], [3, 15], [14, -13],      // 먼 자리 6
];
```

- [ ] **Step 2: 자리가 정말 갈리는지 테스트한다**

`tests/orchard.test.mjs` 에 더한다:

```js
test('자리 배치: 앞 4자리는 시냇가 면제, 뒤 6자리는 매일 물이 필요하다', () => {
  const stream = ORCHARD_STREAM_LOCAL.map(([x, z]) => ({ x, z }));
  const slots = ORCHARD_SLOTS_LOCAL.map(([x, z]) => ({ x, z }));
  assert.equal(slots.length, TREE_SLOTS);
  const near = slots.map(s => nearStream(s, stream));
  assert.deepEqual(near, [true, true, true, true, false, false, false, false, false, false]);
  assert.equal(near.filter(Boolean).length, STREAM_SLOTS);
});
```

Run: `npm test`
Expected: PASS. **FAIL 이면 좌표를 고친다** — 눈으로 배치한 값이 규칙과 어긋나면 "시냇가에 심었는데 물을 달라고 한다"는 버그가 된다. 이 테스트가 이 태스크의 핵심이다.

- [ ] **Step 3: 과일나무 조형을 만든다 — 그루마다 메시를 만들지 않는다**

> 🚨 **드로우콜 예산: 나무 10그루가 다 찬 과수원에서 본 패스 14 이하.**
>
> 그루마다 `THREE.Mesh` 를 만들면 줄기 10 + 잎 10 + 열매 최대 60 + 지면 1 + 시냇물 5 + 빈 자리 10 = **76 드로우콜**이 되고, 그림자 패스까지 치면 152다. 이 저장소는 이미 같은 문제를 풀어 뒀다 — `js/game.js:2576` 의 풀이 "포기마다 메시였던 것을 색깔별 InstancedMesh 3개로(드로우콜 76→3)" 바뀌었다. 과수원도 **같은 방식**을 쓴다.
>
> | 묶음 | 드로우콜 |
> |---|---|
> | 줄기 — 전부 같은 재질이라 InstancedMesh 1개 | 1 |
> | 잎 — 과일 종류별 색이라 종류당 1개 (최대 5종) | ≤5 |
> | 열매 — 종류별 색, 나무별이 아니라 **과수원 전체를 한 덩이로** | ≤5 |
> | 지면 | 1 |
> | 시냇물 — 점 5개를 `mergeGeos` 로 하나로 합침 | 1 |
> | 빈 자리 표시 — InstancedMesh 1개 | 1 |
> | **합계** | **≤14** |

`spawnTree()`(`js/game.js:2608`)에서 가져올 것은 **지오메트리 모양**이지 그루당 메시 구조가 아니다. 줄기·잎덩이 지오메트리는 `shared()` 로 그대로 재사용하되, 메시는 인스턴스로 묶는다.

```js
// 국소 좌표 → 월드 좌표
function orchardStreamWorld() { return ORCHARD_STREAM_LOCAL.map(([x, z]) => ({ x: ORCHARD.x + x, z: ORCHARD.z + z })); }
function orchardSlotsWorld()  { return ORCHARD_SLOTS_LOCAL.map(([x, z]) => ({ x: ORCHARD.x + x, z: ORCHARD.z + z })); }

// 단계별 크기 — 묘목은 작고 다 자라면 1
const ORCHARD_SCALE = { sapling: 0.35, growing: 0.7, mature: 1 };
// 열매가 달리는 자리(나무 국소 좌표) — 최대 6개
const FRUIT_SPOTS = [[-0.7, 2.2, 0.3], [0.8, 2.0, -0.2], [0.2, 2.6, 0.5], [-0.3, 1.8, -0.7], [0.6, 2.5, 0.1], [-0.9, 2.4, -0.3]];

// 🍎 나무를 전부 InstancedMesh 로 묶어 그린다 — 풀(js/game.js:2576)과 같은 방식.
//   그루마다 Mesh 를 만들면 열매까지 76 드로우콜이 된다. 여기선 ≤11 이다.
function syncOrchardTrees() {
  const trees = gameState.orchard?.trees || [];
  const m = new THREE.Matrix4(), q = new THREE.Quaternion(), v = new THREE.Vector3();
  const put = (im, i, x, y, z, s) => { m.compose(v.set(x, y, z), q, new THREE.Vector3(s, s, s)); im.setMatrixAt(i, m); };

  // 1) 줄기 — 전부 같은 재질이라 한 덩이
  if (trees.length) {
    const im = new THREE.InstancedMesh(
      shared('tree.trunk.geo', () => new THREE.CylinderGeometry(0.35, 0.5, 1.6, 7)),
      shared('tree.trunk.mat', () => clayMat(PAL.trunk)), trees.length);
    trees.forEach((t, i) => { const s = ORCHARD_SCALE[t.stage] ?? 1; put(im, i, t.x, 0.8 * s, t.z, s); });
    im.instanceMatrix.needsUpdate = true; im.castShadow = true; orchardGroup.add(im);
  }

  // 2) 잎 — 과일 종류별로 색이 다르니 종류마다 한 덩이(최대 5)
  for (const def of FRUITS) {
    const mine = trees.filter(t => t.kind === def.id);
    if (!mine.length) continue;
    const im = new THREE.InstancedMesh(
      shared('tree.canopy.geo', () => mergeGeos(
        [[0, 0.4, 0, 1.2], [0.7, 0, 0.2, 0.85], [-0.6, 0.05, -0.3, 0.9], [0.1, 0.9, -0.2, 0.7]]
          .map(([bx, by, bz, cs]) => new THREE.IcosahedronGeometry(cs, 0).translate(bx, by, bz)))),
      shared(`orchard.leaf.mat.${def.leafColor}`, () => clayMat(def.leafColor)), mine.length);
    mine.forEach((t, i) => { const s = ORCHARD_SCALE[t.stage] ?? 1; put(im, i, t.x, 2.0 * s, t.z, s); });
    im.instanceMatrix.needsUpdate = true; im.castShadow = true; orchardGroup.add(im);
  }

  // 3) 열매 — 나무별이 아니라 과수원 전체를 종류별 한 덩이로(최대 5)
  for (const def of FRUITS) {
    const spots = [];
    for (const t of trees) {
      if (t.kind !== def.id || t.stage !== 'mature') continue;
      for (let i = 0; i < Math.min(t.fruit || 0, FRUIT_SPOTS.length); i++) {
        spots.push([t.x + FRUIT_SPOTS[i][0], FRUIT_SPOTS[i][1], t.z + FRUIT_SPOTS[i][2]]);
      }
    }
    if (!spots.length) continue;
    const im = new THREE.InstancedMesh(
      shared('orchard.fruit.geo', () => new THREE.IcosahedronGeometry(0.17, 0)),
      shared(`orchard.fruit.mat.${def.fruitColor}`, () => clayMat(def.fruitColor)), spots.length);
    spots.forEach(([x, y, z], i) => put(im, i, x, y, z, 1));
    im.instanceMatrix.needsUpdate = true; orchardGroup.add(im);
  }

  // 🚧 충돌체는 그리기와 무관하다 — 나무마다 하나씩 그대로 둔다
  for (const t of trees) obstacles.push({ x: t.x, z: t.z, r: 0.8 });
}
```

> **흔들림(sway)은 넣지 않는다.** 숲 나무는 캐노피 메시마다 `swayables` 에 등록해 매 프레임 흔드는데, 인스턴스로 묶으면 그 경로를 못 쓴다. 매 프레임 10그루의 인스턴스 행렬을 다시 쓰는 비용보다 흔들림을 빼는 쪽이 낫고, 과수원과 숲은 한 화면에 같이 보이지 않아 어색하지 않다. 나중에 필요하면 풀과 같은 방식(`updateSway` 가 행렬 갱신)으로 붙인다.

- [ ] **Step 4: `rebuildOrchard()` 로 전체를 다시 그린다**

밭의 `rebuildFarm()` 과 같은 꼴이다 — 상태가 바뀌면 통째로 다시 그린다.

```js
function rebuildOrchard() {
  if (!orchardGroup) { orchardGroup = new THREE.Group(); scene.add(orchardGroup); }
  while (orchardGroup.children.length) orchardGroup.remove(orchardGroup.children[0]);
  buildOrchardGround();     // 지면 1 + 시냇물 1(합침)
  syncOrchardTrees();       // 줄기 1 + 잎 ≤5 + 열매 ≤5
  syncOrchardSlotHints();   // 빈 자리 1(인스턴스)
}
```

**이 두 헬퍼도 이 태스크에서 같이 만든다.** 이름만 부르고 끝내면 `ReferenceError` 가 난다. 둘 다 **메시를 하나씩만** 만든다 — 개수만큼 메시를 만들면 예산이 무너진다.

```js
// 언덕 지면 + 시냇물 — 둘 다 정적. 시냇물은 점 5개를 한 지오메트리로 합쳐 드로우콜 1
function buildOrchardGround() {
  const ground = new THREE.Mesh(
    shared('orchard.ground.geo', () => new THREE.CircleGeometry(ORCHARD_HALF, 32).rotateX(-Math.PI / 2)),
    shared('orchard.ground.mat', () => clayMat(0xc0cf9e)));
  ground.position.set(ORCHARD.x, 0.01, ORCHARD.z); ground.receiveShadow = true; orchardGroup.add(ground);

  // 시냇물 — nearStream 이 보는 것과 같은 좌표에 원반을 놓고 하나로 합친다
  const water = new THREE.Mesh(
    shared('orchard.water.geo', () => mergeGeos(ORCHARD_STREAM_LOCAL.map(([x, z]) =>
      new THREE.CircleGeometry(2.2, 12).rotateX(-Math.PI / 2).translate(x, 0, z)))),
    shared('orchard.water.mat', () => clayMat(0x8fb9d6)));
  water.position.set(ORCHARD.x, 0.03, ORCHARD.z); orchardGroup.add(water);
}

// 빈 자리 표시 — 나무 없는 흙 자리에만. 개수가 변하니 InstancedMesh 하나로 묶는다
function syncOrchardSlotHints() {
  const taken = new Set((gameState.orchard?.trees || []).map(t => `${t.x},${t.z}`));
  const free = orchardSlotsWorld().filter(s => !taken.has(`${s.x},${s.z}`));
  if (!free.length) return;
  const im = new THREE.InstancedMesh(
    shared('orchard.slot.geo', () => new THREE.CircleGeometry(0.9, 12).rotateX(-Math.PI / 2)),
    shared('orchard.slot.mat', () => clayMat(0x8a6440)), free.length);
  const m = new THREE.Matrix4();
  free.forEach((s, i) => { m.makeTranslation(s.x, 0.04, s.z); im.setMatrixAt(i, m); });
  im.instanceMatrix.needsUpdate = true; orchardGroup.add(im);
}
```

`setSpaceVisible()`(`js/game.js:524`)에 한 줄:

```js
  if (orchardGroup) orchardGroup.visible = atOrchard;
```

- [ ] **Step 5: 🚨 드로우콜을 측정한다 — 통과 못 하면 이 태스크는 끝난 게 아니다**

빈 과수원과 꽉 찬 과수원 두 번 잰다. 개발 콘솔에서:

```js
// 1) 빈 과수원에서
renderer.info.render.calls
// 2) 10그루를 5종 섞어 채우고 전부 열매까지 달아 본다
gameState.orchard.trees = ORCHARD_SLOTS_LOCAL.map(([x, z], i) => ({
  x: ORCHARD.x + x, z: ORCHARD.z + z, kind: FRUITS[i % 5].id,
  stage: 'mature', age: 9, watered: false, fruit: 6,
}));
rebuildOrchard();
renderer.info.render.calls
```

**Expected: 두 값의 차이가 11 이하** (줄기 1 + 잎 5 + 열매 5). 과수원 전체 예산은 지면·시냇물·빈 자리까지 합쳐 **14 이하**다.

넘으면 원인은 셋 중 하나다:
1. `shared()` 키에 색이 안 들어가 종류마다 재질이 새로 생김 → `orchard.leaf.mat.${def.leafColor}` 처럼 색을 키에 넣는다
2. `InstancedMesh` 대신 `Mesh` 를 루프로 만들고 있음 → Step 3 코드를 다시 본다
3. 시냇물·빈 자리를 개수만큼 메시로 만들고 있음 → `mergeGeos` / `InstancedMesh` 로 묶는다

**측정한 두 숫자와 차이를 보고서에 적는다.** "최적화했다"는 서술이 아니라 숫자가 증거다.

- [ ] **Step 6: 커밋**

```bash
git add js/game.js js/orchard.js tests/orchard.test.mjs
git commit -m "$(cat <<'EOF'
feat: 🍎 과수원 언덕 지형과 과일나무 조형을 그린다

기존 숲 나무의 줄기·잎덩이 지오메트리를 그대로 재사용하고 색만 바꿔
그루당 드로우콜 2를 유지한다. 자리 10개 중 4개가 시냇가인지 테스트로 못 박는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: 심기 · 물주기 · 수확 액션과 일일 정산

**Files:**
- Modify: `js/game.js:11691`(`cycleSeedSel`), `js/game.js:11262` 부근(액션 라우팅), `js/game.js:11917` 부근(날짜 게이트), `js/game.js:12090`(`harvestPlot` — 해금 카운터)

**Interfaces:**
- Consumes: Task 1–3 의 `FRUITS`·`fruitOf`·`sapKeyOf`·`fruitKeyOf`·`settleTrees`·`harvestable`·`nearStream`·`TREE_SLOTS`·`YIELD_PER_DAY`; Task 7 의 `rebuildOrchard`·`orchardStreamWorld`
- Produces: `cycleSapSel()`, `plantSapling(slot)`, `waterTree(tree)`, `harvestTree(tree)`, `settleOrchard()`

- [ ] **Step 1: 씨앗 도구 순환에 묘목을 합류시킨다**

과수원 안에서만 묘목이 후보에 뜬다. `cycleSeedSel()`(`js/game.js:11691`) 맨 앞에 한 줄을 더하고 새 함수를 옆에 둔다:

```js
function cycleSeedSel() {
  if (atOrchard) return cycleSapSel();   // 🍎 과수원에선 묘목만 돈다
  // ...기존 코드 그대로...
}
function cycleSapSel() {
  const owned = FRUITS.filter(f => (gameState.inventory[sapKeyOf(f.id)] || 0) > 0);
  if (!owned.length) { ui.toast?.('🌰 묘목이 없어요. 상점에서 🍎사과·🍐배·🍑복숭아·🍊감·🌰밤 묘목을 팔아요', 2800); return; }
  const i = owned.findIndex(f => f.id === gameState.orchard.sapSel);
  const next = owned[(i + 1) % owned.length];
  gameState.orchard.sapSel = next.id; Sound.blip();
  ui.toast?.(`${next.ico} ${next.name}나무 묘목 (${gameState.inventory[sapKeyOf(next.id)]}개) — 다시 누르면 바꿔요`, 2200);
  trackEvent('sap_select', { kind: next.id });   // [GA4] 묘목 종류 채택
}
```

- [ ] **Step 2: 심기를 구현한다**

```js
function plantSapling(slot) {
  const kind = gameState.orchard.sapSel || 'apple';
  const key = sapKeyOf(kind);
  if ((gameState.inventory[key] || 0) <= 0) { ui.toast?.(`${fruitOf(kind).ico} 묘목이 없어요`, 2400); return; }
  if ((gameState.orchard.trees || []).length >= TREE_SLOTS) { ui.toast?.('🍎 자리가 다 찼어요 — 10그루까지 심을 수 있어요', 2600); return; }
  gameState.inventory[key] -= 1;
  const tree = { x: slot.x, z: slot.z, kind, stage: 'sapling', age: 0, watered: false, fruit: 0 };
  gameState.orchard.trees = [...(gameState.orchard.trees || []), tree];   // 불변 갱신
  doPlayerAction(slot.x, slot.z); Sound.plant();
  rebuildOrchard(); refreshInventoryUI(); requestSave();
  trackEvent('sapling_plant', {                                  // [GA4] 생애주기 2단계
    kind, near_stream: nearStream(tree, orchardStreamWorld()) ? 1 : 0, trees: gameState.orchard.trees.length });
}
```

- [ ] **Step 3: 물주기와 수확을 구현한다**

```js
function waterTree(tree) {
  if (nearStream(tree, orchardStreamWorld())) { ui.toast?.('💧 시냇가 나무라 물을 안 줘도 돼요', 2400); return; }
  if (tree.watered) { ui.toast?.('💧 오늘은 이미 물을 줬어요', 2000); return; }
  tree.watered = true;
  doPlayerAction(tree.x, tree.z); Sound.water(); requestSave();
  spawnFloatText(tree.x, 1.4, tree.z, '💧', '#8fb9d6');
  // ⚠️ source/content 같은 GA4 예약어를 쓰지 않는다 — method 로 보낸다
  trackEvent('tree_water', { kind: tree.kind, method: 'manual' });
}

function harvestTree(tree) {
  const n = harvestable(tree);
  if (!n) { ui.toast?.(tree.stage === 'mature' ? '🌳 아직 열매가 없어요 — 내일 다시 와요' : '🌿 아직 자라는 중이에요', 2400); return; }
  const stacked = Math.ceil(n / YIELD_PER_DAY);
  giveReward({ [fruitKeyOf(tree.kind)]: n }, 'orchard', tree.kind);   // econ_logs 의 item 도 같은 id
  tree.fruit = 0;
  doPlayerAction(tree.x, tree.z); Sound.harvest();
  rebuildOrchard(); refreshInventoryUI(); requestSave();
  trackEvent('fruit_harvest', { kind: tree.kind, n, stacked_days: stacked });   // [GA4]
}
```

- [ ] **Step 4: 일일 정산을 붙인다**

🐝벌통과 같은 날짜 게이트를 쓴다(`js/game.js:11917` 부근 패턴).

**날짜는 `todayStr()`(`js/game.js:139`)로 구한다.** `kstDate` 는 `js/tuning.js` 에 있지만 `game.js` 가 import 하지 않고 쓰지도 않는다 — 새 import 를 늘리지 말고 저장소가 이미 쓰는 헬퍼를 쓴다.

또한 이 단계에서 `enterOrchard()`(Task 6) 의 주석 자리에 `settleOrchard();` 한 줄을 넣는다.

```js
function settleOrchard() {
  const st = gameState.orchard; if (!st) return;
  const today = todayStr();                         // js/game.js:139 — 저장소의 KST 날짜 헬퍼
  if (st.settleDate === today) return;
  const days = st.settleDate ? Math.max(1, Math.round((Date.parse(today) - Date.parse(st.settleDate)) / 86400000)) : 1;
  st.settleDate = today;
  const out = settleTrees(st.trees || [], orchardStreamWorld(), days);
  st.trees = out.trees;
  for (const m of out.matured) trackEvent('tree_mature', m);     // [GA4] kind·grew_days
  for (const f of out.fruited) trackEvent('fruit_ready', f);     // [GA4] kind·n·watered
  for (const c of out.capped)  trackEvent('fruit_capped', c);    // [GA4] kind
  const total = out.fruited.reduce((s, f) => s + f.n, 0);
  if (total) setTimeout(() => ui.toast?.(`🍎 과수원에 열매 ${total}개가 열렸어요`, 2600), 1400);
  rebuildOrchard(); requestSave();
}
```

- [ ] **Step 5: 해금 카운터를 올린다**

`harvestPlot()`(`js/game.js:12090`)에서 고급 작물을 거둘 때:

```js
  if (adv) {
    gameState.progress.advHarvest = (gameState.progress.advHarvest || 0) + 1;
    if (gameState.progress.advHarvest === 1) {
      ui.toast?.('🍎 마을 동쪽 과수원 언덕이 열렸어요!', 3200);
      trackEvent('orchard_unlock', { via: plot.cropType.id });   // [GA4] 어떤 고급 작물이 열었나
      giveReward({ sap_apple: 2 }, 'orchard_unlock', 'apple');   // 빈 언덕 방지 — 사과 묘목 2그루
    }
  }
```

- [ ] **Step 6: 손으로 한 바퀴 돌려본다**

Run: 게임을 켜고 개발 콘솔에서
```js
gameState.progress.advHarvest = 1;   // 해금
gameState.inventory.sap_apple = 3;   // 묘목
```
동쪽 입구로 걸어가 올라가고 → 묘목을 심고 → `gameState.orchard.settleDate = '2026-01-01'` 로 되돌린 뒤 나갔다 들어온다.

Expected: 정산이 여러 날치를 한 번에 돌아 나무가 `mature` 가 되고, **시냇가 나무엔 열매가 달리고 먼 나무엔 안 달린다.**

- [ ] **Step 7: 커밋**

```bash
git add js/game.js
git commit -m "$(cat <<'EOF'
feat: 🍎 묘목 심기·물주기·수확과 일일 정산을 붙인다

씨앗 도구 순환을 과수원 안에서 묘목으로 갈아끼워 새 도구를 만들지 않는다.
고급 작물을 처음 거두면 과수원이 열리고 사과 묘목 2그루가 딸려 온다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: 세이브와 복원

**Files:**
- Modify: `js/game.js:1065`(초기값), `js/game.js:2283` 부근(복원), `js/game.js:2381`(직렬화)

**Interfaces:**
- Consumes: Task 1 의 `FRUITS`·`TREE_SLOTS`
- Produces: 세이브 필드 `orchard: { trees, sapSel, settleDate }`, `progress: { advHarvest }`

- [ ] **Step 1: 초기값이 이미 있는지 확인한다**

`orchard` · `progress` 초기값은 **Task 6 Step 3 에서 이미 넣었다.** `js/game.js:1065` 부근에 아래 두 줄이 있는지만 확인하고, 있으면 이 단계는 건너뛴다.

```js
  orchard: { trees: [], sapSel: 'apple', settleDate: null },
  progress: { advHarvest: 0 },
```

없으면 지금 넣는다.

- [ ] **Step 2: 복원 — 옛 세이브 방어**

`js/game.js:2283` 근처, `saved.farm` 복원 옆에:

```js
  if (saved.orchard) {
    if (Array.isArray(saved.orchard.trees)) {
      gameState.orchard.trees = saved.orchard.trees
        .filter(t => FRUITS.some(f => f.id === t.kind))            // 모르는 종류는 버린다
        .slice(0, TREE_SLOTS)                                       // 상한 방어
        .map(t => ({ x: t.x, z: t.z, kind: t.kind, stage: t.stage || 'sapling',
                     age: t.age || 0, watered: !!t.watered, fruit: Math.max(0, t.fruit || 0) }));
    }
    if (FRUITS.some(f => f.id === saved.orchard.sapSel)) gameState.orchard.sapSel = saved.orchard.sapSel;
    if (typeof saved.orchard.settleDate === 'string') gameState.orchard.settleDate = saved.orchard.settleDate;
  }
  if (saved.progress && typeof saved.progress.advHarvest === 'number') {
    gameState.progress.advHarvest = Math.max(0, Math.floor(saved.progress.advHarvest));
  }
```

- [ ] **Step 3: 직렬화**

`getGameState()`(`js/game.js:2381`)에서 `plots` 와 같은 규칙 — **켜진 값만 기록**한다:

```js
  gameState.orchard = {
    trees: (gameState.orchard.trees || []).map(t => ({ x: t.x, z: t.z, kind: t.kind, stage: t.stage, age: t.age,
      ...(t.watered ? { watered: 1 } : {}), ...(t.fruit ? { fruit: t.fruit } : {}) })),
    sapSel: gameState.orchard.sapSel,
    settleDate: gameState.orchard.settleDate,
  };
```

- [ ] **Step 4: 세이브 왕복을 손으로 확인한다**

Run: 나무를 심고 새로고침한 다음 개발 콘솔에서 `gameState.orchard.trees`
Expected: 심은 나무가 종류·자리·단계 그대로 살아 있다.

> ⚠️ **읽기 실패를 신규로 오인해 덮어쓰지 않도록** 기존 `js/save-guard.js` 경로를 그대로 탄다. 과수원 필드만 따로 저장하는 길을 새로 만들지 않는다(마을 덮어쓰기 사고 재발 방지).

- [ ] **Step 5: 커밋**

```bash
git add js/game.js
git commit -m "$(cat <<'EOF'
feat: 🍎 과수원 나무와 해금 카운터를 세이브에 넣는다

모르는 종류와 상한 초과를 복원 때 걸러내고, 켜진 값만 기록해
옛 세이브와 호환되게 한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: `orchard_events` 테이블과 적재 경로

**Files:**
- Create: `sql/orchard_events.sql`, `functions/api/orchard-events.js`
- Modify: `worker/index.js` (**라우트 등록 — 빠뜨리면 404**)

**Interfaces:**
- Consumes: Task 8 의 이벤트 이름·파라미터
- Produces: `POST /api/orchard-events`

**왜 DB 를 두는가:** 과수원은 리텐션이 존재 이유인데 GA4 만으로는 광고차단에 유실되고 일별 export 라 당일 확인이 안 된다.

- [ ] **Step 1: DDL 을 쓴다**

`sql/orchard_events.sql`:

```sql
-- 🍎 과수원 이벤트 원장 — GA4 유실·지연 대비. game_logs(좌표 전용)와 섞지 않는다.
create table if not exists public.orchard_events (
  id          bigserial primary key,
  user_id     uuid not null references auth.users(id) on delete cascade,
  at          timestamptz not null default now(),
  event       text not null,      -- sapling_plant | tree_water | fruit_ready | fruit_harvest | fruit_capped
  kind        text,               -- apple | pear | peach | persimmon | chestnut
  n           int,
  near_stream boolean,
  method      text,               -- manual | stream | sprinkler  (source 금지 — GA4 예약어)
  trees       int,
  platform    text
);
create index if not exists orchard_events_user_at on public.orchard_events (user_id, at desc);
alter table public.orchard_events enable row level security;
create policy "own rows" on public.orchard_events
  for all using ((select auth.uid()) = user_id) with check ((select auth.uid()) = user_id);
```

> RLS 는 반드시 `(select auth.uid())` 로 감싼다 — 저장소 규칙(행마다 함수 호출이 도는 것을 막는다).

- [ ] **Step 2: 엔드포인트를 쓴다**

`functions/api/orchard-events.js` 를 만든다. **기존 `functions/api/` 의 다른 파일을 먼저 읽고 그 파일의 인증·CORS·에러 처리 꼴을 그대로 따른다.** 과수원만 다른 방식을 쓰지 않는다.

- [ ] **Step 3: 🚨 라우트를 등록한다**

`worker/index.js` 에 `/api/orchard-events` 를 등록한다. **이 단계를 빠뜨리면 404 가 난다** — `dex-notes`·`daily-quests` 가 정확히 이것 때문에 죽었다.

- [ ] **Step 4: 실제로 찍히는지 확인한다**

Run: 게임에서 묘목을 하나 심은 뒤 Supabase 에서
```sql
select event, kind, near_stream, trees, at from orchard_events order by at desc limit 5;
```
Expected: `sapling_plant | apple | true 또는 false | 1` 행이 보인다. **404 나 0행이면 3단계를 안 한 것이다.**

- [ ] **Step 5: 커밋**

```bash
git add sql/orchard_events.sql functions/api/orchard-events.js worker/index.js
git commit -m "$(cat <<'EOF'
feat: 🍎 과수원 이벤트 원장 테이블과 적재 경로를 만든다

GA4 유실·일별 지연에 대비해 리텐션 핵심 지표를 직접 남긴다.
worker/index.js 라우트 등록을 같은 커밋에 넣는다(404 재발 방지).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: 배포 전 검증과 다음 날 재검증

**Files:** 없음 (검증만)

- [ ] **Step 1: 테스트와 빌드**

Run: `npm test`
Expected: 전부 통과

Run: `npm run build`
Expected: 에러 0

- [ ] **Step 2: 이벤트가 전부 찍히는지 한 바퀴 돈다**

해금 → 입장 → 묘목 구매 → 심기 → 물주기 → (정산) → 수확 순서로 돌면서 아래 아홉 개가 **전부** 나오는지 확인한다. 하나라도 빠지면 퍼널이 끊긴다.

`orchard_unlock` · `orchard_enter` · `sapling_buy` · `sapling_plant` · `tree_mature` · `tree_water` · `fruit_ready` · `fruit_harvest` · `fruit_capped`

- [ ] **Step 3: 예약 파라미터가 안 섞였는지 확인한다**

Run: `grep -n "source:\|medium:\|campaign:\|term:\|content:" js/orchard.js`
Expected: 0건

Run: `grep -n "trackEvent('orchard\|trackEvent('tree_\|trackEvent('fruit_\|trackEvent('sap" js/game.js`
Expected: 나온 줄 어디에도 `source:` `medium:` `campaign:` `term:` `content:` 가 없다. 하나라도 있으면 그 세션의 유입 소스가 덮어써진다.

- [ ] **Step 4: `econ_logs` 의 item 이 같은 id 인지 본다**

```sql
select item, count(*) from econ_logs where source in ('orchard','orchard_unlock') group by 1;
```
Expected: `apple`/`pear`/`peach`/`persimmon`/`chestnut` 다섯 종만. `slot:3` 같은 값이 보이면 Task 8 을 고친다.

- [ ] **Step 5: 🚨 배포 다음 날 BigQuery 로 재검증한다**

GA4 export 는 일별이라 당일엔 확인이 안 된다. 배포 다음 날 아래를 돌린다:

```sql
SELECT event_name,
       COUNT(*) AS n,
       COUNTIF((SELECT p.value.string_value FROM UNNEST(event_params) p WHERE p.key='kind') IS NULL) AS kind_missing
FROM `calm-forest.analytics_547127440.events_*`
WHERE _TABLE_SUFFIX NOT LIKE 'intraday_%'
  AND event_name IN ('sapling_plant','tree_water','fruit_ready','fruit_harvest','fruit_capped','tree_mature')
GROUP BY 1 ORDER BY 1
```
Expected: `kind_missing = 0`. **0 이 아니면 그 기간 데이터는 영구 손실이다** — GA4 export 는 되돌릴 수 없다. 즉시 고쳐 재배포한다.

---

## Self-Review 결과

**스펙 커버리지** — §1 위치 → Task 6 · §2 해금 → Task 4·6·8 · §3 내부 배치 → Task 7 · §4 나무 일생 → Task 2·3·8 · §5 경제 → Task 5 · §6 트래킹 → Task 8·10·11 · §7 구현 구조 → Task 1·7·9. 빠진 절 없음.

**빠진 것을 하나 찾아 범위 밖으로 명시했다** — 스펙 §4 의 🚿광역 물조리개 업그레이드를 `UPGRADES` 에 등록하는 작업이 어느 태스크에도 없었다. 후반 완화 장치라 1차 배포에서 빠져도 기능이 성립하므로 **의도적으로 범위 밖에 둔다** — 나무가 6그루를 넘는 유저가 실제로 생긴 뒤(`trees` 파라미터로 확인) 별도 계획으로 붙인다. 그래서 Task 8 의 `tree_water` 는 `method: 'manual'` 을 고정으로 보내고, `sprinkler` 값은 그 계획에서 추가한다. 스펙 §4 의 해당 문단에 "1차 배포 범위 밖"을 적어 넣는다.

**타입 일관성** — `settleTrees` 가 돌려주는 `{ kind, grew_days }` · `{ kind, n, watered }` · `{ kind }` 가 Task 8 에서 `trackEvent` 에 그대로 전달되는 모양과 일치한다. `fruitKeyOf`/`sapKeyOf` 는 Task 1 정의를 Task 5·8·9 가 같은 이름으로 쓴다. `nearStream(tree, stream)` 의 인자 순서가 Task 2·7·8 에서 모두 같다.

**자리표시자** — 0건.
