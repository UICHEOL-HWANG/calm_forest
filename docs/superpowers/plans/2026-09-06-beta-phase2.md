# 베타 2차 — 맵 계단식 열기 · 일지 폼 · 배정 SQL 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2026-09-09(수) 시작하는 베타 4겹 실험 중 아직 없는 세 가지를 만든다 — ① 테스터 배정(번들 × 맵 순서 교차) SQL ② 바다터·안개숲을 가입 D3/D5 에 엇갈려 여는 잠금 ③ 게임 로그인이 이어지는 일지 폼(`/beta/diary`)과 관제 탭.

**Architecture:** 파라미터·판정은 순수 함수로 `js/tuning.js` 에 모으고(테스트 가능), `js/game.js` 는 `enterSea()`/`enterMist()` 첫 줄에서 그 판정만 부른다. 일지는 Worker 없이 같은 도메인 정적 페이지가 Supabase `beta_diary` 에 RLS 로 직접 upsert 한다. 배정은 `scripts/beta-roster.mjs` 가 이메일 10개를 받아 균형 잡힌 INSERT SQL 을 찍어 준다(사용자가 SQL 편집기에서 실행).

**Tech Stack:** 브라우저 ES 모듈(빌드 없음) · `node --test`(Node 22) · Supabase(Postgres RLS · supabase-js v2 esm.sh) · Cloudflare Workers 정적 배포(`scripts/build-web.mjs`)

**Spec:** `docs/BETA_AB_TEST_PLAN.md` — "🧪 2차 설계 (2026-09-06)" 절

## Global Constraints

- 브랜치 `dev`. 커밋 메시지는 한국어 + 접두사(`Feat:`/`Fix:`/`Docs:`/`Test:`) + 이모지 1개, 끝에 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- **테스트는 저장소 루트 `tests/*.test.mjs`** (`node --test 'tests/**/*.test.mjs'`, 현재 42 pass). `js/` 는 통째로 배포되므로 테스트를 거기 두지 않는다. 루트에 `package.json` 을 만들지 않는다.
- **배포 화이트리스트** `scripts/build-web.mjs` 의 `INCLUDE` — 새 폴더 `beta/` 를 넣어야 배포된다. `sql/`·`scripts/`·`docs/` 는 FORBIDDEN(배포 금지).
- **맵 잠금 대상은 `beta_A`/`beta_B` 뿐.** 일반 유저·게스트·토스 유저는 절대 잠기지 않는다.
- **동굴은 잠그지 않는다** (튜토리얼 채굴·조각 단계).
- 실험 시작일 상수 `2026-09-09`. **D1 = max(시작일, 가입일 KST)**, `day = 오늘(KST) − D1 + 1`. `sea_first`: 바다터 day≥3 · 안개숲 day≥5. `mist_first`: 바다터 day≥5 · 안개숲 day≥3.
- 문구(검수 기본값, 전부 `js/tuning.js` `BETA_COPY` 에 모아 나중에 바꿀 수 있게):
  - 메뉴 버튼 `📝 오늘 일지`
  - 잠김 프롬프트 `🌊 바다터는 {N}일차에 열려요` / `🌫️ 안개 낀 숲은 {N}일차에 열려요`
  - 열린 날 배너 `🌊 바다터가 열렸어요 — 먼 바다 대형 물고기와 줄다리기` / `🌫️ 안개 낀 숲이 열렸어요 — 등불과 ♪음악으로 안개를 정화하는 숲`
  - 일지 제목 `고요한 숲 베타 일지`
- 사람 키: 명단·일지는 **이메일(소문자)**. `user_id` 는 조인용으로만.
- 로컬 검증 파라미터는 `localhost` 에서만 먹고, 그 세션은 `IS_DEV_SESSION` 으로 로깅이 꺼진다 — `DEV_PARAMS` 에 새 파라미터를 반드시 등록한다.
- 모바일 실측 필수(폰 세로). 새 UI 문구는 위 기본값 외에 만들지 않는다.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `scripts/beta-roster.mjs` | 이메일 10개 → 번들 A/B(5:5) × 맵 순서(A 안 3:2, B 안 2:3) 균형 배정 → INSERT SQL 출력. `assignRoster(emails, rng)` 순수 함수 export |
| `tests/beta-roster.test.mjs` | 배정 균형·중복 없음·결정적 시드 |
| `sql/admin_analytics.sql` §베타 | `beta_testers.map_order` 컬럼 · `beta_diary` 테이블+RLS · `cf_beta_overview` 확장(map_order · 일지 수 · 일지 목록) |
| `js/tuning.js` | `BETA` 상수(시작일·mapGate·문구) · 순수 함수 `betaDay` `mapOpenDay` `isMapLocked` `lockLine` `openLine` |
| `tests/map-gate.test.mjs` | 날짜 계산·잠금 판정·일반 유저 면제 |
| `js/first-loop.js` | `DEV_PARAMS` 에 `betaDay`·`forceMapOrder` 추가 |
| `js/supabase-client.js` | `resolveBetaGroup` 이 `map_order` 도 읽어 `state.mapOrder` 에 둠 · 로컬 `?forceMapOrder=` |
| `js/game.js` | `enterSea()`/`enterMist()` 잠금 가드 · 게이트 프롬프트 문구 · 열린 날 배너 1회 · GA4 `map_locked`/`map_opened` |
| `beta/diary.html` | 일지 페이지(로그인 재사용 · day 자동 · 4문항, D7 이상 6문항 · upsert) |
| `index.html` | ☰ 메뉴 `📝 오늘 일지` 버튼(베타만 표시) |
| `scripts/build-web.mjs` | `INCLUDE` 에 `'beta'` |
| `dashboards/beta_monitor.html` | 테스터 표에 맵 순서·일지 N/7 열 · 일지 카드 |
| `docs/BETA_TESTER_GUIDE.md` | 테스터 안내문(보내는 글 그대로) |

---

### Task 1: 배정 스크립트 + 스키마 SQL

**Files:**
- Create: `scripts/beta-roster.mjs`
- Create: `tests/beta-roster.test.mjs`
- Modify: `sql/admin_analytics.sql` (§베타, `create table if not exists public.beta_testers` 바로 뒤)

**Interfaces:**
- Produces: `assignRoster(emails: string[], rng?: () => number) -> Array<{email, grp:'A'|'B', map_order:'sea_first'|'mist_first'}>` · `rosterSql(rows) -> string`
- Produces(DB): `beta_testers.map_order text` · 테이블 `beta_diary(email, day, q1..q6, variant, map_order, client_id, created_at, updated_at)` PK `(email, day)`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/beta-roster.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { assignRoster, rosterSql } from '../scripts/beta-roster.mjs';

const EMAILS = Array.from({ length: 10 }, (_, i) => `T${i + 1}@Example.com`);
// 결정적 난수 — 테스트가 매번 같은 배정을 내게
function seeded(seed = 7) { let s = seed; return () => { s = (s * 16807) % 2147483647; return (s - 1) / 2147483646; }; }

test('10명을 A/B 5:5 로 나눈다', () => {
  const rows = assignRoster(EMAILS, seeded());
  assert.equal(rows.length, 10);
  assert.equal(rows.filter(r => r.grp === 'A').length, 5);
  assert.equal(rows.filter(r => r.grp === 'B').length, 5);
});

test('맵 순서는 A 안에서 sea_first 3 · B 안에서 sea_first 2 (교차 배정)', () => {
  const rows = assignRoster(EMAILS, seeded());
  const seaA = rows.filter(r => r.grp === 'A' && r.map_order === 'sea_first').length;
  const seaB = rows.filter(r => r.grp === 'B' && r.map_order === 'sea_first').length;
  assert.equal(seaA, 3);
  assert.equal(seaB, 2);
  assert.equal(rows.filter(r => r.map_order === 'mist_first').length, 5);
});

test('이메일은 소문자·중복 없음, 같은 시드면 같은 결과', () => {
  const a = assignRoster(EMAILS, seeded(3));
  const b = assignRoster(EMAILS, seeded(3));
  assert.deepEqual(a, b);
  assert.ok(a.every(r => r.email === r.email.toLowerCase()));
  assert.equal(new Set(a.map(r => r.email)).size, 10);
});

test('10명이 아니면 던진다', () => {
  assert.throws(() => assignRoster(EMAILS.slice(0, 9)), /10/);
});

test('SQL 은 insert 한 문장 + on conflict 갱신', () => {
  const sql = rosterSql(assignRoster(EMAILS, seeded()));
  assert.match(sql, /insert into public\.beta_testers \(email, grp, map_order, note\)/);
  assert.match(sql, /on conflict \(email\) do update/);
  assert.equal((sql.match(/\('t\d+@example\.com'/g) || []).length, 10);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test tests/beta-roster.test.mjs`
Expected: FAIL — `Cannot find module '../scripts/beta-roster.mjs'`

- [ ] **Step 3: 구현한다**

`scripts/beta-roster.mjs`:

```js
#!/usr/bin/env node
// =============================================================
//  calm forest · 베타 2차 배정 — 이메일 10개 → 번들 A/B × 맵 순서 INSERT SQL
//  ------------------------------------------------------------
//  ▶ node scripts/beta-roster.mjs a@x.com b@x.com ... (10개)   → SQL 을 stdout 에
//  ▶ 설계: docs/BETA_AB_TEST_PLAN.md 2차 설계 §1
//     번들 5:5. 맵 순서는 번들과 교차 — A 안 sea_first 3 / mist_first 2, B 안 2 / 3.
//     그래야 "바다터 먼저" 가 곧 "A군" 이 되지 않는다.
// =============================================================

function shuffle(arr, rng) {
  const a = arr.slice();
  for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(rng() * (i + 1)); [a[i], a[j]] = [a[j], a[i]]; }
  return a;
}

/** 이메일 10개 → 배정 행. rng 를 주면 결정적(테스트·재현용). */
export function assignRoster(emails, rng = Math.random) {
  const list = [...new Set(emails.map(e => e.trim().toLowerCase()))];
  if (list.length !== 10) throw new Error(`테스터는 정확히 10명이어야 한다 (받은 값: ${list.length})`);
  const order = shuffle(list, rng);
  const A = order.slice(0, 5), B = order.slice(5);
  const tag = (grp, group, seaCount) => group.map((email, i) => ({
    email, grp, map_order: i < seaCount ? 'sea_first' : 'mist_first',
  }));
  return [...tag('A', A, 3), ...tag('B', B, 2)];
}

/** 배정 행 → SQL 편집기에 붙여 넣을 한 문장. 다시 실행해도 안전(upsert). */
export function rosterSql(rows, note = '2차 2026-09-09') {
  const values = rows.map(r => `  ('${r.email}', '${r.grp}', '${r.map_order}', '${note}')`).join(',\n');
  return `insert into public.beta_testers (email, grp, map_order, note) values\n${values}\n`
       + `on conflict (email) do update set grp = excluded.grp, map_order = excluded.map_order, note = excluded.note;\n`;
}

if (import.meta.url === `file://${process.argv[1]}`) {
  const emails = process.argv.slice(2);
  process.stdout.write(rosterSql(assignRoster(emails)));
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test tests/beta-roster.test.mjs`
Expected: 5 pass.

- [ ] **Step 5: 스키마 SQL 을 더한다**

`sql/admin_analytics.sql` 의 `alter table public.beta_testers enable row level security;` **바로 앞**에 넣는다:

```sql
-- 🧪 2차(2026-09-06) — 맵 여는 순서. 번들(grp)과 교차 배정(scripts/beta-roster.mjs 가 만든다)
alter table public.beta_testers add column if not exists map_order text
  check (map_order in ('sea_first','mist_first'));
```

같은 파일, `cf_beta_overview` 의 `drop function` 줄 **앞**에 일지 테이블을 넣는다:

```sql
-- =============================================================
--  📝 베타 일지 (2026-09-06) — /beta/diary 가 로그인 세션으로 직접 upsert
--  본인 이메일 행만 읽고 쓴다. 운영자는 cf_beta_overview 로 본다.
-- =============================================================
create table if not exists public.beta_diary (
  email      text not null references public.beta_testers(email),
  day        int  not null check (day between 1 and 14),   -- D1=시작일. 늦게 쓴 일지도 day 로 구분
  q1 text, q2 text,
  q3 int check (q3 between 1 and 5),
  q4 text,
  q5 text, q6 text,                                        -- D7 전용(제일 재밌던 곳 · 처음 10분 선호)
  variant    text,                                         -- 쓴 시점의 beta_A/B (분석 조인용 스냅샷)
  map_order  text,
  client_id  text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (email, day)
);
alter table public.beta_diary enable row level security;
drop policy if exists beta_diary_self_rw on public.beta_diary;
create policy beta_diary_self_rw on public.beta_diary
  for all to authenticated
  using (email = lower(coalesce(auth.jwt() ->> 'email', '')))
  with check (email = lower(coalesce(auth.jwt() ->> 'email', '')));
```

- [ ] **Step 6: 커밋**

```bash
git add scripts/beta-roster.mjs tests/beta-roster.test.mjs sql/admin_analytics.sql
git commit -m "$(cat <<'MSG'
Feat: 🧪 베타 2차 배정 스크립트 + map_order · beta_diary 스키마

번들 5:5 안에서 맵 순서를 3:2/2:3 으로 교차 배정해 두 실험이 한 몸이 되지 않게 한다.
일지는 본인 이메일 행만 읽고 쓰는 RLS 로 잠근다.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
)"
```

---

### Task 2: 맵 잠금 판정 — 순수 함수 + 배정 읽기

**Files:**
- Modify: `js/tuning.js`
- Modify: `js/first-loop.js` (`DEV_PARAMS`)
- Modify: `js/supabase-client.js:56-70` (`resolveBetaGroup`)
- Create: `tests/map-gate.test.mjs`

**Interfaces:**
- Produces(`js/tuning.js`):
  - `BETA = { startDate:'2026-09-09', mapGate:{ sea_first:{sea:3, mist:5}, mist_first:{sea:5, mist:3} } }`
  - `BETA_COPY = { diaryBtn, lock:{sea, mist}, open:{sea, mist}, diaryTitle }` — `{N}` 자리표시
  - `kstDate(ms) -> 'YYYY-MM-DD'`
  - `betaDay(createdAtIso, nowMs) -> number` (D1=1. 가입 전·null 이면 1)
  - `mapOpenDay(mapOrder, map) -> number|null` (배정 없음 → null = 안 잠금)
  - `isMapLocked({variant, mapOrder, createdAtIso, nowMs}, map) -> boolean`
  - `lockLine(map, openDay) -> string` · `openLine(map) -> string`
- Produces(`js/supabase-client.js`): `state.mapOrder: 'sea_first'|'mist_first'|null`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/map-gate.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { BETA, BETA_COPY, kstDate, betaDay, mapOpenDay, isMapLocked, lockLine, openLine } from '../js/tuning.js';

const T = (iso) => Date.parse(iso);

test('kstDate: UTC 자정 직전은 KST 로 다음 날', () => {
  assert.equal(kstDate(T('2026-09-08T15:30:00Z')), '2026-09-09');   // KST 00:30
  assert.equal(kstDate(T('2026-09-08T14:30:00Z')), '2026-09-08');   // KST 23:30
});

test('betaDay: 시작일 이후 가입은 가입일이 D1', () => {
  assert.equal(betaDay('2026-09-10T01:00:00Z', T('2026-09-10T02:00:00Z')), 1);
  assert.equal(betaDay('2026-09-10T01:00:00Z', T('2026-09-12T02:00:00Z')), 3);
});

test('betaDay: 시작일 전에 미리 가입해도 D1 은 시작일', () => {
  assert.equal(betaDay('2026-09-07T01:00:00Z', T('2026-09-09T02:00:00Z')), 1);
  assert.equal(betaDay('2026-09-07T01:00:00Z', T('2026-09-08T02:00:00Z')), 0);   // 시작 전날 = 0
});

test('betaDay: created_at 없으면 1', () => {
  assert.equal(betaDay(null, T('2026-09-20T00:00:00Z')), 1);
});

test('mapOpenDay: 순서별 여는 날', () => {
  assert.equal(mapOpenDay('sea_first', 'sea'), 3);
  assert.equal(mapOpenDay('sea_first', 'mist'), 5);
  assert.equal(mapOpenDay('mist_first', 'sea'), 5);
  assert.equal(mapOpenDay('mist_first', 'mist'), 3);
  assert.equal(mapOpenDay(null, 'sea'), null);
});

test('isMapLocked: 베타 테스터는 여는 날 전까지 잠김', () => {
  const s = { variant: 'beta_A', mapOrder: 'sea_first', createdAtIso: '2026-09-09T01:00:00Z' };
  assert.equal(isMapLocked({ ...s, nowMs: T('2026-09-10T02:00:00Z') }, 'sea'), true);    // D2
  assert.equal(isMapLocked({ ...s, nowMs: T('2026-09-11T02:00:00Z') }, 'sea'), false);   // D3
  assert.equal(isMapLocked({ ...s, nowMs: T('2026-09-11T02:00:00Z') }, 'mist'), true);   // D3, 안개숲은 D5
  assert.equal(isMapLocked({ ...s, nowMs: T('2026-09-13T02:00:00Z') }, 'mist'), false);  // D5
});

test('isMapLocked: 일반 유저·게스트·배정 없음은 절대 안 잠김', () => {
  const now = T('2026-09-09T02:00:00Z');
  assert.equal(isMapLocked({ variant: 'control', mapOrder: 'sea_first', createdAtIso: '2026-09-09T01:00:00Z', nowMs: now }, 'sea'), false);
  assert.equal(isMapLocked({ variant: 'beta_B', mapOrder: null, createdAtIso: '2026-09-09T01:00:00Z', nowMs: now }, 'sea'), false);
  assert.equal(isMapLocked({ variant: 'beta_B', mapOrder: 'mist_first', createdAtIso: null, nowMs: now }, 'sea'), true); // created_at 없으면 D1 취급 → 잠김
});

test('문구: N 이 채워지고 열림 문구는 맵별', () => {
  assert.equal(lockLine('sea', 3), '🌊 바다터는 3일차에 열려요');
  assert.equal(lockLine('mist', 5), '🌫️ 안개 낀 숲은 5일차에 열려요');
  assert.match(openLine('sea'), /바다터가 열렸어요/);
  assert.match(openLine('mist'), /안개 낀 숲이 열렸어요/);
  assert.equal(BETA_COPY.diaryBtn, '📝 오늘 일지');
  assert.equal(BETA.startDate, '2026-09-09');
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test tests/map-gate.test.mjs`
Expected: FAIL — `does not provide an export named 'BETA'`

- [ ] **Step 3: `js/tuning.js` 에 구현한다** (파일 끝에 추가. 기존 `TUNING`·함수는 손대지 않는다)

```js
// =============================================================
//  🧪 베타 2차 (2026-09-06) — 맵 계단식 열기 · 문구 (docs/BETA_AB_TEST_PLAN.md 2차 설계 §5)
//  잠금 대상은 beta_A/beta_B 뿐. 판정은 여기 순수 함수로, game.js 는 부르기만 한다.
// =============================================================
export const BETA = {
  startDate: '2026-09-09',                       // D1 = max(시작일, 가입일 KST)
  mapGate: {
    sea_first:  { sea: 3, mist: 5 },
    mist_first: { sea: 5, mist: 3 },
  },
};

export const BETA_COPY = {
  diaryBtn: '📝 오늘 일지',
  diaryTitle: '고요한 숲 베타 일지',
  lock: { sea: '🌊 바다터는 {N}일차에 열려요', mist: '🌫️ 안개 낀 숲은 {N}일차에 열려요' },
  open: {
    sea:  '🌊 바다터가 열렸어요 — 먼 바다 대형 물고기와 줄다리기',
    mist: '🌫️ 안개 낀 숲이 열렸어요 — 등불과 ♪음악으로 안개를 정화하는 숲',
  },
};

/** ms → KST 날짜 'YYYY-MM-DD'. 서버·브라우저 시간대와 무관하게 한국 날짜로 센다. */
export function kstDate(ms) {
  return new Date(ms + 9 * 3600 * 1000).toISOString().slice(0, 10);
}

const DAY_MS = 86400000;
function dateToMs(ymd) { return Date.parse(ymd + 'T00:00:00Z'); }

/** 며칠째인가. D1 = max(시작일, 가입일). 시작 전날은 0, created_at 이 없으면 1. */
export function betaDay(createdAtIso, nowMs = Date.now()) {
  if (!createdAtIso) return 1;
  const d1 = Math.max(dateToMs(BETA.startDate), dateToMs(kstDate(Date.parse(createdAtIso))));
  return Math.floor((dateToMs(kstDate(nowMs)) - d1) / DAY_MS) + 1;
}

/** 이 순서에서 이 맵이 열리는 날. 배정이 없으면 null(= 잠그지 않는다). */
export function mapOpenDay(mapOrder, map) {
  return BETA.mapGate[mapOrder]?.[map] ?? null;
}

/** 잠겼나. 베타 테스터(beta_*)이고 배정이 있으며 아직 여는 날 전이면 true. */
export function isMapLocked({ variant, mapOrder, createdAtIso, nowMs = Date.now() }, map) {
  if (!/^beta_/.test(variant || '')) return false;
  const openDay = mapOpenDay(mapOrder, map);
  if (openDay == null) return false;
  return betaDay(createdAtIso, nowMs) < openDay;
}

export function lockLine(map, openDay) { return BETA_COPY.lock[map].replace('{N}', String(openDay)); }
export function openLine(map) { return BETA_COPY.open[map]; }
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test 'tests/**/*.test.mjs'`
Expected: 42 + 5(Task 1) + 8 = 55 pass, 0 fail.

- [ ] **Step 5: 배정을 읽고 로컬 강제 파라미터를 등록한다**

`js/supabase-client.js` `state` 객체(18행 근처)에 한 줄:

```js
  mapOrder: null,      // 🧪 베타 2차 — 맵 여는 순서('sea_first'|'mist_first'), 명단 테이블에서
```

`resolveBetaGroup` 안의 조회를 `grp, map_order` 로 바꾸고 로컬 강제를 더한다:

```js
    const forcedOrder = isLocal ? new URLSearchParams(location.search).get('forceMapOrder') : null;
    if (forcedOrder === 'sea_first' || forcedOrder === 'mist_first') state.mapOrder = forcedOrder;
    if (supabase && !isAnon(session)) {
      const email = (session.user.email || '').toLowerCase();
      const { data } = await supabase.from('beta_testers').select('grp, map_order').eq('email', email).maybeSingle();
      if (data?.grp) state.variant = 'beta_' + data.grp;   // 명단이 최우선
      if (data?.map_order) state.mapOrder = data.map_order;
    }
```

`js/first-loop.js` 의 `DEV_PARAMS` 배열에 `'betaDay', 'forceMapOrder'` 를 추가한다(로컬 검증 세션의 로깅 차단). `tests/first-loop.test.mjs` 의 `isDevSession` 테스트에 한 줄 추가:

```js
  assert.equal(isDevSession('?forceVariant=beta_A&forceMapOrder=sea_first&betaDay=3'), true);
```

- [ ] **Step 6: 통과 확인 후 커밋**

Run: `node --test 'tests/**/*.test.mjs'` → 55 pass.

```bash
git add js/tuning.js js/first-loop.js js/supabase-client.js tests/map-gate.test.mjs tests/first-loop.test.mjs
git commit -m "$(cat <<'MSG'
Feat: 🗺️ 맵 계단식 열기 판정 — D1=max(시작일, 가입일) · sea_first/mist_first 순수 함수 + 명단에서 map_order 읽기

잠금은 beta_* 만. 일반 유저·게스트·배정 없음은 어떤 경로로도 안 잠긴다.
문구는 BETA_COPY 한 곳에 — 검수 뒤 바꿔도 코드가 안 바뀐다.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
)"
```

---

### Task 3: 게임 배선 — 입구 가드 · 프롬프트 · 열린 날 배너

**Files:**
- Modify: `js/game.js` — import(27행 근처), `enterMist()`(4079), `enterSea()`(4965), 게이트 프롬프트(7053·7059), 게임 시작부(`initChurnPredictor` 호출 근처 ~1537)

**Interfaces:**
- Consumes: `isMapLocked`, `mapOpenDay`, `betaDay`, `lockLine`, `openLine`(Task 2) · `authState.variant/mapOrder/createdAt` · `ui.toast`, `ui.showHintBanner`, `trackEvent`, `gameState.hintsSeen`
- Produces: GA4 `map_locked{map, day, open_day}` · `map_opened{map, day}` · 세이브 `hintsSeen.mapOpen_sea` / `hintsSeen.mapOpen_mist`

- [ ] **Step 1: 잠금 상태 헬퍼를 만든다** (게임 로직은 브라우저 전용이라 단위 테스트 대신 헬퍼를 얇게 두고 Step 5 에서 브라우저로 확인)

import 줄(`from './tuning.js'`)을 이렇게 바꾼다:

```js
import { TUNING, rewardBoostMult, easeMult, isMapLocked, mapOpenDay, betaDay, lockLine, openLine } from './tuning.js';   // 🧪 [베타 A/B] + 2차 맵 계단식
```

`initChurnPredictor` 정의 위(1400행 근처)에 헬퍼를 둔다:

```js
// ── 🧪 [베타 2차] 맵 계단식 열기 — 판정은 tuning.js, 여기선 상태만 넘긴다 ──────────
//    로컬 검증: ?betaDay=3 (localhost 전용, DEV_PARAMS 라 로깅 꺼짐)
function betaNowMs() {
  const isLocal = location.hostname === 'localhost' || location.hostname === '127.0.0.1';
  const forced = isLocal ? Number(new URLSearchParams(location.search).get('betaDay')) : NaN;
  if (Number.isFinite(forced) && forced >= 1 && authState.createdAt) {
    // 가입일을 D1 로 보고 forced 일차의 정오로 시계를 돌린다
    return Date.parse(authState.createdAt) + (forced - 1) * 86400000;
  }
  return Date.now();
}
function mapLocked(map) {
  return isMapLocked({ variant: authState.variant, mapOrder: authState.mapOrder,
                       createdAtIso: authState.createdAt, nowMs: betaNowMs() }, map);
}
/** 잠긴 입구에서 액션했을 때 — 토스트 + GA4. true 면 입장을 막는다. */
function blockIfLocked(map) {
  if (!mapLocked(map)) return false;
  const openDay = mapOpenDay(authState.mapOrder, map);
  ui.toast?.(lockLine(map, openDay));
  trackEvent('map_locked', { map, day: betaDay(authState.createdAt, betaNowMs()), open_day: openDay });
  return true;
}
/** 열린 날 첫 접속 배너 — 세이브 hintsSeen 으로 1회. 베타가 아니면 아무것도 안 한다. */
function announceMapOpens() {
  if (!/^beta_/.test(authState.variant || '') || !authState.mapOrder) return;
  for (const map of ['sea', 'mist']) {
    const key = 'mapOpen_' + map;
    if (gameState.hintsSeen?.[key] || mapLocked(map)) continue;
    const openDay = mapOpenDay(authState.mapOrder, map);
    if (betaDay(authState.createdAt, betaNowMs()) < openDay) continue;   // 방어
    gameState.hintsSeen[key] = true;
    const [ico, ...rest] = openLine(map).split(' ');
    const [title, line] = rest.join(' ').split(' — ');
    ui.showHintBanner?.({ ico, title, line: line || '', near: () => true });
    trackEvent('map_opened', { map, day: betaDay(authState.createdAt, betaNowMs()) });
  }
}
```

⚠️ `gameState.hintsSeen` 이 없는 오래된 세이브가 있을 수 있다 — `hintsSeen` 을 처음 만드는 곳(예: `merchantVisit` 처리부)을 grep 해서 같은 방식으로 `gameState.hintsSeen ||= {}` 를 `announceMapOpens` 첫 줄에 둔다.

- [ ] **Step 2: 입장 가드**

`enterMist()` 첫 줄, `enterSea()` 첫 줄에 각각:

```js
  if (blockIfLocked('mist')) return;   // 🧪 [베타 2차] 계단식 열기
```
```js
  if (blockIfLocked('sea')) return;    // 🧪 [베타 2차] 계단식 열기
```

`?sea=1`·`?mist=1` 개발 진입(1530행)도 같은 함수를 타므로 따로 손대지 않는다.

- [ ] **Step 3: 게이트 프롬프트**

7053행 `prompt = '🌫️ 안개 낀 숲에 들어가기';` 를:

```js
    prompt = mapLocked('mist') ? lockLine('mist', mapOpenDay(authState.mapOrder, 'mist')) : '🌫️ 안개 낀 숲에 들어가기';
```

7059행 `prompt = '🌊 바다터 (먼 바다로 나가볼까요?)';` 를:

```js
    prompt = mapLocked('sea') ? lockLine('sea', mapOpenDay(authState.mapOrder, 'sea')) : '🌊 바다터 (먼 바다로 나가볼까요?)';
```

잠긴 동안은 `firstHintBanner('mistGate'…)`/`('seaGate'…)` 가 뜨지 않게 각 줄을 `if (!mapLocked('mist'))` / `if (!mapLocked('sea'))` 로 감싼다(열리기 전에 "먼 바다 대형 물고기…" 를 보여 주면 잠금이 우습다).

- [ ] **Step 4: 열린 날 배너 호출**

`enterGame()` 안, `if (!IS_DEV_SESSION) { try { initChurnPredictor(); } … }` 바로 뒤에:

```js
  setTimeout(announceMapOpens, 4000);   // 🧪 [베타 2차] 열린 맵 안내 — 시작 직후 코치·환영 배너와 겹치지 않게 4초 뒤
```

`node --check js/game.js` 로 문법 확인.

- [ ] **Step 5: 브라우저 확인** (dev 서버는 `.claude/launch.json` 의 `calm-forest`(포트 8000) 를 preview_start 로. **Bash 로 서버를 띄우지 않는다.**)

⚠️ 게임 로그인이 필요하다. 로컬에서 구글 로그인이 되면 `?forceVariant=beta_A&forceMapOrder=sea_first&betaDay=2` 로 열어 확인하고, 안 되면 게스트로 열되 `forceVariant` 는 명단이 아닌 로컬 강제라 게스트에도 먹는다(`resolveBetaGroup` 은 익명이어도 forced 를 적용). 확인 항목:

1. `betaDay=2`: 바다터 입구 프롬프트가 `🌊 바다터는 3일차에 열려요`, 액션하면 토스트가 뜨고 입장 안 됨. 안개숲 프롬프트는 `5일차`.
2. `betaDay=3`: 접속 4초 뒤 배너 `바다터가 열렸어요…` 1회. 바다터 입장 됨. 안개숲은 여전히 잠김. 새로고침하면 배너가 다시 안 뜸(세이브 `hintsSeen.mapOpen_sea`).
3. `betaDay=5`: 안개숲도 열림, 배너 1회.
4. `forceVariant` 없이(일반 유저): 두 입구 다 평소 프롬프트, 입장 됨.
5. `forceMapOrder=mist_first&betaDay=3`: 안개숲 열림 · 바다터 잠김(반대).
6. 콘솔 오류 0. 네트워크에 `map_locked`/`map_opened` 는 dev 파라미터라 GA4 로 안 나간다(정상).

- [ ] **Step 6: 커밋**

```bash
git add js/game.js
git commit -m "$(cat <<'MSG'
Feat: 🗺️ 바다터·안개숲 계단식 열기 배선 — 입구 가드 · 잠김 프롬프트 · 열린 날 배너 1회

가드는 enterSea/enterMist 첫 줄 하나씩 — 어떤 경로로 들어와도 같은 판정을 탄다.
열린 날 안내는 세이브 hintsSeen 으로 1회, 시작 4초 뒤(코치와 안 겹치게).

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
)"
```

---

### Task 4: 일지 페이지 `/beta/diary` + 메뉴 버튼 + 배포 목록

**Files:**
- Create: `beta/diary.html`
- Modify: `index.html:943` 근처(메뉴 버튼) · `index.html:2184` 근처(로그인 완료 지점)
- Modify: `scripts/build-web.mjs:36-46` (`INCLUDE`)

**Interfaces:**
- Consumes: `beta_diary` 테이블(Task 1) · `betaDay`, `BETA_COPY`(Task 2) · `js/config.js` `CONFIG`, `isSupabaseConfigured`
- Produces: `beta_diary` upsert `{email, day, q1, q2, q3, q4, q5?, q6?, variant, map_order, client_id}` on conflict `(email, day)`

- [ ] **Step 1: 페이지를 만든다**

`beta/diary.html` (같은 도메인 정적 페이지. 스타일은 `dashboards/_dash.css` 를 **복사하지 않고** 페이지 안에 최소한만 둔다 — 대시보드와 분리하려는 것이므로):

```html
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
<meta name="robots" content="noindex" />
<title>고요한 숲 베타 일지</title>
<style>
  :root { --bg:#f3f6f4; --card:#fbfcfb; --line:#dde5e0; --ink:#16241d; --ink2:#4e5f56; --ink3:#7d8d84; --mint:#8fd6a0; --ok:#217a4e; --warn:#c14f3a; }
  @media (prefers-color-scheme: dark) { :root { --bg:#111917; --card:#182320; --line:#26332e; --ink:#e8efeb; --ink2:#b7c4bd; --ink3:#7f8d86; --mint:#5fae78; } }
  * { box-sizing: border-box; }
  body { margin:0; background:var(--bg); color:var(--ink); font: 15px/1.55 -apple-system, "Apple SD Gothic Neo", "Pretendard", "Noto Sans KR", system-ui, sans-serif; }
  main { max-width: 560px; margin: 0 auto; padding: 20px 16px 48px; }
  h1 { font-size: 20px; margin: 6px 0 2px; }
  .sub { color: var(--ink3); font-size: 13px; margin: 0 0 18px; }
  .card { background: var(--card); border: 1px solid var(--line); border-radius: 14px; padding: 16px; margin-bottom: 12px; }
  label { display:block; font-weight:700; margin-bottom:6px; }
  textarea, select { width:100%; border:1px solid var(--line); border-radius:10px; padding:10px; font:inherit; background:transparent; color:inherit; }
  textarea { min-height: 84px; resize: vertical; }
  .scale { display:flex; gap:8px; }
  .scale label { flex:1; text-align:center; border:1px solid var(--line); border-radius:10px; padding:10px 0; font-weight:700; cursor:pointer; margin:0; }
  .scale input { display:none; }
  .scale input:checked + span { color: var(--ok); }
  .scale label:has(input:checked) { border-color: var(--ok); background: color-mix(in srgb, var(--mint) 25%, transparent); }
  button.primary { width:100%; border:none; border-radius:12px; padding:14px; font: 700 16px/1 inherit; background: var(--mint); color:#0e2a1a; cursor:pointer; }
  button.primary[disabled] { opacity:.5; cursor:default; }
  .msg { padding: 14px; border-radius: 12px; background: var(--card); border:1px solid var(--line); }
  .msg.ok { border-color: var(--ok); }
  .msg.warn { border-color: var(--warn); }
  .hint { font-size: 12.5px; color: var(--ink3); margin-top: 6px; }
</style>
</head>
<body>
<main>
  <h1 id="title">고요한 숲 베타 일지</h1>
  <p class="sub" id="sub">불러오는 중…</p>
  <div id="gate" class="msg" style="display:none"></div>
  <form id="form" style="display:none" autocomplete="off">
    <div class="card"><label for="q1">1. 오늘 뭐 했어요?</label><textarea id="q1" required></textarea></div>
    <div class="card"><label for="q2">2. 어디서 막히거나 지루했어요?</label><textarea id="q2" required></textarea></div>
    <div class="card"><label>3. 돈 안 받아도 내일 또 할 것 같아요?</label>
      <div class="scale" id="q3">
        <label><input type="radio" name="q3" value="1" required /><span>1</span></label>
        <label><input type="radio" name="q3" value="2" /><span>2</span></label>
        <label><input type="radio" name="q3" value="3" /><span>3</span></label>
        <label><input type="radio" name="q3" value="4" /><span>4</span></label>
        <label><input type="radio" name="q3" value="5" /><span>5</span></label>
      </div><div class="hint">1 = 전혀 아니다 · 5 = 꼭 한다</div></div>
    <div class="card"><label for="q4">4. 화면 아래 작은 안내가 뜬 적 있어요? 뭐라고 떴고 따라 했어요?</label><textarea id="q4"></textarea></div>
    <div id="final" style="display:none">
      <div class="card"><label for="q5">5. 일주일 중 제일 재밌었던 곳은?</label>
        <select id="q5"><option value="">고르세요</option><option>바다터</option><option>안개 낀 숲</option><option>마을</option><option>텃밭</option><option>기타</option></select></div>
      <div class="card"><label for="q6">6. 처음 10분 순서 두 가지 영상 중 어느 쪽이 좋아요?</label>
        <select id="q6"><option value="">고르세요</option><option value="A">A</option><option value="B">B</option></select>
        <div class="hint">영상 링크는 안내 메시지로 따로 보내드려요</div></div>
    </div>
    <button class="primary" id="save" type="submit">저장</button>
    <div class="hint">저장하면 "저장됐어요"가 뜹니다. 같은 날 다시 열면 고쳐 쓸 수 있어요.</div>
  </form>
</main>
<script type="module">
  import { CONFIG, isSupabaseConfigured } from '../js/config.js';
  import { betaDay, BETA_COPY } from '../js/tuning.js';
  import { clientId } from '../js/i18n.js';
  const $ = (id) => document.getElementById(id);
  const gate = (html, cls = '') => { $('gate').className = 'msg ' + cls; $('gate').innerHTML = html; $('gate').style.display = 'block'; $('form').style.display = 'none'; };

  (async () => {
    $('title').textContent = BETA_COPY.diaryTitle;
    if (!isSupabaseConfigured()) return gate('설정이 비어 있어요. 운영자에게 알려 주세요.', 'warn');
    const { createClient } = await import('https://esm.sh/@supabase/supabase-js@2');
    const sb = createClient(CONFIG.SUPABASE_URL, CONFIG.SUPABASE_ANON_KEY, { auth: { detectSessionInUrl: true, persistSession: true } });
    const { data: s } = await sb.auth.getSession();
    const user = s?.session?.user;
    if (!user || user.is_anonymous) {
      gate('게임에 쓰던 구글 계정으로 로그인해 주세요.<br><br><button class="primary" id="lg">구글로 로그인</button>');
      $('lg').onclick = () => sb.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: location.origin + location.pathname } });
      return;
    }
    const email = (user.email || '').toLowerCase();
    const { data: me } = await sb.from('beta_testers').select('grp, map_order').eq('email', email).maybeSingle();
    if (!me) return gate('베타 테스터 명단에 없는 계정이에요. 안내받은 구글 계정이 맞는지 확인해 주세요.', 'warn');

    const day = Math.max(1, betaDay(user.created_at, Date.now()));
    $('sub').textContent = `${email} · ${day}일차`;
    if (day >= 7) $('final').style.display = 'block';

    // 오늘 이미 썼으면 불러와서 고쳐 쓸 수 있게
    const { data: prev } = await sb.from('beta_diary').select('*').eq('email', email).eq('day', day).maybeSingle();
    if (prev) {
      $('q1').value = prev.q1 || ''; $('q2').value = prev.q2 || ''; $('q4').value = prev.q4 || '';
      if (prev.q3) document.querySelector(`input[name=q3][value="${prev.q3}"]`).checked = true;
      if (prev.q5) $('q5').value = prev.q5; if (prev.q6) $('q6').value = prev.q6;
      $('sub').textContent += ' · 오늘 일지를 이미 썼어요(고쳐 쓰기 가능)';
    }
    $('form').style.display = 'block';

    $('form').addEventListener('submit', async (e) => {
      e.preventDefault();
      $('save').disabled = true; $('save').textContent = '저장 중…';
      const row = {
        email, day,
        q1: $('q1').value.trim(), q2: $('q2').value.trim(),
        q3: Number(document.querySelector('input[name=q3]:checked')?.value || 0) || null,
        q4: $('q4').value.trim() || null,
        q5: $('q5').value || null, q6: $('q6').value || null,
        variant: 'beta_' + me.grp, map_order: me.map_order, client_id: clientId(),
        updated_at: new Date().toISOString(),
      };
      const { error } = await sb.from('beta_diary').upsert(row, { onConflict: 'email,day' });
      if (error) { $('save').disabled = false; $('save').textContent = '저장'; alert('저장이 안 됐어요. 잠시 뒤 다시 눌러 주세요.\n' + error.message); return; }
      gate(`✅ 저장됐어요. ${day}일차 일지 고마워요!<br><span class="hint">이 창은 닫아도 돼요.</span>`, 'ok');
    });
  })();
</script>
</body>
</html>
```

`js/i18n.js` 의 `clientId` export 를 확인한다(`js/supabase-client.js:14` 가 이미 import 함). i18n 옵저버가 페이지 DOM 을 번역하려 들지 않는지 확인 — `watchDom` 은 index.html 이 명시적으로 부를 때만 돈다.

- [ ] **Step 2: 배포 목록**

`scripts/build-web.mjs` `INCLUDE` 의 `'dashboards',` 다음 줄에:

```js
  'beta',            // 🧪 베타 일지 페이지(/beta/diary.html) — 대시보드와 분리
```

`node scripts/build-web.mjs` 로 `dist/beta/diary.html` 이 생기는지 확인(`dist/` 는 git-ignored).

- [ ] **Step 3: 메뉴 버튼**

`index.html:943` `lb-btn` 줄 바로 뒤에:

```html
      <button id="diary-btn" style="display:none;"><span class="uico">📝</span><span class="ulbl">오늘 일지</span></button>
```

`index.html:2184` 근처 `trackLogin(authState.provider || 'offline');` **바로 뒤**(로그인이 끝나 `authState.variant` 가 명단으로 확정된 자리. 2138행의 `startCoach()` 안에 두면 튜토리얼이 있는 새 유저에게만 실행되므로 거기가 아니다)에:

```js
      // 🧪 [베타 2차] 일지 버튼 — 베타 테스터(beta_*)에게만. 새 탭으로 열어 게임은 그대로 둔다.
      if (/^beta_/.test(authState.variant || '')) {
        $('diary-btn').style.display = '';
        $('diary-btn').onclick = () => { trackEvent('diary_open', { day: 0 }); window.open('/beta/diary.html', '_blank', 'noopener'); };
      }
```

`trackEvent` 는 이미 index.html 에 import 돼 있다(1454행). `day` 는 index.html 에서 `betaDay` 를 import 해 채운다: import 줄 1461 을 `import { TUNING, isBetaA, betaDay } from './js/tuning.js';` 로 바꾸고 `day: betaDay(authState.createdAt)` 로.

- [ ] **Step 4: 브라우저·폰 확인**

1. 로컬 dev 서버(preview_start `calm-forest`)에서 `/beta/diary.html` 열기 — 로그인 게이트 문구가 뜬다.
2. 구글 로그인 후(명단에 없는 계정이면) "명단에 없는 계정" 문구. **Supabase 에 스키마 SQL 을 아직 안 돌렸으면 여기서 멈추고 사용자에게 요청**(Task 1 Step 5 의 SQL).
3. 명단 SQL 실행 뒤 본인 계정을 임시로 명단에 넣고(`note='검증'`) 4문항 저장 → "저장됐어요". 새로고침 → 값이 불러와지고 "이미 썼어요" 표시. Supabase `beta_diary` 행 1개 확인. 검증 끝나면 그 행과 명단 행을 지운다.
4. 폰 세로(resize_window mobile)에서 3번 반복 — 라디오 5칸이 한 줄에, 저장 버튼이 손가락 크기.
5. 게임에서 `?forceVariant=beta_A` 로 ☰ 메뉴에 `📝 오늘 일지` 가 보이고, 없으면 안 보인다.

- [ ] **Step 5: 커밋**

```bash
git add beta/diary.html index.html scripts/build-web.mjs
git commit -m "$(cat <<'MSG'
Feat: 📝 베타 일지 — /beta/diary (로그인 이어짐 · day 자동 · 4문항, D7 에 2문항) + ☰ 메뉴 버튼

구글 설문지 대신 같은 도메인에 둔다. 누가 며칠째 쓴 일지인지 자동으로 붙고
그날 플레이 기록과 조인된다. Worker 라우트 없이 RLS 로 본인 행만 upsert.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
)"
```

---

### Task 5: 관제 — 맵 순서·일지 열 + 일지 카드

**Files:**
- Modify: `sql/admin_analytics.sql` (`cf_beta_overview` 의 `roster`·`testers`·`select jsonb_build_object`)
- Modify: `dashboards/beta_monitor.html:84-95`(테스터 카드 뒤 새 카드) · `renderTesters`(241-266) · `render`(228-238)

**Interfaces:**
- Consumes: `beta_diary`, `beta_testers.map_order`(Task 1)
- Produces(RPC): `testers[].map_order`, `testers[].diary_days`(쓴 일수), `diary[]` = `{email, grp, day, q1, q2, q3, q4, q5, q6, updated_at}` 최신순 70건

- [ ] **Step 1: RPC 확장**

`roster` CTE 를:

```sql
  roster as (  -- 명단 ⨝ 계정(가입 전 테스터는 user_id null로 표시)
    select bt.email, bt.grp, bt.map_order, u.id as user_id
    from beta_testers bt
    left join auth.users u on lower(u.email) = bt.email
  ),
  diary_cnt as (
    select email, count(*) as diary_days from beta_diary group by email
  ),
```

`tester_sessions` 의 select/group by 에 `r.map_order` 를 추가하고, `'testers'` jsonb 에 `'map_order', ts.map_order, 'diary_days', coalesce(dc.diary_days, 0)` 를 넣고 `left join diary_cnt dc on dc.email = ts.email` 을 붙인다. `jsonb_build_object` 마지막에:

```sql
    'diary', (select coalesce(jsonb_agg(jsonb_build_object(
                  'email', d.email, 'grp', bt.grp, 'day', d.day, 'q1', d.q1, 'q2', d.q2, 'q3', d.q3,
                  'q4', d.q4, 'q5', d.q5, 'q6', d.q6, 'updated_at', d.updated_at
                ) order by d.updated_at desc), '[]'::jsonb)
                from (select * from beta_diary order by updated_at desc limit 70) d
                join beta_testers bt on bt.email = d.email)
```

- [ ] **Step 2: 표 열과 카드**

`renderTesters` 헤더에 `<th>군</th>` 다음 `<th>맵 순서</th><th style="text-align:right">일지</th>` 를, 행에 `<td><span class="grp ${grpCls}">…</span></td>` 다음:

```js
              <td>${r.map_order === 'sea_first' ? '🌊→🌫️' : r.map_order === 'mist_first' ? '🌫️→🌊' : '—'}</td>
              <td class="n">${nf(r.diary_days ?? 0)}/7</td>
```

`beta_monitor.html` 87행 테스터 카드 `</div>` 뒤에 카드를 추가:

```html
      <div class="card" style="grid-column:1/-1;">
        <h2>📝 일지</h2>
        <p class="hint">테스터가 /beta/diary 에 쓴 것 · 최신순 · 3번은 "돈 안 받아도 내일 또?"(1~5)</p>
        <div style="overflow-x:auto"><table class="mini" id="diaryTbl"></table></div>
      </div>
```

`render(d)` 끝에 `renderDiary(d.diary || []);` 를 넣고 함수를 추가:

```js
    // ── 일지 ──
    function renderDiary(rows) {
      if (!rows.length) { $('diaryTbl').innerHTML = '<tr><td class="empty">아직 쓴 일지가 없습니다.</td></tr>'; return; }
      const cut = (s, n = 90) => { s = String(s ?? ''); return escapeHtml(s.length > n ? s.slice(0, n) + '…' : s); };
      $('diaryTbl').innerHTML =
        '<tr><th>테스터</th><th>군</th><th style="text-align:right">일차</th><th>1 오늘 한 것</th><th>2 막힌 곳</th><th style="text-align:right">3</th><th>4 안내 봤나</th><th>5·6</th><th style="text-align:right">쓴 시각</th></tr>'
        + rows.map(r => `<tr>
              <td>${escapeHtml(r.email)}</td>
              <td><span class="grp ${r.grp === 'A' ? 'a' : 'b'}">${escapeHtml(r.grp || '?')}</span></td>
              <td class="n">${nf(r.day)}</td>
              <td title="${escapeHtml(r.q1 || '')}">${cut(r.q1)}</td>
              <td title="${escapeHtml(r.q2 || '')}">${cut(r.q2)}</td>
              <td class="n">${r.q3 ?? '—'}</td>
              <td title="${escapeHtml(r.q4 || '')}">${cut(r.q4, 60)}</td>
              <td>${escapeHtml([r.q5, r.q6].filter(Boolean).join(' · ') || '—')}</td>
              <td class="n" style="color:var(--ink-3)">${ago(r.updated_at)}</td>
            </tr>`).join('');
    }
```

- [ ] **Step 3: 확인**

SQL 은 사용자가 Supabase 편집기에서 다시 실행해야 한다(Task 1 의 스키마와 함께 한 번에). 실행 뒤 `dashboards/beta_monitor.html` 을 로컬로 열어 관리자 계정으로 표에 새 열 2개와 일지 카드가 뜨는지 확인. 명단이 비어 있으면 "명단이 아직 없습니다" 가 그대로 뜨면 된다.

- [ ] **Step 4: 커밋**

```bash
git add sql/admin_analytics.sql dashboards/beta_monitor.html
git commit -m "$(cat <<'MSG'
Feat: 📊 베타 관제 — 맵 순서·일지 N/7 열 + 일지 카드(cf_beta_overview 확장)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
)"
```

---

### Task 6: 테스터 안내문 + 운영 체크리스트

**Files:**
- Create: `docs/BETA_TESTER_GUIDE.md`
- Modify: `docs/BETA_AB_TEST_PLAN.md` 2차 설계 §7 (완료 표시·명령)

- [ ] **Step 1: 안내문을 쓴다** (보내는 글 그대로. 실험 내용은 드러내지 않는다)

`docs/BETA_TESTER_GUIDE.md`:

```markdown
# 고요한 숲 베타 테스트 안내 (2026-09-09 ~ 09-15)

안녕하세요! 일주일 동안 「고요한 숲」을 같이 놀아 주실 분께 드리는 안내예요.

## 언제
- **9월 9일(수)부터 9월 15일(화)까지 7일**이에요.
- 매일 **최소 한 번** 접속해서 놀아 주세요. 더 하셔도 물론 좋아요.

## 어떻게 시작하나요
1. 9월 9일(수)에 https://calmforest.cloud 를 열어요. (폰·PC 아무거나. 크롬 권장)
2. **안내받은 구글 계정**으로 로그인해요. 다른 계정으로 하면 기록이 안 남아요.
3. 캐릭터를 고르고 튜토리얼을 따라가요. 그다음은 마음대로 놀면 돼요.
4. 콘텐츠가 며칠에 걸쳐 차례로 열려요. 잠긴 곳은 화면에 "N일차에 열려요"라고 떠요.

## 매일 저녁, 일지 3분
- 게임 왼쪽 위 ☰ 메뉴 → **📝 오늘 일지** 를 눌러요. (같은 구글 계정으로 열려요)
- 질문 4개예요. 짧게 써도 돼요. 마지막 날엔 2개가 더 있어요.
- 그날 안에 쓰면 제일 좋고, 놓쳤으면 다음 날 써도 돼요.

## 부탁
- 버그나 이상한 점은 일지 2번 칸에 적어 주세요. 스크린샷이 있으면 더 좋아요.
- 다른 테스터와 게임 이야기를 나누는 건 자유지만, 일지는 본인 느낌으로 써 주세요.

## 보상
- 7일 접속 + 일지 7회를 채우면 약속한 보상을 드려요. 마지막 날 설문 뒤에 이번 테스트가 무엇을 봤는지도 알려드릴게요.

궁금한 건 언제든 물어보세요. 고마워요 🌲
```

- [ ] **Step 2: 계획서 §7 에 실행 명령을 적는다**

`docs/BETA_AB_TEST_PLAN.md` 2차 설계 §7 "사용자" 목록 아래에:

```markdown
**실행 명령 (사용자)**
1. 배정 SQL 만들기: `node scripts/beta-roster.mjs a@x.com b@x.com … (10개)` → 출력을 복사
2. Supabase SQL 편집기에서 순서대로 실행: `sql/admin_analytics.sql` §베타 전체(컬럼·일지 테이블·RPC) → 1번 출력
3. 로컬 확인: `?forceVariant=beta_A&forceMapOrder=sea_first&betaDay=2` (localhost 전용)
4. 배포: `git checkout main && git merge dev && node scripts/build-web.mjs && npx wrangler deploy && git push`
5. 9/8 폰에서 `/beta/diary.html` 한 번 써 보고 그 행 삭제
```

- [ ] **Step 3: 커밋**

```bash
git add docs/BETA_TESTER_GUIDE.md docs/BETA_AB_TEST_PLAN.md
git commit -m "$(cat <<'MSG'
Docs: 📨 베타 테스터 안내문 + 2차 실행 명령

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
)"
```

---

## 전체 검증 (모든 태스크 후)

- [ ] `node --test 'tests/**/*.test.mjs'` — 55 pass
- [ ] `node --check js/game.js` · `node scripts/build-web.mjs` 후 `dist/beta/diary.html` 있음, `dist/tests` 없음
- [ ] 로컬: 일반 유저는 두 맵 다 열림 / `forceVariant=beta_A&forceMapOrder=sea_first&betaDay=2` 는 둘 다 잠김 / `betaDay=3` 바다터만 / `mist_first&betaDay=3` 안개숲만
- [ ] 폰 세로: 잠김 프롬프트가 컨텍스트 슬롯에 한 줄로, 일지 페이지 저장까지
- [ ] 관제에 맵 순서·일지 열, 일지 카드
- [ ] 안내문에 실험 내용(A/B·배너·날짜 차이)이 드러나지 않음

## Self-Review 기록

**스펙 대조** — 2차 설계 §1 배정 → Task 1 · §4 일지 → Task 4·5 · §5 맵 잠금 → Task 2·3 · §3 지침 → Task 6 · §6 데이터 원천 → 기존 계측 + Task 3 의 `map_locked`/`map_opened` · §7 준비 목록 → Task 1~6 전부.
**타입 일관성** — `map_order` 값 `'sea_first'|'mist_first'` 가 SQL CHECK · `assignRoster` · `BETA.mapGate` 키 · `state.mapOrder` · 관제 렌더에 같은 철자. `betaDay(createdAtIso, nowMs)` 시그니처가 tuning · game.js · diary.html · index.html 에 동일.
**남은 위험** — ① 테스터가 시작일 전에 가입해도 D1 은 시작일로 고정(`max`) ② 세이브 `hintsSeen` 부재 세이브는 Task 3 Step 1 의 `||= {}` ③ Supabase SQL 은 사용자 손 — 실행 전엔 일지 저장이 실패하며 페이지가 "명단에 없는 계정" 을 띄운다(정상 동작).
