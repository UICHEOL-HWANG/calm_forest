# 🦋 텃밭 방문객 도감 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 텃밭에 장식을 배치해 환경을 만들면 방문객 4종(🦋🐦🦔🐸)이 스스로 찾아와 도감에 등록되는, 행동이 아니라 설계로 채우는 신규 도감 카테고리를 만든다.

**Architecture:** 레시피를 짜지 않는다. 장식·작물·나무에 환경 태그(꽃·빛·은신·물기·그늘·먹이·공포)를 붙이고 지점 주변을 합산해 **하한선**으로 판정한다. 판정 규칙은 순수 모듈 `js/habitat.js`에 격리하고(THREE·DOM 의존 0), 좌표 수집과 렌더는 호출부가 맡는다. 매 프레임 계산하지 않고 dirty 플래그 + 1초 스로틀로 재평가한다.

**Tech Stack:** 바닐라 ES 모듈 · Three.js · `node --test` (의존성 추가 없음)

**Spec:** `docs/superpowers/specs/2026-09-17-habitat-design.md`

## Global Constraints

- **브랜치**: `feat/habitat` (2026-09-18, `d49f608`에서 분기)
- **테스트**: `npm test` — `node --test tests/*.test.mjs`. 모든 태스크 끝에 **전체**가 통과해야 한다
- **커밋 메시지**: `<type>: <description>`, 타입은 feat/fix/refactor/docs/test/chore/perf/ci. **Co-Authored-By 등 attribution 줄을 붙이지 않는다**(전역 비활성)
- **판정 반경**: 일반 태그 3 · `fear` 6 · 💧우물 `damp` 5 · 둘레 나무 `shade` 8 (월드 유닛). `CELL = 2`
- **좌표계**: `gameState.outdoor`와 `plots`는 **월드 좌표**. `perimeterTrees()`는 **밭 로컬**이라 `FARM`(0,0,84)을 더해야 한다
- **태그 소스는 세이브에 남는 것만.** `plot.watered`·`plot.wetUntil`·`tree.hp`·`plot.digAt`는 런타임 전용이라 **쓰면 안 된다**
- **새로 저장하는 것은 `dex.visitor` 하나뿐.** 환경 점수는 저장하지 않고 매번 파생한다
- **i18n**: 새 사용자 문구는 한국어 원문이 키. `js/i18n-en.js`에 같이 넣는다. `" · "` 글루와 `{0}` 슬롯 함정 주의
- **UI 문구 선검수**: Task 5·6의 사용자 노출 문구와 조형 사양은 구현 전에 사람 검수를 받는다

---

## 스펙에서 수정된 전제 (2026-09-18 구현 착수 시 발견)

스펙은 `shade` 소스를 "둘레 나무 +2 (반경 3)"로 적었으나, `perimeterTrees()`는 나무를 **울타리에서 정확히 5.0 바깥**에 세운다(`js/farm-stage.js:59`, `r = half + 5 + ((i*7)%6)*2.4`). 반경 3이면 밭 안 어디에도 그늘이 닿지 않아 🦔·🐸가 **영원히 불가능**하다. 세 단계 전부 최소거리 5.0으로 동일하다.

반경 후보 실측:

| 그늘 반경 | half=6 | half=9 | half=11 | 판정 |
|---|---|---|---|---|
| 6 | 4% | 2% | 1% | 너무 좁아 못 찾는다 |
| **8** | **28%** | **14%** | **10%** | **채택** — 가장자리 링만 |
| 10 | 58% | 35% | 24% | 1단계 절반이 그늘, 설계 무의미 |

**둘레 나무 `shade` 반경 8**로 확정한다. 스펙의 "그늘은 밭 가장자리에만 있다"는 의도가 그대로 유지된다(모든 단계에서 바깥 약 3 유닛 링). 남쪽 출입구·서쪽 측량소 마당은 나무를 건너뛰므로 그쪽엔 그늘이 없다 — 의도된 공간 texture다.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `js/habitat.js` (신규) | 환경 태그표·방문객표·판정. 순수 함수만. THREE·DOM·gameState 의존 0 |
| `tests/habitat.test.mjs` (신규) | 위 모듈 잠금 + "런타임 전용 필드가 태그 소스에 없다" 회귀 테스트 |
| `js/farm-visitors.js` (신규) | 스폰 후보 탐색·페이드·근접 등록. THREE 의존 |
| `js/visitor-art.js` (신규) | 방문객 4종 조형 |
| `js/game.js` (수정) | 도감 카테고리 배선 · 소스 어댑터 · dirty 플래그 · 배치 미터 연결 |
| `js/museum.js` (수정) | 2층 배정 · `DEX_NEVER` |
| `index.html` (수정) | 미터 DOM/CSS/`ui.setHabitatMeter` |
| `js/i18n-en.js` (수정) | 신규 문구 영어 |

`game.js`는 11,475줄이다. 판정 로직을 여기 두지 않는다 — 규칙은 모듈, 렌더는 기능 파일.

---

### Task 1: `js/habitat.js` 순수 모듈

**Files:**
- Create: `js/habitat.js`
- Test: `tests/habitat.test.mjs`

**Interfaces:**
- Consumes: 없음 (최초 태스크)
- Produces:
  - `TAGS: string[]` = `['nectar','light','shelter','damp','shade','food','fear']`
  - `TAG_LABEL: Record<string,string>` — 한국어 라벨 단일 출처
  - `ENV_TAG: Record<string, {tag: string, value: number, radius: number}>` — 장식 id → 태그
  - `CROP_FOOD: {value, radius}`, `TREE_SHADE: {value, radius}`, `RAIN_DAMP: number`
  - `VISITORS: Array<{id, ico, name, need, block?, when, hint}>`
  - `visitorOf(id) -> Visitor|null`
  - `envAt(src, x, z) -> Record<tag, number>` — 모든 TAGS 키가 채워진다
  - `whenOk(when, ctx) -> boolean`, `meets(v, env) -> boolean`
  - `matchVisitors(env, ctx) -> Visitor[]`
  - `progressOf(v, env) -> number`, `blockerOf(v, env) -> string|null`
  - `nearMiss(env, ctx, known) -> {visitor, blocker, progress}|null`
  - `NEAR_MISS_RATIO = 0.7`
  - `src` 형태: `{ decor: [{id,x,z}], mature: [{x,z}], trees: [{x,z}], rain: boolean }` — 좌표는 전부 **월드**
  - `ctx` 형태: `{ night: boolean, rain: boolean }`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/habitat.test.mjs` 생성:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { ENV_TAG, CROP_FOOD, TREE_SHADE, RAIN_DAMP, TAGS, VISITORS,
         envAt, whenOk, meets, matchVisitors, progressOf, blockerOf, nearMiss,
         NEAR_MISS_RATIO } from '../js/habitat.js';

const src = (o = {}) => ({ decor: [], mature: [], trees: [], rain: false, ...o });
const emptyEnv = () => Object.fromEntries(TAGS.map(t => [t, 0]));

test('반경 상수: 일반 3 · fear 6 · 우물 5 · 나무 8', () => {
  assert.equal(ENV_TAG.flowerbed.radius, 3);
  assert.equal(ENV_TAG.scarecrow.radius, 6);
  assert.equal(ENV_TAG.well.radius, 5);
  assert.equal(TREE_SHADE.radius, 8);
  assert.ok(ENV_TAG.scarecrow.radius > ENV_TAG.flowerbed.radius,
    'fear 가 더 넓어야 "허수아비를 옮긴다" 는 선택이 생긴다');
});

test('환경 반경 3 은 CELL=2 기준 정확히 3×3 밭칸을 덮는다', () => {
  const s = src({ decor: [{ id: 'flowerbed', x: 0, z: 0 }] });
  assert.equal(envAt(s, 2, 0).nectar, 2, '한 칸 옆(거리 2) 은 포함');
  assert.equal(envAt(s, 2, 2).nectar, 2, '대각선 한 칸(거리 2.83) 은 포함');
  assert.equal(envAt(s, 4, 0).nectar, 0, '두 칸 옆(거리 4) 은 제외');
});

test('fear 반경 6 — 중심에서 8 떨어진 허수아비는 밭 중앙에 영향이 없다', () => {
  const s = src({ decor: [{ id: 'scarecrow', x: 0, z: 8 }] });
  assert.equal(envAt(s, 0, 0).fear, 0, '밖으로 물리면 방문객을 안 쫓는다(밤손님 방어는 반경 9 라 유지)');
  assert.equal(envAt(s, 0, 4).fear, 3, '거리 4 는 여전히 쫓는다');
});

test('둘레 나무 그늘 반경 8 — 울타리에서 5 바깥 나무가 밭 가장자리에 닿는다', () => {
  const s = src({ trees: [{ x: 0, z: 11 }] });   // half=6 일 때 최근접 나무
  assert.equal(envAt(s, 0, 6).shade, 2, '울타리 선(6) 은 그늘');
  assert.equal(envAt(s, 0, 3).shade, 2, '안쪽 3 까지 닿는다');
  assert.equal(envAt(s, 0, 0).shade, 0, '중앙은 그늘이 없다 — 중앙은 농사, 가장자리는 생태');
});

test('damp: 우물 3(반경 5) + 비 오는 날 2(밭 전체)', () => {
  const well = src({ decor: [{ id: 'well', x: 0, z: 0 }] });
  assert.equal(envAt(well, 0, 5).damp, 3);
  assert.equal(envAt(well, 0, 6).damp, 0, '반경 5 밖');
  assert.equal(envAt(src({ rain: true }), 99, 99).damp, RAIN_DAMP, '비는 자리를 안 가린다');
  assert.equal(envAt({ ...well, rain: true }, 0, 0).damp, 3 + RAIN_DAMP);
});

test('food: 다 자란 작물만 +2 (반경 3)', () => {
  const s = src({ mature: [{ x: 0, z: 0 }, { x: 2, z: 0 }] });
  assert.equal(envAt(s, 0, 0).food, CROP_FOOD.value * 2, '두 칸이 겹쳐 합산된다');
});

test('태그 합산은 같은 태그끼리 누적된다', () => {
  const s = src({ decor: [{ id: 'flowerbed', x: 0, z: 0 }, { id: 'flowerbed', x: 2, z: 0 }] });
  assert.equal(envAt(s, 1, 0).nectar, 4);
});

test('모르는 장식 id 는 무시한다 — 태그 없는 장식이 더 많다', () => {
  assert.equal(envAt(src({ decor: [{ id: 'path', x: 0, z: 0 }] }), 0, 0).nectar, 0);
});

test('envAt 은 모든 태그 키를 0 으로 채워 돌려준다 — 호출부가 undefined 를 안 만나게', () => {
  const env = envAt(src(), 0, 0);
  for (const t of TAGS) assert.equal(env[t], 0, t);
});

test('VISITORS: 스펙의 4종 — id·시간대·조건이 정확히 일치한다', () => {
  assert.deepEqual(VISITORS.map(v => v.id), ['butterfly', 'sparrow', 'hedgehog', 'frog']);
  const by = Object.fromEntries(VISITORS.map(v => [v.id, v]));
  assert.deepEqual(by.butterfly.need, { nectar: 3 });
  assert.deepEqual(by.butterfly.block, { fear: 1 });
  assert.equal(by.butterfly.when, 'day');
  assert.deepEqual(by.sparrow.need, { food: 2 });
  assert.deepEqual(by.sparrow.block, { fear: 1 });
  assert.equal(by.sparrow.when, 'day');
  assert.deepEqual(by.hedgehog.need, { shelter: 3, shade: 2 });
  assert.equal(by.hedgehog.when, 'night');
  assert.deepEqual(by.frog.need, { damp: 4, shade: 1 });
  assert.equal(by.frog.when, 'rain');
  for (const v of VISITORS) {
    assert.ok(v.ico && v.name && v.hint, `${v.id}: 아이콘·한국어 이름·힌트가 있어야 도감에 뜬다`);
    for (const k of Object.keys(v.need)) assert.ok(TAGS.includes(k), `${v.id}: 모르는 태그 ${k}`);
    for (const k of Object.keys(v.block || {})) assert.ok(TAGS.includes(k), `${v.id}: 모르는 태그 ${k}`);
  }
});

test('whenOk: day 는 낮, night 는 밤, rain 은 비 오는 날', () => {
  assert.equal(whenOk('day',   { night: false, rain: false }), true);
  assert.equal(whenOk('day',   { night: true,  rain: false }), false);
  assert.equal(whenOk('night', { night: true,  rain: false }), true);
  assert.equal(whenOk('night', { night: false, rain: false }), false);
  assert.equal(whenOk('rain',  { night: false, rain: true  }), true);
  assert.equal(whenOk('rain',  { night: true,  rain: true  }), true, '개구리는 밤에도 운다');
  assert.equal(whenOk('rain',  { night: false, rain: false }), false);
});

test('meets: need 는 하한선, block 은 상한선', () => {
  const b = VISITORS.find(v => v.id === 'butterfly');
  assert.equal(meets(b, { ...emptyEnv(), nectar: 3 }), true, '딱 맞으면 통과');
  assert.equal(meets(b, { ...emptyEnv(), nectar: 9 }), true, '넘치면 통과 — 정확한 조합을 강요하지 않는다');
  assert.equal(meets(b, { ...emptyEnv(), nectar: 2 }), false);
  assert.equal(meets(b, { ...emptyEnv(), nectar: 9, fear: 3 }), false, '공포가 있으면 꽃이 많아도 안 온다');
});

test('matchVisitors: 조건과 시간대를 둘 다 만족한 종만', () => {
  const env = { ...emptyEnv(), nectar: 6, food: 4 };
  const day = matchVisitors(env, { night: false, rain: false }).map(v => v.id);
  assert.deepEqual(day.sort(), ['butterfly', 'sparrow']);
  assert.deepEqual(matchVisitors(env, { night: true, rain: false }).map(v => v.id), [],
    '밤에는 낮 손님이 안 온다');
});

test('blockerOf: block 위반을 need 부족보다 먼저 집는다 — 치우면 바로 오니까', () => {
  const b = VISITORS.find(v => v.id === 'butterfly');
  assert.equal(blockerOf(b, { ...emptyEnv(), nectar: 1, fear: 3 }), 'fear');
  assert.equal(blockerOf(b, { ...emptyEnv(), nectar: 1 }), 'nectar');
  assert.equal(blockerOf(b, { ...emptyEnv(), nectar: 3 }), null, '막는 게 없으면 null');
});

test('blockerOf: need 가 여럿이면 가장 모자란 하나만 집는다', () => {
  const h = VISITORS.find(v => v.id === 'hedgehog');   // shelter 3, shade 2
  assert.equal(blockerOf(h, { ...emptyEnv(), shelter: 3, shade: 0 }), 'shade');
  assert.equal(blockerOf(h, { ...emptyEnv(), shelter: 0, shade: 2 }), 'shelter');
});

test('progressOf: need 여러 개의 평균 충족률', () => {
  const h = VISITORS.find(v => v.id === 'hedgehog');
  assert.equal(progressOf(h, { ...emptyEnv(), shelter: 3, shade: 2 }), 1);
  assert.equal(progressOf(h, { ...emptyEnv(), shelter: 3, shade: 0 }), 0.5);
  assert.equal(progressOf(h, { ...emptyEnv(), shelter: 9, shade: 0 }), 0.5, '넘쳐도 1 로 잘린다');
});

test('nearMiss: 70% 이상 왔을 때만 신호를 준다', () => {
  const ctx = { night: false, rain: false };
  assert.equal(nearMiss({ ...emptyEnv(), nectar: 1 }, ctx, {}), null, '33% 는 아직 힌트를 주지 않는다');
  const close = nearMiss({ ...emptyEnv(), nectar: 9, fear: 3 }, ctx, {});
  assert.equal(close.visitor, 'butterfly');
  assert.equal(close.blocker, 'fear');
});

test('nearMiss: 이미 발견한 종은 신호를 주지 않는다 — 도감에 조건이 공개돼 있다', () => {
  const ctx = { night: false, rain: false };
  assert.equal(nearMiss({ ...emptyEnv(), nectar: 9, fear: 3 }, ctx, { butterfly: 1 }), null);
});

test('nearMiss: 조건을 이미 다 채운 종은 신호 대상이 아니다 — 그건 곧 온다', () => {
  const ctx = { night: false, rain: false };
  const nm = nearMiss({ ...emptyEnv(), nectar: 9 }, ctx, {});
  assert.notEqual(nm?.visitor, 'butterfly');
});

test('nearMiss: 시간대가 안 맞으면 신호도 없다 — 지금 할 수 있는 것만 말해준다', () => {
  assert.equal(nearMiss({ ...emptyEnv(), nectar: 9, fear: 3 }, { night: true, rain: false }, {}), null);
});

test('NEAR_MISS_RATIO 는 0.7', () => assert.equal(NEAR_MISS_RATIO, 0.7));

// ⚠️ 회귀 잠금 — 이 테스트가 이 기능의 가장 큰 함정을 막는다
test('런타임 전용 필드를 태그 소스로 쓰지 않는다', () => {
  const code = readFileSync(new URL('../js/habitat.js', import.meta.url), 'utf8');
  for (const bad of ['watered', 'wetUntil', 'elapsedTime', 'digAt']) {
    assert.ok(!code.includes(bad),
      `habitat.js 가 ${bad} 를 참조한다 — 세션이 끊기면 사라지는 값이라 태그 소스가 될 수 없다. ` +
      `WET_TIME=9 라 물 준 흙은 9초만 촉촉하고 스폰 지연이 6~14초다(🐸 가 영원히 안 온다).`);
  }
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

```bash
npm test 2>&1 | head -20
```

Expected: FAIL — `Cannot find module '../js/habitat.js'`

- [ ] **Step 3: 모듈을 구현한다**

`js/habitat.js` 생성:

```js
// =============================================================
//  calm forest · 🦋 텃밭 방문객 서식 규칙 (순수 함수 — DOM/Three 의존 없음)
//  ------------------------------------------------------------
//  스펙: docs/superpowers/specs/2026-09-17-habitat-design.md
//  ▶ 레시피를 짜지 않는다. 태그 점수를 합산해 **하한선**으로 판정한다.
//    "정확히 이 배치" 를 요구하면 체크리스트가 된다 — 대충 꽃밭이면 나비가 온다.
//  ▶ ⚠️ 태그 소스는 **세이브에 남는 것만** 쓴다.
//    plot.watered / wetUntil 은 런타임 전용이고 흙은 9초만 촉촉하다(game.js 의 WET_TIME).
//    스폰 지연이 6~14초라 이걸 damp 소스로 쓰면 🐸 는 뜨기도 전에 조건이 사라진다.
//    (tests/habitat.test.mjs 가 이 파일에 해당 이름이 등장하는지로 잠근다)
//  ▶ 좌표는 전부 **월드**. perimeterTrees() 는 밭 로컬이라 호출부가 FARM 을 더해서 넘긴다.
//  ▶ 테스트: npm test (tests/habitat.test.mjs)
// =============================================================

export const TAGS = ['nectar', 'light', 'shelter', 'damp', 'shade', 'food', 'fear'];

/** 미터·힌트에 쓰는 한국어 라벨 — 사용자 노출 문구의 단일 출처(index.html 이 가져다 쓴다) */
export const TAG_LABEL = {
  nectar:  '🌷 꽃',
  light:   '🏮 빛',
  shelter: '🧱 은신처',
  damp:    '💧 물기',
  shade:   '🌑 그늘',
  food:    '🌾 먹이',
  fear:    '🎃 공포',
};

/**
 * 야외 장식 id → 환경 태그. 값과 반경은 소스마다 다르다.
 *  · 일반 3 — CELL=2 이므로 정확히 3×3 밭칸
 *  · 🎃 fear 6 — 환경 반경의 2배. 밤손님 방어(반경 9, game.js 의 computeNightDefense)와 분리했다.
 *    허수아비를 울타리 밖으로 물리면(중심에서 8) 방어는 유지되고 방문객은 안 쫓는다.
 *    이 비대칭이 "희생 없는 배치 문제" 를 만든다 — 값을 맞추면 설계가 사라진다.
 *  · 💧 well 5 — 시설 자체의 효과 반경(farm-building.js 의 well.radius)과 같은 숫자를 쓴다
 */
export const ENV_TAG = {
  flowerbed:  { tag: 'nectar',  value: 2, radius: 3 },
  postlamp:   { tag: 'light',   value: 2, radius: 3 },
  brazier:    { tag: 'light',   value: 2, radius: 3 },
  spiritlamp: { tag: 'light',   value: 2, radius: 3 },
  stonewall:  { tag: 'shelter', value: 2, radius: 3 },
  fence:      { tag: 'shelter', value: 1, radius: 3 },
  well:       { tag: 'damp',    value: 3, radius: 5 },
  scarecrow:  { tag: 'fear',    value: 3, radius: 6 },
};

/** 장식이 아닌 소스 */
export const CROP_FOOD  = { value: 2, radius: 3 };   // 다 자란 밭칸(state === 'mature')
export const TREE_SHADE = { value: 2, radius: 8 };   // 울타리 둘레 나무
export const RAIN_DAMP  = 2;                          // 🌧️ 비 오는 날 — 밭 전체

// ⚠️ TREE_SHADE.radius 를 줄이면 그늘이 밭에 닿지 않는다.
//    perimeterTrees() 는 나무를 울타리에서 **정확히 5.0 바깥**에 세운다(farm-stage.js).
//    세 단계 전부 같은 간격이다. 실측:
//      반경 6  → 밭의 1~4% 만 그늘  (🦔·🐸 를 사실상 못 찾는다)
//      반경 8  → 10~28%, 바깥 약 3 유닛 링  ← 채택
//      반경 10 → 24~58%, 1단계 절반이 그늘 ("가장자리" 라는 설계가 사라진다)

/**
 * 방문객 4종. need 는 하한선, block 은 상한선(그 값 **이상**이면 안 온다).
 * when: 'day' | 'night' | 'rain'
 */
export const VISITORS = [
  { id: 'butterfly', ico: '🦋', name: '호랑나비', need: { nectar: 3 },            block: { fear: 1 }, when: 'day',   hint: '꽃이 많은 곳' },
  { id: 'sparrow',   ico: '🐦', name: '참새',     need: { food: 2 },              block: { fear: 1 }, when: 'day',   hint: '먹을 게 익은 곳' },
  { id: 'hedgehog',  ico: '🦔', name: '고슴도치', need: { shelter: 3, shade: 2 },                     when: 'night', hint: '숨을 데가 있는 어두운 곳' },
  { id: 'frog',      ico: '🐸', name: '청개구리', need: { damp: 4, shade: 1 },                        when: 'rain',  hint: '축축하고 그늘진 곳' },
];

export function visitorOf(id) { return VISITORS.find(v => v.id === id) || null; }

const dist = (ax, az, bx, bz) => Math.hypot(ax - bx, az - bz);

/**
 * 한 지점의 환경 점수.
 * @param {{decor?:{id,x,z}[], mature?:{x,z}[], trees?:{x,z}[], rain?:boolean}} src 월드 좌표
 * @returns {Record<string, number>} 모든 TAGS 키가 채워진다
 */
export function envAt(src, x, z) {
  const env = {};
  for (const t of TAGS) env[t] = 0;
  for (const d of src.decor || []) {
    const def = ENV_TAG[d.id];
    if (def && dist(d.x, d.z, x, z) <= def.radius) env[def.tag] += def.value;
  }
  for (const c of src.mature || []) {
    if (dist(c.x, c.z, x, z) <= CROP_FOOD.radius) env.food += CROP_FOOD.value;
  }
  for (const t of src.trees || []) {
    if (dist(t.x, t.z, x, z) <= TREE_SHADE.radius) env.shade += TREE_SHADE.value;
  }
  if (src.rain) env.damp += RAIN_DAMP;
  return env;
}

/** @param {{night:boolean, rain:boolean}} ctx */
export function whenOk(when, ctx) {
  if (when === 'day')   return !ctx.night;
  if (when === 'night') return !!ctx.night;
  if (when === 'rain')  return !!ctx.rain;   // 개구리는 밤에도 운다 — 시간대를 더 걸지 않는다
  return true;
}

export function meets(v, env) {
  for (const [k, n] of Object.entries(v.need)) if ((env[k] || 0) < n) return false;
  for (const [k, n] of Object.entries(v.block || {})) if ((env[k] || 0) >= n) return false;
  return true;
}

export function matchVisitors(env, ctx) {
  return VISITORS.filter(v => whenOk(v.when, ctx) && meets(v, env));
}

/** need 충족률 0~1. block 은 안 본다 — "얼마나 왔나" 와 "왜 막혔나" 는 다른 질문이다. */
export function progressOf(v, env) {
  const parts = Object.entries(v.need).map(([k, n]) => Math.min(1, (env[k] || 0) / n));
  return parts.reduce((a, b) => a + b, 0) / parts.length;
}

/**
 * 막는 요인 **하나**. 정답을 알려주지 않고 방향만 알려준다.
 * block 위반을 먼저 집는다 — 치우면 바로 오기 때문에 가장 행동 가능한 힌트다.
 */
export function blockerOf(v, env) {
  for (const [k, n] of Object.entries(v.block || {})) if ((env[k] || 0) >= n) return k;
  let worst = null, worstRatio = Infinity;
  for (const [k, n] of Object.entries(v.need)) {
    const r = (env[k] || 0) / n;
    if (r < 1 && r < worstRatio) { worstRatio = r; worst = k; }
  }
  return worst;
}

export const NEAR_MISS_RATIO = 0.7;

/**
 * "거의 다 왔다" 신호 — 침묵 실패를 막는 장치.
 * 목표치를 안 보여주는 대신, 70% 이상 채운 **미발견** 종의 막는 요인 하나를 집어 준다.
 * @param {Record<string, any>} known gameState.dex.visitor
 */
export function nearMiss(env, ctx, known = {}) {
  let best = null;
  for (const v of VISITORS) {
    if (known[v.id]) continue;            // 발견한 종은 도감에 조건이 다 공개돼 있다
    if (!whenOk(v.when, ctx)) continue;   // 지금 할 수 있는 것만 말해준다
    if (meets(v, env)) continue;          // 곧 온다 — 힌트가 필요 없다
    const progress = progressOf(v, env);
    if (progress < NEAR_MISS_RATIO) continue;
    if (!best || progress > best.progress) best = { visitor: v.id, blocker: blockerOf(v, env), progress };
  }
  return best;
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

```bash
npm test 2>&1 | tail -20
```

Expected: 전체 PASS.

- [ ] **Step 5: 커밋**

```bash
git add js/habitat.js tests/habitat.test.mjs
git commit -m "feat: 🦋 환경 점수 판정 모듈 — 태그표·방문객 4종·근접 신호"
```

---

### Task 2: 도감 카테고리 `visitor` 배선

**Files:**
- Modify: `js/game.js:1087` (기본 객체), `js/game.js:1186` (DEX 끝), `js/game.js:2387` (applySave), `js/game.js:4444` (라벨), import 영역
- Modify: `js/museum.js:19` (2층), `js/museum.js:73` (DEX_NEVER)
- Test: `tests/museum.test.mjs`

**Interfaces:**
- Consumes: Task 1의 `VISITORS`
- Produces: `gameState.dex.visitor` 저장 슬롯 · `DEX.visitor` 4종 · `DEX_TOTAL` 33 → 37

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/museum.test.mjs` 상단 import에 `pickMissingDex`와 `VISITORS`가 없으면 더한다:

```js
import { MUSEUM_FLOORS, floorEntries, floorProgress, openFloors, nextFloorNeed, pickMissingDex } from '../js/museum.js';
import { VISITORS } from '../js/habitat.js';
```

파일 끝에 추가:

```js
test('⚠️ 큐레이터는 방문객을 집지 않는다 — 장식·날씨가 선행이라 오늘 안에 못 깬다', () => {
  // 🐸 는 비 오는 날(약 20%)에만 온다. weather 카테고리와 정확히 같은 문제다.
  // DEX_NEVER 에서 빠지면 그날 dex_one 의뢰가 통째로 막힌다.
  const DEX = { visitor: VISITORS.map(v => ({ id: v.id, name: v.name, ico: v.ico })) };
  for (let seed = 0; seed < 50; seed++) {
    assert.equal(pickMissingDex({}, DEX, seed, {}), null,
      'visitor 만 있는 풀에서는 아무것도 집으면 안 된다');
  }
});

test('visitor 카테고리가 어느 층엔가 전시된다', () => {
  const placed = new Set(MUSEUM_FLOORS.flatMap(f => f.cats));
  assert.ok(placed.has('visitor'), '빠지면 방문객 4종이 영영 전시되지 않는다');
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

```bash
npm test 2>&1 | grep -B2 -A6 "큐레이터는 방문객"
```

Expected: FAIL — `pickMissingDex`가 visitor를 집어 `null`이 아닌 값을 돌려준다.

- [ ] **Step 3: `js/museum.js` 를 고친다**

19행 2층 `cats`에 `'visitor'`를 더한다:

```js
  { id: 2, name: '2층',   cats: ['forage', 'bug', 'dig', 'track', 'visitor'], need: 9 },
```

73행 주석과 상수:

```js
//   · weather — "그 날씨인 날 접속" 이라 오늘 안에 맞출 수가 없다
//   · visitor — 🦋텃밭 방문객. 장식 구매·배치가 선행이고 🐸 는 비 오는 날(약 20%)에만 온다.
//     weather 와 정확히 같은 문제라 같이 막는다.
const DEX_NEVER = ['weather', 'visitor'];
```

- [ ] **Step 4: `js/game.js` 를 고친다 — 다섯 곳**

**(a)** import 영역(기존 `from './museum.js'` 근처)에:

```js
import { VISITORS, ENV_TAG, TAG_LABEL, envAt, matchVisitors, nearMiss, visitorOf } from './habitat.js';   // 🦋 텃밭 방문객 서식 규칙
```

**(b)** 1087행 기본 `dex` 객체 끝에 `visitor: {}`:

```js
  dex: { fish: {}, crop: {}, ore: {}, cook: {}, npc: {}, weather: {}, bug: {}, forage: {}, track: {}, river: {}, spirit: {}, dig: {}, visitor: {} }, // 📖 도감 — 카테고리별 { 종id: 첫발견시각(ms) }
```

**(c)** 2387행 `applySave` 복원도 **똑같이**. ⚠️ 한 곳만 고치면 세이브가 있는 유저에게 `gameState.dex.visitor`가 `undefined`가 되고 `dexDiscover`의 첫 줄(`if (!gameState.dex[cat] ...) return;`)에서 **조용히 반환해 등록이 안 된다**:

```js
    gameState.dex = { fish: {}, crop: {}, ore: {}, cook: {}, npc: {}, weather: {}, bug: {}, forage: {}, track: {}, river: {}, spirit: {}, dig: {}, visitor: {}, ...saved.dex }; // 📖 도감 복원
```

**(d)** `DEX` 객체 끝(`weather` 배열 다음, 닫는 `};` 앞)에 방문객. **표를 두 번 쓰지 않는다** — `habitat.js`가 단일 출처다:

```js
  // 🦋 방문객 — 텃밭 환경을 만들면 스스로 찾아온다. 표의 단일 출처는 js/habitat.js
  visitor: VISITORS.map(v => ({ id: v.id, name: v.name, ico: v.ico })),
};
```

**(e)** 4444행 `DEX_CAT_LABEL` 끝:

```js
  weather: '🌦️ 날씨', npc: '🧑 주민', cook: '🍳 요리', visitor: '🦋 방문객' };
```

그리고 `DEX_TOTAL` 줄의 주석 `(현재 33종)`을 `(현재 37종)`으로 고친다.

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

```bash
npm test 2>&1 | tail -20
grep -n "현재 37종" js/game.js
```

Expected: 전체 PASS, 주석 갱신 확인.

- [ ] **Step 6: 도감 모달에서 미보유 표시를 확인한다**

`.claude/launch.json`의 서버 이름을 확인해 preview를 띄우고 ☰ → 📖 도감을 연다.

```bash
cat .claude/launch.json
```

`🦋 방문객` 구역에 4칸이 보이는지, 미보유 항목이 이름까지 드러나는지 본다.
- 이미 `???` 같은 가림 처리가 있으면 그대로 둔다
- 이름이 그대로 보이면 **`visitor` 카테고리만** 미보유 시 이름·힌트를 가리는 처리를 넣는다(스펙: 실루엣만, 조건 비공개)

- [ ] **Step 7: 커밋**

```bash
git add js/game.js js/museum.js tests/museum.test.mjs
git commit -m "feat: 🦋 도감 visitor 카테고리 배선 — 박물관 2층·큐레이터 제외"
```

---

### Task 3: 환경 소스 어댑터 + dirty 플래그

**Files:**
- Modify: `js/game.js` — `habitatSources()` 외 4개 함수 신규, dirty 플래그 호출 4곳

**Interfaces:**
- Consumes: Task 1(`envAt`, `ENV_TAG`)
- Produces:
  - `habitatSources() -> {decor, mature, trees, rain}` — 월드 좌표
  - `habitatCtx() -> {night, rain}`
  - `habitatEnvAt(x, z) -> env` — 스로틀 캐시를 쓰는 편의 함수
  - `habitatCells() -> {x,z}[]` — 밭 안 후보 지점(월드)
  - `markHabitatDirty()`

- [ ] **Step 1: 어댑터를 구현한다**

`js/game.js`에서 `farmBuildingRecs`(632행) 근처에 추가:

```js
// ── 🦋 텃밭 방문객 — 환경 점수 입력 ───────────────────────────
//   habitat.js 는 순수 모듈이라 좌표·상태를 여기서 모아 넘긴다.
//   ⚠️ gameState.outdoor 와 plots 는 월드 좌표, perimeterTrees() 는 밭 로컬이다.
//   ⚠️ 여기에 plot.watered / wetUntil 을 넣지 않는다 — 9초짜리 런타임 값이다(habitat.js 주석).
let habitatSrc = null, habitatSrcAt = -1e9;   // 1초 스로틀 캐시
let habitatDirty = true;

function markHabitatDirty() { habitatDirty = true; }

/** 밭 주변만 본다 — 마을 장식은 80 이상 떨어져 있어 반경 8 안에 들어올 수 없다 */
function habitatSources() {
  const H = farmHalf(), reach = H + 12;
  const nearFarm = (x, z) => Math.hypot(x - FARM.x, z - FARM.z) <= reach;
  return {
    decor: gameState.outdoor.filter(o => ENV_TAG[o.id] && nearFarm(o.x, o.z)),
    mature: plots.filter(p => p.state === 'mature').map(p => ({ x: p.x, z: p.z })),
    trees: perimeterTrees(H).map(t => ({ x: FARM.x + t.x, z: FARM.z + t.z })),
    rain: RAIN_DAY,
  };
}

function habitatCtx() { return { night: isNight(), rain: RAIN_DAY }; }

/** 스로틀된 소스로 한 지점 판정. 매 프레임 불러도 초당 1회만 다시 모은다. */
function habitatEnvAt(x, z) {
  const now = clock.elapsedTime;
  if (habitatDirty || !habitatSrc || now - habitatSrcAt >= 1) {
    habitatSrc = habitatSources(); habitatSrcAt = now; habitatDirty = false;
  }
  return envAt(habitatSrc, x, z);
}

/** 밭 안을 CELL 간격으로 훑은 후보 지점 — 3단계(half 11)에서 최대 약 121칸 */
function habitatCells() {
  const H = farmHalf(), out = [];
  for (let x = -H; x <= H; x += FARM_CELL) {
    for (let z = -H; z <= H; z += FARM_CELL) {
      if (Math.hypot(x, z) > H) continue;
      out.push({ x: FARM.x + x, z: FARM.z + z });
    }
  }
  return out;
}
```

- [ ] **Step 2: dirty 플래그를 걸 지점을 찾는다**

```bash
grep -n "gameState.outdoor.push(rec)" js/game.js
grep -n "outdoor.splice\|outdoor.indexOf" js/game.js
grep -n "state = 'mature'\|state: 'mature'" js/game.js
grep -n "state = 'empty'" js/game.js
```

- [ ] **Step 3: dirty 플래그를 건다**

각 지점 바로 뒤에 `markHabitatDirty();` 한 줄:

1. `placeOutdoor()`의 `gameState.outdoor.push(rec)` 직후 — 장식 배치·이동 완료
2. 장식을 들어 올려 `gameState.outdoor`에서 빼는 직후 — 옮기는 중에도 즉시 반영
3. 작물이 `'mature'`가 되는 지점 — `food` 발생
4. 수확으로 `state = 'empty'`가 되는 지점 — `food` 소멸

날짜 전환은 `RAIN_DAY`가 접속 시 정해지는 상수라 따로 걸지 않는다(재접속하면 소스가 새로 모인다).

- [ ] **Step 4: 디버그 훅을 노출한다**

`window.__space` 정의부(2262행) 근처에:

```js
    window.__habitat = { envAt: habitatEnvAt, ctx: habitatCtx, cells: habitatCells, src: habitatSources };   // 🦋 검수용
```

- [ ] **Step 5: 브라우저에서 확인한다**

```bash
cat .claude/launch.json
```

preview를 띄우고 텃밭에 들어간 뒤 콘솔에서:

```js
__habitat.ctx()                                          // { night: false, rain: false }
__habitat.src().trees.length                             // 20 내외
__habitat.envAt(player.position.x, player.position.z)     // 현재 위치 환경
```

기대: 🌷꽃밭 2개를 나란히 놓고 그 사이에서 `nectar: 4`. 밭 가장자리에서 `shade: 2` 이상, 중앙에서 `shade: 0`.

- [ ] **Step 6: 커밋**

```bash
git add js/game.js
git commit -m "feat: 🦋 환경 소스 어댑터 — dirty 플래그 + 1초 스로틀"
```

---

### Task 4: 스폰·등록·페이드 (`js/farm-visitors.js`)

**Files:**
- Create: `js/farm-visitors.js`
- Modify: `js/game.js` — `enterFarm`/`exitFarm`/프레임 루프, 임시 조형

**Interfaces:**
- Consumes: Task 1(`matchVisitors`, `visitorOf`), Task 3(`habitatEnvAt`, `habitatCells`, `habitatCtx`)
- Produces:
  - `createVisitors(deps) -> { update(dt), clear(), alive }`
  - `deps` = `{ group, makeMesh, cells, envAt, matchVisitors, ctx, playerPos, onSpawn, onDiscover }`
  - 상수 `MAX_ALIVE = 2`, `SPAWN_DELAY_MIN = 6`, `SPAWN_DELAY_MAX = 14`, `STAY = 25`, `CATCH_R = 2.5`, `FADE = 1.2`

- [ ] **Step 1: 모듈을 만든다**

`js/farm-visitors.js`:

```js
// =============================================================
//  calm forest · 🦋 텃밭 방문객 스폰·등록 (렌더 — THREE 객체를 다룬다)
//  ------------------------------------------------------------
//  판정 규칙은 js/habitat.js 가 단일 출처다. 여기는 "언제 뜨고 언제 사라지나" 만 맡는다.
//  ▶ 텃밭 체류 중에만 뜬다. 오프라인 방문·흔적은 비범위(서버 판정이 필요해진다).
//  ▶ 등록은 1회, 방문은 반복 — 다 모은 뒤에도 정원 설계가 의미를 잃지 않게.
//  ▶ 메시는 farmGroup 자식이라 텃밭을 나가면 같이 숨는다.
//  ▶ 의존은 전부 주입받는다(THREE 를 import 하지 않는다) — 호출부가 메시를 만들어 준다.
// =============================================================

export const MAX_ALIVE = 2;
export const SPAWN_DELAY_MIN = 6;    // 조건 충족 즉시 뜨면 싸구려가 된다
export const SPAWN_DELAY_MAX = 14;
export const STAY = 25;              // 등록 여부와 무관하게 이만큼 머물고 떠난다
export const CATCH_R = 2.5;          // 다가가면 등록되는 거리
export const FADE = 1.2;

function setOpacity(obj, v) {
  obj.traverse?.(o => {
    if (!o.material) return;
    o.material.transparent = true;
    o.material.opacity = v;
  });
}

/**
 * @param {object} deps
 *   group          THREE.Group            farmGroup — 여기에 메시를 붙인다
 *   makeMesh       (visitorId) => Object3D
 *   cells          () => {x,z}[]          후보 지점(월드)
 *   envAt          (x,z) => env
 *   matchVisitors  (env, ctx) => Visitor[]
 *   ctx            () => {night, rain}
 *   playerPos      () => {x,z}
 *   onSpawn        (id) => void
 *   onDiscover     (id) => void           이미 등록된 종이면 호출부가 재방문 처리
 */
export function createVisitors(deps) {
  let alive = [];
  let nextTry = 0;

  function clear() {
    for (const a of alive) deps.group?.remove(a.mesh);
    alive = [];
    nextTry = 0;
  }

  /** 지금 뜰 수 있는 (종, 자리) 후보 중 하나를 고른다. 이미 떠 있는 종은 제외. */
  function pickSpot() {
    const ctx = deps.ctx();
    const taken = new Set(alive.map(a => a.id));
    const spots = [];
    for (const c of deps.cells()) {
      for (const v of deps.matchVisitors(deps.envAt(c.x, c.z), ctx)) {
        if (!taken.has(v.id)) spots.push({ id: v.id, c });
      }
    }
    return spots.length ? spots[Math.floor(Math.random() * spots.length)] : null;
  }

  function update(dt) {
    // 살아 있는 것들 — 수명·페이드·근접 등록
    for (const a of alive) {
      a.life += dt;
      const fadeIn = Math.min(1, a.life / FADE);
      const fadeOut = Math.min(1, Math.max(0, (STAY - a.life) / FADE));
      setOpacity(a.mesh, Math.min(fadeIn, fadeOut));
      if (!a.caught) {
        const p = deps.playerPos();
        if (Math.hypot(a.mesh.position.x - p.x, a.mesh.position.z - p.z) <= CATCH_R) {
          a.caught = true;
          deps.onDiscover(a.id);
        }
      }
    }
    for (const a of alive) if (a.life >= STAY) deps.group?.remove(a.mesh);
    alive = alive.filter(a => a.life < STAY);

    // 새로 띄우기
    nextTry -= dt;
    if (nextTry > 0 || alive.length >= MAX_ALIVE) return;
    nextTry = SPAWN_DELAY_MIN + Math.random() * (SPAWN_DELAY_MAX - SPAWN_DELAY_MIN);
    const pick = pickSpot();
    if (!pick) return;
    const mesh = deps.makeMesh(pick.id);
    mesh.position.x = pick.c.x; mesh.position.z = pick.c.z;
    setOpacity(mesh, 0);
    deps.group?.add(mesh);
    alive.push({ id: pick.id, mesh, life: 0, caught: false });
    deps.onSpawn(pick.id);
  }

  return { update, clear, get alive() { return alive.map(a => a.id); } };
}
```

- [ ] **Step 2: 임시 조형을 붙인다**

Task 6에서 제대로 만들 때까지 색만 다른 구체로 띄운다. `js/game.js`에:

```js
// 🦋 임시 조형 — Task 6 에서 js/visitor-art.js 로 교체한다
const VISITOR_TMP_COLOR = { butterfly: 0xe8a0c8, sparrow: 0xb09070, hedgehog: 0x8a6a4a, frog: 0xa8d586 };
function makeVisitorMesh(id) {
  const m = new THREE.Mesh(new THREE.SphereGeometry(0.35, 12, 10),
    new THREE.MeshStandardMaterial({ color: VISITOR_TMP_COLOR[id] || 0xffffff }));
  m.position.y = 0.45;
  return m;
}
```

⚠️ `mesh.position.y`는 `createVisitors`가 건드리지 않는다(x·z만 설정) — 조형이 자기 높이를 정한다.

- [ ] **Step 3: 게임 루프에 연결한다**

`js/game.js` 상단 import에:

```js
import { createVisitors } from './farm-visitors.js';   // 🦋 방문객 스폰·등록
```

선언부(`let farmGroup, mineGroup;` 근처):

```js
let visitors = null;   // 🦋 텃밭 체류 중에만 살아 있다
```

`enterFarm()`(9824행 `atFarm = true;` 뒤) 안에:

```js
  visitors = createVisitors({
    group: farmGroup,
    makeMesh: makeVisitorMesh,
    cells: habitatCells,
    envAt: habitatEnvAt,
    matchVisitors,
    ctx: habitatCtx,
    playerPos: () => player.position,
    onSpawn: (id) => trackEvent('visitor_spawn', { visitor: id, farm_stage: gameState.farm.stage }),   // [GA4] 퍼널 3단
    onDiscover: (id) => {
      if (!gameState.dex.visitor?.[id]) {
        dexDiscover('visitor', id);   // 📖 등록 + 토스트 + 박물관 게이트 + 퀘스트 + GA4 를 한 번에
      } else {
        const v = visitorOf(id);      // 재방문 — "정원이 살아있다" 는 신호. 보상은 없다.
        ui.toast?.(`${v.ico} ${v.name}가 다시 찾아왔어요`, 1800);
      }
    },
  });
```

`exitFarm()`(9831행 `atFarm = false;` 근처):

```js
  visitors?.clear(); visitors = null;
```

프레임 루프에서 (`dt`를 쓰는 기존 갱신들 옆):

```js
  if (atFarm && visitors) visitors.update(dt);
```

⚠️ `farmGroup`이 `exitFarm`에서 `scene.remove` + `null`이 되므로(9765행), `clear()`를 **그 전에** 불러야 한다.

- [ ] **Step 4: 브라우저에서 확인한다**

preview를 띄우고 텃밭에서:

1. 🌷꽃밭 2개를 나란히 놓는다
2. 6~14초 기다린다 → 분홍 구체가 페이드인
3. 다가간다 → `📖 도감 등록! 🦋 호랑나비 (34/37)` 토스트
4. 25초 뒤 사라진다
5. 다시 기다리면 또 온다 → `🦋 호랑나비가 다시 찾아왔어요`
6. 🎃허수아비를 꽃밭에서 거리 6 이내에 놓는다 → 더는 안 온다
7. 허수아비를 거리 8로 물린다 → 다시 온다
8. 텃밭을 나갔다 들어온다 → 에러 없이 다시 동작한다

`read_console_messages`로 에러가 없는지 확인한다.

- [ ] **Step 5: 커밋**

```bash
git add js/farm-visitors.js js/game.js
git commit -m "feat: 🦋 방문객 스폰·근접 등록·재방문 + GA4 출현 이벤트"
```

---

### Task 5: 배치 환경 미터 + 근접 신호

**Files:**
- Modify: `index.html` — 미터 DOM/CSS/`ui.setHabitatMeter`
- Modify: `js/game.js:7858` `updateDecorGhost()`, `removeDecorGhost()`, `buildDecorGhost()`

**Interfaces:**
- Consumes: Task 1(`TAG_LABEL`, `nearMiss`), Task 3(`habitatEnvAt`, `habitatCtx`)
- Produces: `ui.setHabitatMeter(env, near)` / `ui.setHabitatMeter(null)` · `ui.setHabitatLabels(TAG_LABEL, BLOCK_LINE)`

- [ ] **Step 1: ⚠️ 문구 검수를 먼저 받는다**

구현 전에 사람에게 아래 후보를 보여주고 확정한다(모바일 375 폭 실측 포함). 확정 전에는 Step 2로 넘어가지 않는다.

```
제목:       🌿 이 자리
빈 상태:    아직 허전해요
태그 라벨:  🌷 꽃 · 🏮 빛 · 🧱 은신처 · 💧 물기 · 🌑 그늘 · 🌾 먹이 · 🎃 공포
근접 신호:  ❓ 무언가 맴돌다 갔어요 — {0}
  fear    무서워하는 것 같아요
  nectar  꽃이 더 있어야 할 것 같아요
  food    먹을 게 없나 봐요
  shelter 숨을 데가 없나 봐요
  shade   그늘이 부족한가 봐요
  damp    너무 말랐나 봐요
```

- [ ] **Step 2: DOM 과 CSS 를 넣는다**

`index.html` HUD 영역에 (모바일 3단 레이아웃 — 컨텍스트 슬롯·프롬프트 줄을 침범하지 않는 자리):

```html
<div id="habitat-meter" hidden>
  <div class="hm-title">🌿 이 자리</div>
  <div class="hm-rows"></div>
  <div class="hm-near" hidden></div>
</div>
```

```css
#habitat-meter { position:absolute; left:12px; bottom:96px; z-index:6;
  background:rgba(20,28,22,.82); color:#eaf3ea; border-radius:10px; padding:8px 10px;
  font-size:13px; line-height:1.5; pointer-events:none; max-width:220px; }
#habitat-meter .hm-title { font-weight:700; margin-bottom:4px; }
#habitat-meter .hm-row { display:flex; justify-content:space-between; gap:8px; }
#habitat-meter .hm-dots { letter-spacing:1px; }
#habitat-meter .hm-near { margin-top:6px; padding-top:6px;
  border-top:1px solid rgba(255,255,255,.18); font-size:12px; opacity:.9; }
```

- [ ] **Step 3: `ui.setHabitatMeter` 를 만든다**

`index.html`의 ui 객체에. **문구를 여기에 다시 적지 않는다** — game.js가 habitat.js의 `TAG_LABEL`을 한 번 넘겨준다:

```js
  _hmLabel: {}, _hmBlock: {},
  setHabitatLabels(label, block) { this._hmLabel = label; this._hmBlock = block; },
  setHabitatMeter(env, near) {
    const el = document.getElementById('habitat-meter');
    if (!env) { el.hidden = true; return; }
    el.hidden = false;
    const rows = Object.entries(env)
      .filter(([, n]) => n > 0)                     // 0 인 태그는 줄을 먹지 않는다
      .map(([k, n]) => `<div class="hm-row"><span>${this._hmLabel[k] || k}</span>` +
                       `<span class="hm-dots">${'●'.repeat(Math.min(6, n))}</span></div>`)
      .join('');
    el.querySelector('.hm-rows').innerHTML = rows || '<div class="hm-row"><span>아직 허전해요</span></div>';
    const nb = el.querySelector('.hm-near');
    nb.hidden = !near;
    if (near) nb.textContent = `❓ 무언가 맴돌다 갔어요 — ${this._hmBlock[near.blocker] || ''}`;
  },
```

- [ ] **Step 4: `js/game.js` 에 연결한다**

Step 1에서 확정한 문구로, 상수를 게임 쪽에 한 번 둔다(ui 초기화 근처에서 넘긴다):

```js
// 🦋 막는 요인별 안내 — 정답이 아니라 방향만 말한다
const HABITAT_BLOCK_LINE = {
  fear:    '무서워하는 것 같아요',
  nectar:  '꽃이 더 있어야 할 것 같아요',
  food:    '먹을 게 없나 봐요',
  shelter: '숨을 데가 없나 봐요',
  shade:   '그늘이 부족한가 봐요',
  damp:    '너무 말랐나 봐요',
};
ui.setHabitatLabels?.(TAG_LABEL, HABITAT_BLOCK_LINE);
```

`ghostRing` 선언부 근처에:

```js
let lastNearMiss = null;   // 🦋 같은 근접 신호를 GA4 로 반복해 쏘지 않기 위한 기억
```

`updateDecorGhost()`(7858행) 끝에:

```js
  // 🦋 배치 중에만 환경 미터를 띄운다 — 정보가 필요한 순간이 정확히 지금이다
  if (atFarm && decorGhost && (placingOutdoor || pickedOutdoor)) {
    const env = habitatEnvAt(decorGhost.position.x, decorGhost.position.z);
    const near = nearMiss(env, habitatCtx(), gameState.dex.visitor || {});
    ui.setHabitatMeter?.(env, near);
    if (near && near.visitor !== lastNearMiss) {
      lastNearMiss = near.visitor;
      trackEvent('visitor_nearmiss', { visitor: near.visitor, blocker: near.blocker });   // [GA4] 퍼널 2단 — blocker 가 튜닝 축
    }
  }
```

`removeDecorGhost()` 안에:

```js
  ui.setHabitatMeter?.(null); lastNearMiss = null;
```

`buildDecorGhost(id, outdoor)` 안에 (배치 모드 진입 1회):

```js
  if (atFarm && outdoor) trackEvent('habitat_meter', { farm_stage: gameState.farm.stage });   // [GA4] 퍼널 1단
```

- [ ] **Step 5: 브라우저에서 확인한다**

1. 텃밭에서 🌷꽃밭 배치 모드 진입 → 미터가 뜬다
2. 기존 꽃밭 옆으로 움직인다 → `🌷 꽃 ●●` 이 늘어난다
3. 🎃허수아비 근처로 간다 → `🎃 공포 ●●●` + `❓ 무언가 맴돌다 갔어요 — 무서워하는 것 같아요`
4. 배치를 확정하거나 취소한다 → 미터가 사라진다
5. `resize_window`로 375 폭에서 컨텍스트 슬롯·프롬프트 줄과 겹치지 않는지 확인한다

- [ ] **Step 6: 커밋**

```bash
git add js/game.js index.html
git commit -m "feat: 🦋 배치 환경 미터 + 근접 신호 · GA4 퍼널 1·2단"
```

---

### Task 6: 방문객 3D 조형 4종

**Files:**
- Create: `js/visitor-art.js`
- Modify: `js/game.js` — `makeVisitorMesh`를 교체하고 임시 색 상수를 지운다

**Interfaces:**
- Consumes: 없음(주입받은 THREE만)
- Produces: `makeVisitor(THREE, id) -> THREE.Group` — `position.y`를 자기가 정한다

- [ ] **Step 1: 🐸 를 만든다 (사양 확정됨)**

2026-09-17에 사용자가 레퍼런스를 지정했다. **레퍼런스 이미지는 워터마크가 박힌 스톡 에셋이므로 쓰지 않고 특징만 재현한다.**

| 요소 | 사양 |
|---|---|
| 실루엣 | 몸통 없이 둥근 머리 하나. 가로로 약간 넓은 타원(약 1 : 0.9) |
| 눈 | 머리 윤곽 **위로 튀어나온** 흰 구 2개 + 검은 동공 |
| 동공 | 정면 고정, 흰자 대비 약 40%. 하이라이트 없음 |
| 콧구멍 | 눈 사이 아래 작은 검은 점 2개 |
| 입 | 아래로 볼록한 굵은 곡선 하나 |
| 볼터치 | 연분홍 사선 2줄씩 양볼 |
| 색 | 연두 단색. 아웃라인 없음 — 검정은 눈·코·입에만 |

⚠️ 정면 2D 도안이라 3D로 옮기면 측면이 빈다. 눈은 별도 구체로 붙이고, 입·콧구멍·볼터치는 얕은 디칼이나 텍스처로 처리해 정면성을 유지한다.

- [ ] **Step 2: 나머지 3종 사양을 확정받는다**

🦋🐦🦔 는 같은 형식(실루엣·눈·색·특징)으로 사람에게 확정받은 뒤 만든다. 4종 모두 **머리가 몸의 대부분**을 차지하는 비율로 통일한다(기존 플러시 캐릭터 톤).

- [ ] **Step 3: `makeVisitorMesh` 를 교체한다**

```js
import { makeVisitor } from './visitor-art.js';   // 🦋 방문객 조형
// ...
function makeVisitorMesh(id) { return makeVisitor(THREE, id); }
```

`VISITOR_TMP_COLOR` 상수를 지운다.

- [ ] **Step 4: 드로우콜을 확인한다**

브라우저 콘솔에서 방문객이 없을 때와 2마리일 때를 비교한다:

```js
renderer.info.render.calls
```

차이가 5 이내여야 한다. 넘으면 눈·볼터치를 부모 지오메트리에 병합한다.

- [ ] **Step 5: 커밋**

```bash
git add js/visitor-art.js js/game.js
git commit -m "feat: 🦋 방문객 4종 조형 — 🐸 는 2026-09-17 레퍼런스 사양"
```

---

### Task 7: i18n · 소식함 공지 · 마무리 검증

**Files:**
- Modify: `js/i18n-en.js`
- Create: 소식함 공지 SQL

**Interfaces:**
- Consumes: Task 1~6의 모든 사용자 노출 문구

- [ ] **Step 1: 영어 사전을 채운다**

`js/i18n-en.js`의 `EN` 객체에. 한국어 원문이 키다:

```js
  '🌿 이 자리': 'Right here',
  '아직 허전해요': 'Still bare',
  '🌷 꽃': '🌷 Flowers',
  '🏮 빛': '🏮 Light',
  '🧱 은신처': '🧱 Shelter',
  '💧 물기': '💧 Moisture',
  '🌑 그늘': '🌑 Shade',
  '🌾 먹이': '🌾 Food',
  '🎃 공포': '🎃 Fear',
  '🦋 방문객': '🦋 Visitors',
  '호랑나비': 'Swallowtail',
  '참새': 'Sparrow',
  '고슴도치': 'Hedgehog',
  '청개구리': 'Tree Frog',
  '꽃이 많은 곳': 'Somewhere full of flowers',
  '먹을 게 익은 곳': 'Somewhere with ripe food',
  '숨을 데가 있는 어두운 곳': 'A dark spot with places to hide',
  '축축하고 그늘진 곳': 'Somewhere damp and shaded',
  '무서워하는 것 같아요': 'something seems afraid',
  '꽃이 더 있어야 할 것 같아요': 'it wants more flowers',
  '먹을 게 없나 봐요': 'there is nothing to eat',
  '숨을 데가 없나 봐요': 'there is nowhere to hide',
  '그늘이 부족한가 봐요': 'it needs more shade',
  '너무 말랐나 봐요': 'the ground is too dry',
  '❓ 무언가 맴돌다 갔어요 — {0}': '❓ Something circled by and left — {0}',
  '{0}가 다시 찾아왔어요': '{0} came back',
```

⚠️ 근접 신호와 재방문 토스트는 **패턴 슬롯 `{0}`** 으로 넣는다. `" · "` 글루처럼 문자열을 이어 붙이면 키가 안 맞아 한국어가 그대로 노출된다. Task 5 Step 3의 템플릿 리터럴이 슬롯 형태와 일치하는지 확인한다 — 안 맞으면 i18n 쪽이 아니라 **호출부를 슬롯 꼴로** 고친다.

- [ ] **Step 2: 커버리지를 점검한다**

```bash
node scripts/i18n_check.mjs
```

Expected: 신규 문구가 미번역 목록에 없다.

- [ ] **Step 3: 소식함 공지를 쓴다**

`DEX_TOTAL`이 33 → 37로 늘어 기존 완성자의 카운터가 `33/37`로 보인다. `awardBadge()`는 회수하지 않고(`js/game.js:1263`) 완성 보상 재지급도 막혀 있지만(`:1201`), 안내 없이 두면 "내 도감이 깨졌다"로 읽힌다.

기존 공지 형식을 확인하고 같은 꼴로 1건 작성한다:

```bash
ls *.sql sql/ supabase/ 2>/dev/null
grep -rn "insert into notices\|INSERT INTO notices" --include=*.sql . | head -3
```

본문 요지: 텃밭에 🦋방문객 4종이 새로 생겼고, 도감 총계가 37종으로 늘었으며, 이미 다 모은 분의 배지는 그대로라는 것.

- [ ] **Step 4: 전체 검증**

```bash
npm test
```

Expected: 전체 PASS.

브라우저에서 4종을 전부 재현한다:

| | 재현 방법 |
|---|---|
| 🦋 | 꽃밭 2개, 허수아비 거리 6 밖 |
| 🐦 | 작물 한 칸 다 키우기, 허수아비 거리 6 밖 |
| 🦔 | 밭 가장자리에 돌담 2개 · 밤 |
| 🐸 | 💧우물 + 밭 가장자리 · `?weather=rain` |

`?weather=rain`은 `js/game.js:158`이 지원한다. 밤은 시간 경과를 기다리거나 `js/daynight.js`의 테스트 훅을 쓴다.

세이브 왕복도 확인한다: 방문객 1종을 등록 → 새로고침 → 도감에 남아 있고, 같은 정원에서 다시 온다.

- [ ] **Step 5: 커밋**

```bash
git add js/i18n-en.js
git commit -m "feat: 🦋 방문객 도감 영어 문구"
```

---

## Self-Review

**스펙 커버리지**

| 스펙 항목 | 태스크 |
|---|---|
| 환경 점수 모델 · 태그표 · 반경 | Task 1 |
| 방문객 4종 · 난이도 곡선 | Task 1 |
| 태그 소스는 세이브에 남는 것만 | Task 1 (회귀 테스트로 잠금) |
| 생명주기 · 재방문 · 1초 스로틀 | Task 3, 4 |
| 도감 실루엣 · 발견 후 조건 공개 | Task 2 Step 6 |
| 배치 미터 · 근접 신호 | Task 5 |
| `DEX_NEVER` 함정 | Task 2 |
| 박물관 2층 · 새 층 금지 | Task 2 |
| 기존 유저 영향 · 소식함 공지 | Task 7 |
| 저장은 `dex.visitor` 만 | Task 2 (b)(c), Task 3 |
| 트래킹 퍼널 4단 | Task 5 (meter·nearmiss), Task 4 (spawn·dex_discover) |
| 파일 분리 | Task 1, 4, 6 |
| i18n | Task 7 |
| 3D 에셋 4종 | Task 6 |

**타입 일관성 확인**

- `envAt(src, x, z)` — Task 1 정의, Task 3·5에서 같은 시그니처로 호출 ✅
- `nearMiss(env, ctx, known)` — Task 1 정의, Task 5에서 `gameState.dex.visitor || {}` 전달 ✅
- `deps.matchVisitors` — Task 4가 주입받는 이름과 Task 1의 export 이름 일치 ✅
- `markHabitatDirty()` — Task 3 정의, Task 3 Step 3에서만 호출 ✅
- `ui.setHabitatMeter(env, near)` / `(null)` — Task 5 정의·호출 일치 ✅
- `makeVisitorMesh(id)` — Task 4 임시 정의, Task 6에서 같은 이름으로 교체 ✅

**남은 판단 지점(구현 중 사람 확인)**

- **Task 2 Step 6** — 도감 모달이 미보유를 이미 가리는지. 안 가리면 `visitor`만 가리는 처리를 그 자리에서 넣는다
- **Task 5 Step 1** — 사용자 노출 문구 검수 게이트
- **Task 6 Step 2** — 🦋🐦🦔 조형 사양 확정 게이트(🐸만 확정됨)
