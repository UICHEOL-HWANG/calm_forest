# 🌿 calm forest

동물의 숲 톤의 포근한 로우폴리 3D 브라우저 게임. **Vanilla JS + Three.js(WebGL)** 로 만들었고, **외부 이미지·모델·사운드 파일 없이** 모든 형상·효과음을 코드로 생성합니다.

🎮 **플레이**: https://calmforest.icuchoel.workers.dev/

**주요 기능**: 걷기 · 벌목 · 농사(밭갈기/심기/물주기/수확/시들기) · 건축(단계 건설) · 집 실내 입장 & 꾸미기 · 낚시 · NPC 6명 & 퀘스트 체인(총 17개+데일리) · **출석 보상(연속 스트릭) & 데일리 의뢰(럭키박스)** · **비 오는 날씨(자동 성장·낚시 행운)** · 주민 선물 & 친밀도 · **작업대 제작(요리·도구·야외장식·선물)** · **상점 & 코인 경제** · **개인 텃밭 필드** · 낮·밤 그라데이션(+수동 조절) · 별·반딧불이·창문 불빛 · 파티클 & 블룸 · 절차적 사운드 · 모바일 터치 · 인터랙티브 튜토리얼(11단계) · 구글 로그인 & 게스트 · 자동 클라우드 저장 · 행동 데이터 분석(Supabase/GA4/BigQuery, A/B 세그먼트)

기술 스택: **Vanilla JS + Three.js**(프론트) · **Supabase**(Auth/Postgres) · **GA4 + BigQuery**(분석) · **Cloudflare Pages/Workers**(호스팅) · **GitHub Actions**(데이터 파이프라인).

## 📁 파일 구조

```
calm_forest/
├─ index.html            # 게임 진입점 · UI · importmap · 오케스트레이션 · OG 메타
├─ preview.png           # 링크 미리보기(OG) 커버 이미지 (1200×630)
├─ README.md
├─ js/                   # 🎮 게임 소스 (ES 모듈)
│  ├─ config.js          #   ⚙️ 설정(키/ID, 로깅 간격, A/B EXPERIMENT 스위치)
│  ├─ supabase-client.js #   🔐 로그인·저장/불러오기·로그 전송·세그먼트(client_id/is_guest/variant)
│  ├─ analytics.js       #   📈 GA4 이벤트 + gtag 자동 로딩
│  ├─ logger.js          #   🛰️ 센서 데이터 throttle 수집 + 배치 전송
│  ├─ game.js            #   🌳 3D 씬·걷기·농사·건축·낚시·NPC·제작·상점·텃밭·파티클·블룸
│  ├─ metrics.js         #   📐 계측: 경제 원장(econ_logs) + 세션 요약(session_logs)
│  ├─ controls.js        #   📱 모바일 가상 조이스틱 + 액션 버튼
│  └─ sound.js           #   🔊 절차적 효과음(WebAudio)
├─ dashboards/           # 📊 분석 대시보드 (브라우저에서 열기)
│  ├─ analytics.html     #   본인 데이터: 이동 히트맵·세션·시선
│  └─ admin_analytics.html #  관리자: 전체 유저 합산 통계
├─ sql/                  # 🗄️ Supabase / BigQuery SQL
│  ├─ supabase_setup.sql #   테이블·RLS·분석 뷰 생성 (한 번 실행)
│  ├─ admin_analytics.sql#   관리자 집계 보안함수
│  ├─ analytics_queries.sql # 전체 유저 집계 쿼리 팩 (E: 세그먼트·A/B·pre/post)
│  ├─ bigquery_queries.sql  # BigQuery 쿼리 팩 (GA4 + 행동로그 세그먼트/AB)
│  ├─ quality_checks.sql #   🩺 계측 품질 체크(체험단 전 검증용)
│  ├─ migrate_ab_fields.sql    # (기존 DB) client_id·is_guest·variant 추가+백필
│  ├─ migrate_ab_fields_bq.sql # (BigQuery) 동 컬럼 추가+variant 백필
│  └─ migrate_metrics_tables.sql # 📐 계측 테이블(econ_logs·session_logs) 생성+RLS
├─ scripts/              # 🔄 Supabase→BigQuery 일일 적재+경량화+익명계정 정리
│  └─ export_to_bq.py
├─ .github/workflows/    # ⚙️ GitHub Actions
│  ├─ ci.yml             #   dev/PR 구문 검사(배포 아님)
│  ├─ supabase-to-bq.yml #   매일 cron 데이터 파이프라인
│  └─ deploy.yml         #   (비활성) 옛 GitHub Pages 배포 — Cloudflare로 이전
└─ docs/                 # 📚 문서 (DEPLOY / GA4_GUIDE / DATA_PIPELINE)
```

> 실제 연동(Supabase 키·GA4)은 `js/config.js`, DB 초기화는 `sql/supabase_setup.sql` 참고. 키를 안 넣어도 게임은 오프라인 폴백으로 동작합니다.

## 🔭 데이터 분석 구조 (한눈에)

계측은 게임 클라이언트에서 시작해 두 갈래로 흐릅니다. **행동 로그**(좌표·세션)는 Supabase→BigQuery로, **이벤트**(벌목·수확·제작·상점 등)는 GA4→BigQuery로.

```
[게임 클라이언트]
   ├─ logger.js  ── game_logs 배치(좌표·마우스·카메라 + client_id·is_guest·variant)
   ├─ metrics.js ── econ_logs(코인 원장) + session_logs(세션 요약: 행동 카운트·플레이 시간)
   │        ▼
   │   Supabase(Postgres, RLS "본인만")  ──일일 파이프라인(GitHub Actions)──▶  BigQuery
   │        · 7일치만 보관(hot) · 익명계정 7일 뒤 정리        scripts/export_to_bq.py   (전체 이력, cold)
   │        ▼
   │   dashboards/ (본인·관리자 대시보드)
   │
   └─ analytics.js ── GA4 이벤트 ──▶ GA4 ──(native export)──▶ BigQuery(analytics_*)
```

세그먼트 필드 — `client_id`(영구 기기 ID, 게스트 재방문/리텐션), `is_guest`(게스트 vs 로그인), `variant`(A/B, 기본 `control`). A/B는 `js/config.js`의 `EXPERIMENT`로 켬. 체험단/런칭 전에는 `sql/quality_checks.sql`로 계측 정상 여부를 먼저 확인.

## ▶ 로컬 실행

ES 모듈이라 `file://`로 열면 CORS로 막힙니다. 로컬 서버로 여세요:

```
cd calm_forest
python3 -m http.server 8000   # → http://localhost:8000
```

## 🚀 배포 (Cloudflare Pages/Workers)

호스팅은 **Cloudflare**를 사용합니다. GitHub 레포를 Cloudflare Pages에 연결하면 `main` push마다 자동 배포됩니다.

- **빌드 설정**: Framework preset `None` · Build command 비움 · Output directory `/` (순수 정적, 빌드 없음)
- **Production branch**: `main`
- **브랜치 운영**: `dev`에서 작업 + 로컬 테스트 → `main` 머지 시에만 배포 (빌드 횟수 절약)
- **커스텀 도메인**: Cloudflare 대시보드 → Pages → Custom domains 에서 연결
- **OAuth**: 주소가 바뀌면 Supabase → Authentication → URL Configuration → Redirect URLs에 새 주소(`https://.../**`) 추가 필요 (`redirectTo`는 현재 origin 기준 동적)

> 데이터 파이프라인(`supabase-to-bq.yml`)과 CI(`ci.yml`)는 호스팅과 무관하게 GitHub Actions에서 계속 돌아갑니다. 옛 `deploy.yml`(GitHub Pages)은 비활성 상태입니다.

## 🎮 조작

**하단 도구 하트바에서 도구를 고르고 상호작용합니다.** 도구 선택은 숫자키 `1~7`(🪓도끼·⛏️괭이·🌰씨앗·💧물조리개·🌾낫·🔨망치·🎣낚싯대) 또는 슬롯 탭, 상호작용은 `Space`/클릭(모바일은 액션 버튼).

| 키 | 도구 | 하는 일 |
|----|------|---------|
| 1 | 🪓 도끼 | 나무 벌목 (3번 → 쓰러짐, 목재 +3) |
| 2 | ⛏️ 괭이 | 빈 땅 갈아 밭 만들기 / 시든 밭 다시 갈기 |
| 3 | 🌰 씨앗 | 갈아둔 밭에 씨앗 심기 |
| 4 | 💧 물조리개 | 자라는 작물에 물주기(물 줘야만 성장) |
| 5 | 🌾 낫 | 다 자란 작물 수확 (작물 +1, 씨앗 +2) |
| 6 | 🔨 망치 | 집 터에서 목재 10개씩 단계 건설 |
| 7 | 🎣 낚싯대 | 호숫가에서 낚시 |

- **이동**: WASD·방향키 (모바일은 좌측 가상 조이스틱)
- **저장**: 전부 자동(30초 주기 + 탭 숨김/종료 시) — 수동 저장 버튼 없음
- **모바일 액션 버튼**은 상황에 따라 아이콘이 바뀝니다: 도구 · 💬주민대화 · 🚪문 · 🍳작업대 · 🛒상점 · 🌾텃밭 · 가구/장식 배치
- 우측 상단 인벤토리(🪵목재 🌰씨앗 🥕작물 🐟물고기 🪙코인) + 낮/밤 인디케이터 상시 표시

## 🌱 농사

**괭이**로 빈 땅을 갈면 이랑 밭이 생기고, **씨앗**을 심고 **물조리개**로 물을 주면 작물이 3단계(🌱→🌿→🍎)로 자랍니다. **물을 줘야만 자라고**, 오래 방치하면 🥀 시들어 괭이로 다시 갈아야 해요. 다 자라면 **낫**으로 수확(작물 +1, 씨앗 +2). 작물 종류(당근·토마토·블루베리·호박)는 랜덤 배정.

## 🏗️ 건축 · 🏠 집 · 🎨 꾸미기

맵의 반투명 집 터에서 **망치**로 목재 10개씩 3단계(데크→통나무벽→지붕) 건설. 완성하면 문으로 **실내 입장** → 상단 **🎨 꾸미기**로 러그·화분·의자·테이블·램프·소파·어항을 작물/물고기로 사서 바닥에 배치. 밤엔 방 안이 은은하게 밝아집니다.

## 🎣 낚시

호숫가에서 **낚싯대**로 던지고, **"❗ 물었어요!"** 뜰 때 다시 눌러 낚아채는 반응 미니게임. 등급별로 피라미·붉은 물고기·**무지개 물고기(희귀)**.

## 🧑‍🌾 NPC · 퀘스트 · 선물

마을 주민 5명(농부·목수·상인·낚시꾼·요리사 판다)이 각자 퀘스트 체인을 줍니다. 근처에서 대화(모바일은 **대화하기** 버튼) → 수락 → 목표 달성 → 보상(자원+🪙코인). **작업대에서 만든 선물**을 주민에게 주면 **친밀도 ❤️** 가 오르고, 일정 단계마다 답례를 받아요.

## 🎁 출석 보상 · 📋 데일리 의뢰 · 🌧️ 날씨

- **출석 보상** — 매일 첫 접속 시 코인 지급. **연속 출석(스트릭)** 일수록 커지고(5→30🪙), 7일 연속마다 💎보석 보너스.
- **데일리 의뢰** — 스폰 근처 **의뢰 올빼미 🦉** 가 매일 3개 의뢰(벌목·수확·낚시·채굴 등, 날짜 시드라 전원 동일)를 줍니다. 완료하면 코인 + **🎁럭키박스**(확률: 코인 60%/씨앗 25%/💎보석 10%/대박 5%).
- **날씨** — 날짜 시드로 매일 결정(전원 동일): 맑음 55% · 🌧️비 20% · ❄️눈 12% · 🌫️안개 13%. **비**: 빗줄기+빗소리, 밭이 물 없이 저절로 자라고 낚시 입질↑. **눈**: 흩날리는 눈송이, 벌목 목재 +1 확률. **안개**: 짙은 안개+낮에도 반딧불이, 동굴 보석 확률 2배. 실내·동굴엔 날씨 연출 없음. 테스트: `?weather=rain|snow|fog`.
- **🔮 내일 예보** — 출석 보상 카드와 의뢰 올빼미의 완료 대사가 **내일 날씨를 예고**합니다("내일은 🌧️ 비 소식 — 밭이 저절로 자라는 날!") — 내일 또 올 이유.

## 📖 도감 (+ 회원 전환 훅)

물고기 3종·작물 4종·광물 3종·요리 3종·이웃 6명, **총 19종**을 처음 발견하면 도감에 기록됩니다(인벤토리 아래 📖 도감 버튼 — 모바일에서도 항상 표시, 미발견은 ??? 실루엣). 전부 모으면 **🪙150 보상**. **게스트에겐 회원 유치 장치**로 동작해요 — 게스트 도감은 휘발성이라, 도감 모달 하단 배너와 수집 마일스톤(5·12종) 넛지가 "구글 로그인하면 영구 보존 + 기기 연동"을 안내합니다. 노출→클릭→로그인이 전부 트래킹되어(`login_nudge` → `login_nudge_click` → `login`) **게스트→회원 전환 퍼널**을 분석할 수 있습니다.

## 🛠️ 작업대 (제작)

스폰 근처 작업대에서 4개 탭으로 제작합니다:

- **🍳 요리** — 작물/물고기로 음식 제작 → 일시 버프(이동속도·낚시행운·벌목보너스)
- **🔧 도구** — 강철 도끼(2번 벌목)·큰 물조리개·튼튼한 낚싯대·🍲큰 냄비(요리 버프 1.5배, 돌+석탄) (영구 업그레이드)
- **🌷 야외** — 울타리·디딤돌·꽃밭·정원등을 마당 원하는 곳에 설치
- **🎁 선물** — 주민에게 줄 선물 제작 · 📿보석 목걸이(💎보석 소비)는 친밀도 +2 최상급 선물

## 🛒 상점 · 🪙 코인

상점 좌판에서 **작물·물고기·목재를 팔아 코인**을 벌고(탭=1개, 꾹=전부), **씨앗 등을 구매**. 코인 경제가 다른 시스템의 화폐 기반이 됩니다. **📊 오늘의 시세** — 판매가가 날짜 시드로 매일 0.7~1.3배 변동(전원 동일). **상점 옆 시세 전광판**에 오늘의 최고/최저 품목이 크게 표시되고, 가까이 가서 상호작용하면 전체 시세판(품목별 가격·등락 + 내일 예보)이 열립니다. 판매 탭에도 등락 뱃지 표시 — **팔 타이밍 전략**이 생깁니다.

## 🌾 개인 텃밭

스폰 앞 게이트로 들어가는 **나만의 텃밭 필드**(집처럼 별도 공간). 울타리 두른 넓은 밭에서 자유롭게 농사짓고, 심은 작물은 저장돼 재접속해도 유지됩니다.

## 🙂 이모트 · 📷 액션샷

- **이모트** — 🙂 버튼에서 기분을 고르면 캐릭터가 **실제 모션**을 합니다: 👋 까딱 인사 · 😄 두 번 폴짝 · ❤️ 한 바퀴 돌며 반짝 · 🎵 빙글빙글 스텝 댄스. 🪑 앉기는 그대로.
- **사진(액션샷)** — 📷 버튼을 누르면 카메라가 **캐릭터 정면에 확 밀착**하고 랜덤 포즈가 발동, **포즈의 정점 프레임**에서 자동 캡처됩니다. 촬영 후 카메라는 부드럽게 복귀. 저장/공유는 기존과 동일.
- **공유 카드 합성** — 캡처된 사진에 크림 프레임 + 하단 정보 바(🌿 calm forest 타이틀·게임 주소·오늘 날씨·📖 도감 진행도·날짜)가 캔버스로 자동 합성됩니다. 저장/공유 시 그대로 홍보 카드가 되어 유입 링크 역할을 합니다.

## 🌅 분위기 · 🔊 사운드 · 📱 모바일

거리 안개·비네팅·따뜻한 컬러 그레이딩, 자정→여명→아침→정오→노을→밤 7단계 낮/밤 그라데이션(우측 상단 시간 바 드래그로 수동 조절). 효과음은 전부 WebAudio 실시간 합성(오디오 파일 없음). 터치 기기에선 좌측 조이스틱 + 우측 액션 버튼이 자동 표시되고, 메뉴/모달은 모바일 화면에 맞춰 반응형으로 동작합니다.

상단 좌측 툴바는 자주 쓰는 **📷 사진 · 🙂 이모트**만 노출하고, 나머지(❓ 도움말 · 📊 활동 기록 · 🎵 배경음악 · 🏠 집 꾸미기 · 🚪 나가기/로그아웃)는 **☰ 메뉴** 안에 아이콘+글자 라벨 리스트로 정리되어 있습니다(데스크톱·모바일 공통).

## 🆕 튜토리얼

첫 로그인 시 이동→벌목→밭갈기→씨앗→물주기→수확→**판매→시세 확인**→주민 퀘스트→**채굴→도감 확인**→낚시→집짓기→입장→꾸미기 **15단계 인터랙티브 코치**가 안내(1회). 경제(팔기·시세)와 채굴까지 초보자가 자연스럽게 발견하도록 확장. 좌측 상단 **❓** 버튼으로 다시 볼 수 있어요.

## 🔑 실제 연동 (플레이스홀더 교체)

`js/config.js` 상단만 바꾸면 됩니다. **값을 안 바꿔도 오프라인 폴백으로 정상 플레이**되며 저장/로그는 콘솔에 출력됩니다.

```js
SUPABASE_URL: 'https://xxxx.supabase.co',
SUPABASE_ANON_KEY: 'sb_publishable_...',   // 브라우저 안전(RLS로 보호)
GA4_MEASUREMENT_ID: 'G-XXXXXXXXXX',
EXPERIMENT: 'off',                          // 'map' 으로 켜면 A/B 배정 시작
```

## 🗄️ Supabase

`sql/supabase_setup.sql` + `sql/migrate_metrics_tables.sql`을 SQL Editor에 한 번씩 실행하면 테이블·RLS·분석 뷰가 생성됩니다. **RLS(본인 데이터만)** 로 보호되며, **게스트 수집을 쓰려면 Authentication → Anonymous sign-ins 활성화** 필요. game_saves(진행도 jsonb) + game_logs(행동 로그) + econ_logs(코인 원장) + session_logs(세션 요약) + feedback 테이블로 구성.

## 💾 저장 데이터

`game_saves.state`(jsonb)에 인벤토리·집 단계·밭·NPC 퀘스트·친밀도·업그레이드·야외장식·선물·코인·시간대가 함께 저장됩니다. **30초마다 + 탭 숨김/종료 시 자동 저장**되어 창을 닫았다 열어도 이어지고, 구글 로그인 시 기기 간에도 이어집니다.
